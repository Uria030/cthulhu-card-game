# E12 續作工單 — 揭穿傳說收尾(細項拆解)

> 接續現有工作樹(sharedActions.ts + resolver 已在),規格母文件:`docs/E12規格_ACT共用動作_揭穿傳說_v0_1_26070211.md`。
> 逐項做、逐項勾;每項含驗收。全部完成才 Gate 交件(方式一送守燈人本尊)。日期碼 26070501。

## A|引擎收尾

- [ ] **A1 雙機制擇一**:`sharedActions.ts` 的 `resetSharedActionUsesForNewTurn` 與「使用紀錄綁 turnNumber 自動過期」二擇一(建議留自動過期、刪 reset 函式);留下的機制加註解說明回合邊界語意。驗收:grep 無死碼,單測覆蓋跨回合重置。
- [ ] **A2 migration 定稿**:shared_actions 欄位落到 act 卡實際儲存處(依現 schema 查明 act_cards 存哪張表),`ADD COLUMN IF NOT EXISTS` 冪等;**同 migration 對雨夜的真相 ACT2 回填一筆**:`[{"code":"expose_legend","name_zh":"揭穿傳說","target_variant":"G1_deep_one_slit_mouth","ratio":1,"team_limit_per_turn":1,"narrative":"你們把線索拼成完整的真相——傳說最怕被人看穿。"}]`,回填加「僅當欄位為空」守衛。驗收:migration 重跑不疊加;prod bootstrap 的 ACT2 帶出 shared_actions。
- [ ] **A3 API 補齊**:stages.ts create/update 對 shared_actions 的寫入與回讀(草稿已起頭);admin 端點能改此欄。驗收:PATCH 後 GET 一致。

## B|AI 接入(規格 §實作要點 5;缺這塊 AI 測不出新 meta)

- [ ] **B1 候選生成**:`enumerateCandidates` 加 `use_shared_action` 候選。條件:當前幕有該動作+`objectiveProgress>0`+本回合未達 team limit+目標在場存活。amount = min(線索池, 目標剩餘 HP/ratio 向上取整)——不浪費線索過量傷害。價值 = amount×ratio×DMG_V(必中全額),目標為幕目標時吃 objectiveBonus 同級加成。驗收:AI 測試 D3。
- [ ] **B2 指揮層需求折抵**:`estimateSubgoalDemand` 的 kill 需求,在當前/後續幕有線索轉傷動作時,需求 AP 扣除「池內線索可轉傷害」的等效量;detail 字串註明折抵。驗收:指揮層測試 D4。
- [ ] **B3 ⚠ 模擬管線接線(本包最容易漏的雷)**:`investigatorAI.ts` 的 `simulateStep` 建 RuleContext 時**目前沒傳 actCards**——不補的話 beam search 模擬永遠看不到共用動作,B1 的候選會在前瞻中被真引擎駁回。`InvestigatorAIContext` 加 `actCards?`,simulateStep 與 runInvestigatorAITurn 的 ruleCtx 都帶上。驗收:planTurn 測試裡 AI 能在模擬中選出並通過 use_shared_action。
- [ ] **B4 本體 ruleCtx 接線**:`TestScenarioScreen` 的玩家 intent 與 AI 回合的 RuleContext 補 `actCards: setup.actData`;sim 腳本同步。驗收:sim log 出現 shared_action_used。

## C|UI(戰鬥板)

- [ ] **C1 按鈕與資格**:動作列「揭穿傳說」鈕,僅資格成立時顯示(當前幕有+目標在場);鈕上或旁顯示「線索池 N・本回合已用/可用」。
- [ ] **C2 數量選擇**:棄多少線索的選擇介面(1~池;預設帶 B1 同款的最適量);實作形式自決(滑桿/加減鈕皆可)。
- [ ] **C3 演出與紀錄**:接 `shared_action_used`/`attack_hit`/`clues_spent` 效果進三段演出與戰役紀錄;narrative 用資料欄的句子。驗收:實點一次,演出、log、線索池、boss HP 四處一致。

## D|測試

- [ ] **D1 引擎測試**(ruleEngine.test.ts):①資格不成立駁回(幕無動作/目標不在場)②線索池不足駁回 ③傷害 = amount×ratio 且走 enemyDamageAfterDefense ④擊殺 → enemy_defeated ⑤回合上限:同回合第二次駁回 ⑥次回合可再用(turnNumber 過期)。
- [ ] **D2 多人一致性回歸**:同回合兩席先後 use_shared_action,後到者被駁回且駁回訊息是情境敘事(非冷錯誤)。
- [ ] **D3 AI 測試**(investigatorAI.test.ts):ACT2+池 5+boss 剩 3 HP → AI 生成候選且 amount=3(不過量);候選價值高於同局面徒手攻擊。
- [ ] **D4 指揮層測試**(investigatorCommand.test.ts):同局面 kill 需求 AP 因線索折抵而下降,detail 含折抵註記。

## E|交件前驗證

- [ ] **E1 全套複跑**:shared 全測試檔+client tsc。
- [ ] **E2 sim 單種子**:`--seed 42` 全場跑通,log 見揭穿傳說至少一次使用;貼進 handoff。
- [ ] **E3 handoff**:照 Gate 格式,含本工單勾選狀態;**方式一送守燈人本尊**(動引擎核心)。

## 範圍外(明確不做)

- 交叉測試複跑(合流後由守燈人跑)、個人線索持有化(規則書對賬時另議)、法器互動(E3 已處理的不動)。
