# E14 工單 — Playtest 修正批次 #1(Uria 實測五項)

> 來源:Uria 2026-07-05 首輪實測回饋。逐項勾,全綠 Gate 交件送守燈人 review。日期碼 26070504。
> 修 bug 附回歸測試;UX 項附截圖進 handoff。

## 1|選誰都顯示「鐵證」

- [ ] **1a 診斷**:選人→顯示鏈已查起點——`selectedInvestigator.ts` 存取正常、`TestScenarioScreen:453` 會讀。嫌疑:①大廳 64 選人面板選定後**沒呼叫 `setSelectedInvestigator`**(只更新 party?)②關卡簡報/進場畫面(`ScenarioBriefingScreen`)顯示名取了寫死值或 fallback(null → 第一位 completed = 鐵證)。逐段對照修正。
- [ ] **1b 驗收**:選任一位(含草稿)→ 進入關卡前後所有顯示(簡報/戰鬥板名牌/結算)都是所選調查員;selectedInvestigator 為 null 時顯示「未選擇」而非默默 fallback 鐵證。

## 2|移除世界地圖的「三地點測試關卡」

- [ ] **2a** 它與「雨夜的真相」同用途重複。**用資料層隱藏,不刪 stage**(可能被教學/舊流程引用):stages 加 `is_hidden` 欄(migration,冪等)或沿用既有 flag;世界地圖別針過濾 is_hidden。
- [ ] **2b 驗收**:世界地圖只見「雨夜的真相」;直連舊關卡 URL 不炸(顯示「此關卡已下架」之類)。

## 3|AI 行動零延遲(體感核心)

- [ ] **3a 設計依據**:Uria 6/13 已裁定的動作節奏原則(`project_action_timing_pacing`):**每個動作有預設秒數與時間差**——目的 A 節奏感、B 遮蔽延遲載入;三段演出(敘述→檢定→結果)各有延遲。現況:AI 一席的整回合(最多 3+ 動作)瞬間全部落地。
- [ ] **3b 實作**:AI 的**每個動作**之間插入可調延遲(常數收 config,建議預設:動作間 900-1200ms、三段演出內部節拍沿用玩家側既有值);動作逐個進 Log 而非整批;延遲期間玩家仍可自由行動(不阻塞,維持自由順序裁定)。
- [ ] **3c 紅線**:不得動計時器競態防護模式(訊號→fresh 閉包執行);延遲實作照同一模式(訊號驅動,不在 setTimeout 閉包寫 state)。
- [ ] **3d 驗收**:肉眼可跟上每位 AI 的每個動作;Uria 體感點頭為準。

## 4|Log 收攏態顯示最後三動作

- [ ] **4a** 右側戰役紀錄收攏時,改為顯示**最近 3 條動作行**(滾動即時更新),展開仍是全量。配合 #3 修好後,收攏態就能當「即時字幕」用。
- [ ] **4b 驗收**:收攏態即時看到最新三動作;展開/收攏切換不丟位置。

## 5|手牌卡片介面重設計(資訊完整性)

- [ ] **5a 現況**:放大檢視(a03ab2c)只有打出/消耗/投入鈕,**看不到卡片能力**。
- [ ] **5b 必顯示欄位**(依 s06 卡面規格):名稱/費用/類型(資產·事件·技能·盟友)/陣營、**全部 effects 的 desc_zh**(依【行動】【被動】【反應】框架分行)、投入圖示(commit_icons 逐屬性)、彈藥/使用次數(現值/上限)、盟友 HP·SAN、風味文字(次要層級)。武器另示 combat_style 與傷害。
- [ ] **5c 版式**:照卡面閱讀順序(名稱費用 → 類型行 → 能力區 → 圖示列 → 風味);能力區是主體,佔最大版面;長文字內部捲動不擠壓按鈕列。手機/窄幅可讀(遊戲畫面單屏鐵則)。
- [ ] **5d 驗收**:任抽 5 張不同類型卡(武器/事件/技能/盟友/法器),每張的能力一眼可讀;Uria 過目。

## 交件

- 全項附回歸測試(#1/#2/#3 邏輯類)或截圖(#4/#5 UX 類);client tsc + shared 測試複跑;Gate 交件送守燈人。

---

## 1c|守燈人通盤查詢結果(26070504):顯示名同步規格

**根因**:①64 位草稿 `name_zh` 為空(MOD-11 名字留白設計)②各顯示點各自 fallback 不一致 ③server bootstrap「未帶 investigator 參數 → 靜默退回第一位 completed(=鐵證)」遮蔽了缺參數錯誤。

**單一事實源修法**:

- [ ] **1c-1 displayName helper**:`displayNameFor(inv)` = `name_zh?.trim() || title_zh?.trim() || mbti_code`,放 client 共用(草稿玩家顯示稱號——與 E11 AI 泛用 profile 用稱號當名的規則一致)。全部顯示點一律經此函式,禁止散裝 fallback。
- [ ] **1c-2 SelectedInvestigator 型別補欄**:加 `title_zh`;`pickInvestigator` 存全欄(現況缺 title → 書櫃空白的直接原因)。
- [ ] **1c-3 消滅靜默鐵證**:client 正式流程(簡報→開局)在 `getSelectedInvestigator()` 為 null 時**擋下並導回選人**,不讓 server 預設值上場;server 的無參數 fallback 僅保留給 dev 工具。

**顯示點清單(逐一改用 helper,驗收六處同名)**:

| # | 位置 | 現況 |
|---|---|---|
| 1 | LobbyScreen:222 書櫃「我」名 | `selected.name_zh` 直讀 → 草稿空白 |
| 2 | LobbyScreen:202 椅上影子 name | 同上 |
| 3 | LobbyScreen:284 選人卡格 | 已有 `\|\|` fallback(正確,收斂進 helper) |
| 4 | ScenarioBriefingScreen:111 起 簡報顯示 | 逐段檢查 |
| 5 | gameSetup:441 `investigatorName`(戰鬥板名牌/結算源頭) | `inv?.name_zh ?? '調查員'` — **空字串穿透 ??** → 改 helper |
| 6 | DepartureBoardScreen:36 戰役進度 keyed by id | id 制,無名字問題(確認顯示側) |
| 7 | 結算畫面玩家名 | 沿 investigatorName,隨 #5 修好 |

**驗收矩陣**:選一位草稿(如騙子魔術師)→ 大廳書櫃/椅上影子/簡報/戰鬥板名牌/戰役紀錄/結算 六處顯示一致(= 稱號);選鐵證 → 六處均為其名;未選 → 擋在選人,無任何鐵證殘影。
