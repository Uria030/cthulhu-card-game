# E1 交件 - 遭遇卡觸發接線

日期碼: 26070210

## 範圍

依 `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md` 的 E1 施工:

- 接上遭遇卡四條觸發路徑:回合結束、混沌頭條、城主 encounter mythos、玩家行動/進入地點。
- 新增 stage/scenario 層 `encounter_trigger_config` schema 與 CRUD 保存。
- bootstrap 讀取 stage/scenario trigger config,前端保留 encounter deck。
- TestScenarioScreen 增加遭遇三拍 Modal:敘事 -> 選項 -> 結算。
- 遭遇選項使用既有 `resolveEncounterOption()` 通用結算;不處理 E3 的法器細部破除。

## 主要變更

- `packages/shared/src/game/encounters.ts`
  - 新增 `EncounterTriggerConfig` / `EncounterTriggerContext` / `drawTriggeredEncounter()`。
  - 支援 config normalise/merge 與不觸發時不消耗牌堆。
- `packages/shared/src/game/encounters.test.ts`
  - 增加四條觸發路徑與未觸發不耗牌測試。
- `packages/shared/src/game/keeperAI.ts`
  - `card_category='encounter'` 的 mythos 卡可執行,並給 selection 評分。
- `packages/shared/src/game/keeperAI.test.ts`
  - 增加 encounter mythos 可被城主選出測試。
- `packages/server/src/db/migrate.ts`
  - 新增 `MIGRATION_036`: `stages.encounter_trigger_config`、`scenarios.encounter_trigger_config`。
  - 對 `g_slit_mouth_legend_st1` 補一份保守預設 trigger config。
- `packages/server/src/routes/stages.ts`
  - stage/scenario create/update 支援 `encounter_trigger_config`。
- `packages/client/src/game/gameSetup.ts`
  - `GameSetup` 增加 `encounterCards` / `encounterTriggerConfig`。
  - 合併 stage/scenario trigger config。
- `packages/client/src/screens/TestScenarioScreen.tsx`
  - 接上玩家 action/headline、城主 mythos、turn_end 觸發。
  - 增加遭遇 Modal 與通用選項結算。
  - 遭遇 Modal 開啟時暫停 AI 自動行動。
- `packages/client/src/screens/TestScenarioScreen.css`
  - 遭遇選項按鈕垂直排列,避免長文字擠壓。

## 驗證

```text
> packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\encounters.test.ts
8 passed, 0 failed

> packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\keeperAI.test.ts
18 passed, 0 failed

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\shared\tsconfig.json
exit 0

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\server\tsconfig.json
exit 0

> packages\client\node_modules\.bin\tsc.CMD --noEmit -p packages\client\tsconfig.json
exit 0

> git diff --check
exit 0
```

## 未處理邊界

- 遭遇效果仍走 M1 通用結算;法器/九宮格破除細節保留給 E3。
- 目前 encounter effect schema 未新增全隊 target_rule;若未來遭遇卡需要直接打全隊,應沿用既有 `updatedAllies` 管線擴充,避免前端自行分叉。
- 本次只做本地型別與單元測試;未啟動 dev server 做瀏覽器互動 smoke。

## Review

請守燈人 review:

- `encounter_trigger_config` schema 與 stage/scenario 合併口徑是否可接受。
- 玩家 action 完成後才開 encounter Modal 的排程是否符合三拍演出與不疊 Modal 的工程紀律。
- 城主 encounter mythos 以 category 可執行、前端再抽 encounter pool 的拆法是否符合 E1,或是否應改成 mythos effect action_code 明示。
