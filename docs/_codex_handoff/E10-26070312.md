你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」與 `AGENTS.md` 工程協作模式執行,結論以 PASS/WARN/BLOCK 開頭回覆;本批依 Uria 指示 E3-E10 全部完成後再一次 push,所以本次即使 PASS 也不要 push。

# E10 衝突體感敘事 handoff

## 工作包

- 依據: `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md`
- 項目: `E10 | 衝突體感敘事(②殘件,小,可穿插)`
- 範圍:自由順序下後到 intent 因狀態變化失效時,用情境敘事取代冷駁回。
- 紅線:不得動計時器競態防護;不改裁決邏輯。
- 本次不 push: Uria 已指示 E3-E10 全部完成後一次推送。

## 讀過的必要文件

- `AGENTS.md`
- `docs/_codex_handoff/README.md`
- `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md`
- `docs/M1規劃_一關玩到完整_v0_1_26070208.md` 的工作包②「衝突處理設計」

## 變更摘要

1. 在 `resolveIntent` 共享的 rule engine 內新增 stale intent rejection helper。
2. 只替換以下「狀態已被別的行動改變」時的 rejection 敘事:
   - 穩定救援:目標已經站起/被扶穩。
   - 搜尋:同地點共享可發現資源已被拿走。
   - 攻擊/盟友攻擊/武器攻擊/法術攻擊/嘲諷:指定敵人已倒下或不再可用。
   - 閃避:交戰已解除,或指定敵人不再纏著你。
3. 不改任何 accepted/rejected 判斷;仍然回 `outcome='rejected'`,只換 `rejection.narrative/suggestion`。
4. 完全未碰 `TestScenarioScreen` 的 AI timer / `setTimeout` 競態防護。

## 動過的檔案

- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/ruleEngine.test.ts`
- `docs/_codex_handoff/E10-26070312.md`

## 測試結果

### ruleEngine.test

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\ruleEngine.test.ts
```

Result:

```text
83 passed, 0 failed
```

新增覆蓋:

```text
E10:救援目標已站起時維持駁回,但回饋搶先一步敘事
E10:指定敵人已倒下時維持駁回,但回饋目標失效敘事
E10:共享物資已被取走時維持駁回,但回饋藏物處已空敘事
E10:交戰已解除時維持駁回,但回饋糾纏已解開敘事
```

### TypeScript

Commands:

```powershell
packages\client\node_modules\.bin\tsc.CMD -p packages\shared\tsconfig.json --noEmit
packages\client\node_modules\.bin\tsc.CMD -p packages\client\tsconfig.json --noEmit
packages\server\node_modules\.bin\tsc.CMD -p packages\server\tsconfig.json --noEmit
```

Result:

```text
all exit 0
```

### G1 smoke

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070314 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke
```

Result:

```text
exit 0
SIM_RESULT_JSON seed=2026070314 victory=false outcomeCode=B turnNumber=14 keeperActivations=28
```

註: `victory=false/outcomeCode=B` 是該 seed 的關卡結果,不是 smoke failure;本次檢查重點是引擎不崩、E10 不影響裁決/流程。

### Diff check

Command:

```powershell
git -c safe.directory=C:/Ug/cthulhu-card-game diff --check
```

Result:

```text
exit 0
```

僅有 Windows checkout 的 LF/CRLF warning,無 whitespace error。

## 自知風險 / 請 reviewer 特別看

1. 敵人目標失效的敘事用了「像是有人搶先一步處理了它」,這是體感層近似敘事;引擎目前沒有保存「誰先動」的 actor attribution,所以沒有硬寫具名角色。
2. 本次未新增 result schema 欄位,也沒有新增 `stale` code;維持相容,但未來若要 UI 做專屬 styling,可另加 structured rejection reason。
3. 共享物資耗盡只在 `discoverablePools` 同地點曾有 taken slot 時改敘事;原本就沒有可發現資源的地點仍維持舊提示。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070313)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待批次推送。

#### 內容審查

1. **範圍對賬**:`cfe51fc` 僅 2 code 檔(ruleEngine.ts/.test.ts)+ handoff,恰為 E10 工單範圍;`docs/v07*` 零觸碰;**TestScenarioScreen/AI timer 完全未動**——紅線「不得動計時器競態防護」逐字遵守。✓
2. **裁決邏輯零變動驗證**:逐路徑核對 diff——穩定救援/嘲諷/搜尋/攻擊/盟友攻擊/武器攻擊/施法攻擊/閃避八處,全部維持原 `reject` 出口與 accepted/rejected 判斷,只替換 narrative/suggestion;新增的 stale 分支(taunt 指定敵已倒、ally/weapon/spell attack 有 requestedId)在原程式碼中同樣落入 reject,結果不變、只是敘事更準。✓
3. **複跑測試**(26070313 本機實測):`ruleEngine.test.ts` 83 passed(含 E10 四案例 + Raviel BLOCK 三回歸);shared/server/client 三包 `tsc --noEmit` exit 0。
4. **sim 不崩**:新種子 `2026070315` 全場跑完,城主啟用 28 次,無 crash;E10 不影響裁決與流程。✓
5. **紀律**:無 actor attribution 就不硬寫具名角色(「像是有人搶先一步」近似敘事)——不腦補資料,正確;不加 schema 欄位維持相容,structured rejection reason 留未來,同意。✓

#### 次要備註(不擋)

- 共享物資敘事只在該地點曾有 taken slot 時切換(`takenBy !== null` 檢查),原無資源地點維持舊提示——區分準確。
- 未來 UI 若要對 stale rejection 做專屬樣式,再加 structured `stale` code;本包不需要。

**PASS(不 push,batch push later)。**

