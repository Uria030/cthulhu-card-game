# E12 handoff - ACT shared action "揭穿傳說"

工作包: `docs/E12續作工單_細項_26070501.md`
Codex 交件時間: 2026-07-05
Review: 本輪 Uria 明示「不用送Review」,未呼叫守燈人。

## 變更摘要

- 完成 ACT 共用動作資料模型:新增 `shared_actions` helpers,`ScenarioState.sharedActionUses`,以及 `use_shared_action` intent。
- `ruleEngine` 接入「揭穿傳說」:1 AP、棄隊伍線索、依 `ratio` 造成傷害、走 `enemyDamageAfterDefense`、觸發 `enemy_defeated`,並以 `turnNumber` 自動過期 team limit。
- `MIGRATION_038` 對 `stage_act_cards` 新增 `shared_actions JSONB`,並只在欄位空陣列時回填「雨夜的真相」ACT2 指定 JSON。
- `stages.ts` 補齊 ACT create/update 的 `shared_actions` 寫入;GET 既有 `SELECT *` 可回讀。
- AI 接入:候選生成、beam search/run turn 的 `RuleContext.actCards` 傳遞、kill 幕中線索可轉傷時的新調查估值、指揮層 kill AP 需求折抵。
- `TestScenarioScreen` 補玩家與 AI 的 `actCards` 接線,加入「揭穿傳說」按鈕、數量選擇與效果紀錄。
- `sim-slit-3ai.ts` 補 `actCards`、把 `use_shared_action` 視為 kill 對齊行動,並在 log 明確輸出 `揭穿傳說`。

## 動過的檔案

- `packages/shared/src/game/sharedActions.ts`
- `packages/shared/src/game/state.ts`
- `packages/shared/src/game/messages.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/ruleEngine.test.ts`
- `packages/shared/src/game/investigatorAI.ts`
- `packages/shared/src/game/investigatorAI.test.ts`
- `packages/shared/src/game/investigatorCommand.ts`
- `packages/shared/src/game/investigatorCommand.test.ts`
- `packages/shared/src/game/gameProgress.ts`
- `packages/shared/src/game/index.ts`
- `packages/shared/src/types/scenario.ts`
- `packages/server/src/db/migrate.ts`
- `packages/server/src/routes/stages.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `scripts/g1-sandbox/sim-slit-3ai.ts`

## 工單勾選

- A1:完成。保留 `turnNumber` 自動過期機制,無 reset helper。
- A2:完成。Migration 038 `ADD COLUMN IF NOT EXISTS`,ACT2 空欄位 guarded 回填指定 JSON。
- A3:完成。ACT create/update 可寫 `shared_actions`,GET 回讀。
- B1:完成。AI 生成 `use_shared_action`,amount 不過量,價值吃必中傷害與 objective bonus。
- B2:完成。指揮層 kill demand 折抵線索轉傷,detail 含折抵。
- B3:完成。`simulateStep` 與 `runInvestigatorAITurn` 的 RuleContext 都帶 `actCards`。
- B4:完成。玩家 intent、AI 回合與 sim 都帶 `actCards`。
- C1/C2/C3:完成。戰鬥板按鈕、數量選擇、演出/紀錄接上。
- D1/D2/D3/D4:完成。見測試。
- E1/E2/E3:完成。E2 使用暫存 E12 bootstrap cache 做本機 smoke,限制如下。

## 測試結果

`packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\ruleEngine.test.ts`

```text
87 passed, 0 failed
```

`packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\investigatorAI.test.ts`

```text
49 passed, 0 failed
```

`packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\investigatorCommand.test.ts`

```text
19 passed, 0 failed
```

`packages\client\node_modules\.bin\tsc.CMD -p packages\shared\tsconfig.json --pretty false`

```text
PASS (no output)
```

`packages\client\node_modules\.bin\tsc.CMD -b packages\client\tsconfig.json --pretty false`

```text
PASS (no output)
```

`packages\server\node_modules\.bin\tsc.CMD -p packages\server\tsconfig.json --pretty false`

```text
PASS (no output)
```

E2 smoke:

```text
packages\client\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --seed 42 --cache-dir .tmp_e12_bootstrap_cache --team d6f5f836-8c1c-45df-8928-ba88b144c783,4e4ecd62-82cf-4596-8f6a-8e642f8a532b,f86acf74-67eb-4b6c-b4a3-6c3003ccb255,df3448a4-994b-4a84-8206-7ee5ebc93518

T5: 📜 賞金獵人 揭穿傳說:棄1線索→1傷
T5: 賞金獵人:investigate→use_shared_action【揭穿傳說】→taunt
T6: 📜 警長 揭穿傳說:棄1線索→1傷
T6: 警長:investigate→investigate→use_shared_action【揭穿傳說】
T7: 📜 警長 揭穿傳說:棄1線索→1傷
T7: 警長:play_card【老兵彈藥箱】→investigate→use_shared_action【揭穿傳說】
結局:✓ 勝利
警長: ... use_shared_action×2 ...
賞金獵人: ... use_shared_action×1 ...
```

註:repo 內既有 cross-test bootstrap cache 是 2026-07-02 舊快照,沒有 `shared_actions`。E2 smoke 因此使用本輪建立的暫存 cache 注入 Migration 038 的 ACT2 JSON 後執行;該暫存目錄已刪除。這證明本機程式路徑會實際使用 `use_shared_action`,但不是 live DB migration/bootstrap 證據。

## 自知風險與範圍外

- 未啟動本機 server/DB,也未對 prod 執行 migration;部署後仍需確認 prod bootstrap ACT2 實際帶出 `shared_actions`。
- UI 未做瀏覽器人工點擊驗證;已由 tsc 與 engine/sim 管線覆蓋資料流。
- 本輪依 Uria 最新指示不送 Review;也未 push。
- 既有大量未追蹤 hub/cross-test 工具檔未納入本 commit。

## Review

本輪 Uria 指示「不用送Review」,此節保留空白給後續若需補 review 使用。
