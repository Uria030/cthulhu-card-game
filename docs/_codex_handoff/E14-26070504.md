# E14 playtest 修正批次 1 handoff

日期: 2026-07-05
工作包: `docs/E14工單_playtest修正批次1_26070504.md`
commit: 本地 commit 已建立；實際 hash 以 `git log -1` 為準。

## 變更摘要

- 修正草稿調查員顯示名:新增 `displayNameFor()`，大廳、座位、開局 intro 改為 `name_zh || title_zh || mbti_code`，並把 `title_zh` 存入選角 localStorage。
- 正式流程不再允許未選調查員 silent fallback: briefing / battle 缺選角時回大廳；server 無參數 fallback 保留給 dev/tooling。
- 下架三地點測試關卡:新增 `stages.is_hidden` migration、玩家關卡清單過濾 hidden、bootstrap hidden stage 回 `410 此關卡已下架`，前端 `/scenario/test` 與 briefing 顯示下架頁。
- AI 行動節奏:維持 signal -> fresh closure 計時模式，新增 AI log step queue，AI 每個 action 逐步進 log；玩家操作不被 queue 鎖住。
- 收合 log:新增 `latestActionRows()`，收合狀態改為每位調查員各顯示自己的最新 action。
- 手牌詳情:卡片 detail modal 改為結構化顯示費用、類型、陣營、描述、效果群組、投入圖示、uses/ammo、盟友 HP/SAN、武器風格/傷害與 flavor。
- 投入加值:移除手牌詳情與手牌面板的常駐投入按鈕；玩家宣告檢定時才開投入 modal，有候選卡才顯示，AI 路徑不變。

## 動過檔案

- `packages/client/src/api.ts`
- `packages/client/src/game/displayName.ts`
- `packages/client/src/game/displayName.test.ts`
- `packages/client/src/game/battleLogPreview.ts`
- `packages/client/src/game/battleLogPreview.test.ts`
- `packages/client/src/game/gameSetup.ts`
- `packages/client/src/game/selectedInvestigator.ts`
- `packages/client/src/screens/DepartureBoardScreen.tsx`
- `packages/client/src/screens/LobbyScreen.tsx`
- `packages/client/src/screens/ScenarioBriefingScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.css`
- `packages/server/src/db/migrate.ts`
- `packages/server/src/routes/play.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/types/scenario.ts`
- `docs/_codex_handoff/E14-retired-scenario.png`
- `docs/_codex_handoff/E14-retired-briefing.png`

## 驗證

```text
packages\client\node_modules\.bin\tsx.CMD packages\client\src\game\displayName.test.ts
4 passed, 0 failed

packages\client\node_modules\.bin\tsx.CMD packages\client\src\game\battleLogPreview.test.ts
1 passed, 0 failed

foreach shared src/game *.test.ts
總計 20 個測試檔全綠，含 ruleEngine 87 passed、investigatorAI 49 passed、encounters 13 passed、partyAutoCompose 5 passed。

packages\client\node_modules\.bin\tsc.CMD -p packages\shared\tsconfig.json --noEmit
PASS

packages\client\node_modules\.bin\tsc.CMD -p packages\client\tsconfig.json --noEmit
PASS

packages\server\node_modules\.bin\tsc.CMD -p packages\server\tsconfig.json --noEmit
PASS

git -c safe.directory=C:/Ug/cthulhu-card-game diff --check
PASS
```

## 瀏覽器驗證

- `/scenario/test`:顯示 `此關卡已下架;請從世界地圖選擇「雨夜的真相」。`
  - 截圖: `docs/_codex_handoff/E14-retired-scenario.png`
- `/scenario/test/briefing`:顯示三地點測試關卡已下架，進入按鈕 disabled 且文字為 `此關卡已下架`。
  - 截圖: `docs/_codex_handoff/E14-retired-briefing.png`
- 大廳選角:prod API 草稿 `ENFJ-1` 的 `name_zh=""`，picker 顯示 `社運領袖 草稿`，選取後座位顯示 `我 社運領袖`。
- 正式戰鬥板:用 `雨夜的真相` stage `9ad171b3-c439-4049-b673-b929f91366ce` 開局成功；收合 log DOM 顯示每位調查員各自最新行動，例如 `社區護士 整理隨身物資`、`夢遊詩人 打出「通靈密室」`、`無名鐵證偵探 整理隨身物資`。
- 手牌詳情 DOM 驗證:技能卡顯示 `投入: 魅力 +2` 與效果群組；事件卡顯示 `投入: 無投入圖示`，不再誤標 `武器 傷害 0`。
- 投入時機 DOM 驗證:以 `無名鐵證偵探` 宣告 `調查` 時出現 `投入加值 · 🔎 調查` modal，含 `檢定: 感知`、`DC: 13`、`基礎修正: +5`、`目前估算: 65%`，候選卡包含 `雙管獵槍 +2`、`.45 銀色子彈 +1`、`霰彈 +1`、`消音器 +1`、`探長觀察 +2`。
- 以 `社運領袖` 宣告 `調查` 時，手牌沒有感知候選，直接進入原三段行動演出，未出現投入 modal，符合 gate。

## 已知風險

- browser 截圖工具在戰鬥板 fixed overlay/modal 上多次只截到黑底；因此 modal 視覺圖未提交，只提交下架頁與 briefing 兩張可讀截圖，modal 改以 DOM 文本驗證。
- `execute_card_action` 的實際檢定屬性可能由風格卡抽牌後才決定；前端投入 modal 對這類行動採「顯示有任一投入圖示的候選」策略，實際加值仍由 shared rule engine 依抽到屬性裁決。
- `crossTest=true` 仍沿用既有 public route 行為，未在 E14 範圍內收斂。

## 範圍外

- 未改城主/關卡配置。
- 未改 AI 評分策略本體，只調整前端 log 呈現節奏。
- 未處理 M5 真人上線前的 crossTest route 收斂。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070512)

**結論:PASS。** `5abbbd5` 為本機唯一未推 commit(Gate 正確);review record 本機 commit,push 由守燈人本尊/Uria 依慣例執行。

#### 內容審查

1. **範圍對賬**:19 檔全在 E14 工單五主題內(顯示名/隱藏測試關/AI 動作節奏/Log 收攏/手牌介面+投入時機);`docs/v07*` 零觸碰;城主/關卡配置與 AI 評分本體未動,如 handoff 所述。✓
2. **計時器紅線核心審查**:AI step queue 的 setTimeout 閉包**只寫 log 展示層**(appendLines + functional setAiStepQueue),遊戲狀態 commit 仍全部在 aiTick effect 內以 fresh 閉包同步完成;stepAITeammate 改回傳 steps 維持純計算式;aiPaused 納入 queue.length>0 讓下一位 AI 等播報完——「不得在 setTimeout 閉包寫 state」紅線遵守,signal→fresh closure 模式未破壞。✓
3. **MIGRATION_039 冪等**:ADD COLUMN IF NOT EXISTS + 條件 UPDATE;下架用 is_hidden 軟隱藏不刪資料;bootstrap 對 hidden 回 410 語意正確;玩家清單過濾 COALESCE 防 NULL。MIGRATION_038 的縮排 whitespace 修改對已套用 DB 無影響(runOne 以 name 判斷跳過)。✓
4. **複跑測試**(26070512 本機實測):displayName 4 passed、battleLogPreview 1 passed、shared 回歸 ruleEngine 87/investigatorAI 49/encounters 13/partyAutoCompose 5 全綠;三包 tsc exit 0;sim 新種子 2026070510 完賽不崩。
5. **silent fallback 消除**:briefing/battle 缺選角回大廳,server 無參數 fallback 留給 dev/tooling——「消滅靜默鐵證」工單目標達成且留逃生路線。✓
6. **瀏覽器驗證**:handoff 附兩張下架頁截圖 + modal 以 DOM 文本驗證(截圖工具對 fixed overlay 只截到黑底,替代方案合理);投入 modal gate(無候選卡不跳)有正反兩例驗證。✓

#### 次要備註(不擋)

- AI step queue 播報中若玩家先結束回合,殘餘播報會落到下一階段的 Log——純展示層時序,無狀態影響;在意觀感時可於 endTurn 清空 queue 一行解。
- execute_card_action 投入候選採「任一投入圖示」寬鬆顯示、實際加值由 shared engine 依抽到屬性裁決——顯示與裁決分離,正確取捨,已在 handoff 記錄。
- crossTest route 收斂與 prod 部署後補跑 smoke,沿既有債務追蹤,不在本包。

**PASS(本機 commit review record;push 由守燈人本尊依慣例執行)。**
