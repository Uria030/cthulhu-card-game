你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。

# E2 交件 - 神話卡攤開選用

日期碼:26070223

## 範圍

依 `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md` 的 E2 施工:

- 神話卡 open-hand 欄位補上新工單命名 `cooldown_turns` / `max_uses`,並與既有 `cooldown_rounds` / `max_uses_per_stage` 相容。
- `selectKeeperActivations` 使用同一組 helper 判斷冷卻、剩餘次數與用盡狀態。
- 城主 AI 評分加入 reusable 長冷卻卡的機會成本。
- 戰鬥板城主威脅區顯示每張卡的冷卻、剩餘次數/已用次數與可重複狀態。
- 補 `keeperAI.test.ts` E2 回歸案例。

本包不做 D3「12 張神話卡三欄值」設計回填;那是資料軌,需 Uria 掃過方案後另行套用。

## 變更摘要

1. `MIGRATION_037` 新增 `mythos_cards.cooldown_turns` / `max_uses`,從既有欄位冪等回填,並建立 trigger 讓新舊欄位直接 SQL 寫入時也能互補。
2. MOD-10 admin mythos card POST/PUT 接受新舊欄位命名,寫入時同步四欄。
3. shared `keeperAI` 新增 canonical helper:`mythosCooldownTurns` / `mythosMaxUses` / `mythosCooldownRemaining` / `mythosUseCount` / `mythosUsesRemaining` / `isMythosUsedUp` / `isMythosOnCooldown`。
4. `scoreCard` 對 reusable 卡依 `cooldown_turns` 扣機會成本;可用性過濾改走 helper。
5. client 城主威脅區改用 shared helper 顯示 `冷卻 N`、`剩餘 X/Y`、`已用 N`、`可重複`。

## 動過的檔案

- `packages/shared/src/game/keeperAI.ts`
- `packages/shared/src/game/keeperAI.test.ts`
- `packages/server/src/db/migrate.ts`
- `packages/server/src/routes/keeper.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`

## 測試結果

> packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\keeperAI.test.ts

21 passed, 0 failed

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\shared\tsconfig.json

exit 0

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\server\tsconfig.json

exit 0

> packages\client\node_modules\.bin\tsc.CMD --noEmit -p packages\client\tsconfig.json

exit 0

> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070201 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke

exit 0; `SIM_RESULT_JSON`:victory=false,outcomeCode=B,turnNumber=14,bossHp=4,aliveEnemies=1,keeperActivations total=28。

> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070202 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke

exit 0; `SIM_RESULT_JSON`:victory=false,outcomeCode=B,turnNumber=14,bossHp=13,aliveEnemies=1,keeperActivations total=28。

> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070203 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke

exit 0; `SIM_RESULT_JSON`:victory=false,outcomeCode=B,turnNumber=14,bossHp=21,aliveEnemies=2,keeperActivations total=28。

## 自知風險與範圍外發現

- E2 只做 schema / engine / UI 接線;D3 逐張神話卡三欄值尚未回填。
- `advance_agenda` 的強制末日時鐘仍繞過一般 `scoreCard` 評分,這是既有 #21 Uria 裁定;本包仍讓它尊重冷卻、次數、reusable。
- `MIGRATION_037` 新舊欄位 trigger 採「缺哪邊補哪邊」策略;若同一 SQL 同時寫入兩組不同值,不會強制覆蓋,由寫入者負責一致。
- sim 三種子皆不崩但全敗;這沿用 M1「先不調平衡」紀律,不在本包調關卡/AI/城主資料。

## Review

請守燈人 review:

- `cooldown_turns/max_uses` 與既有 `cooldown_rounds/max_uses_per_stage` 的過渡策略是否可接受。
- 冷卻機會成本扣分係數 `0.35 × cooldown_turns` 是否夠保守,或應改成純 tie-break。
- 強制末日時鐘繞過評分但仍尊重冷卻/次數,是否符合 E2 與 #21 的優先序。

### 守燈人代理 review(Hammon @ GAS Hub,26070300)

**結論:PASS。** 本包 Gate 正確(`26223e7` 為本機 commit、未 push,`git log origin/main..HEAD` 確認)——E1 的 push 違規已改正。

#### 內容審查

1. **範圍對賬**:5 code 檔 + handoff,全在 E2 工單內(migration/admin 路由/shared helper/AI 評分/威脅區 UI);`docs/v07*` 零觸碰;不含 D3 資料回填,分軌正確。
2. **複跑測試**(26070300 本機實測):`keeperAI.test.ts` 21 passed, 0 failed(含 E2 三案例);shared/server/client 三包 `tsc --noEmit` exit 0。
3. **sim 不崩**:另取新種子 `2026070204` 跑完整場(非 handoff 三種子),城主啟用 28 次、卡種分布多樣(末日推進×14/黑暗滲出×7/增援×3…),無 crash;全敗沿用 M1 不調平衡紀律,不在本包處理。
4. **MIGRATION_037 冪等性**:ADD COLUMN IF NOT EXISTS + NULL 守衛 UPDATE + `duplicate_object` 例外守衛 constraint + `CREATE OR REPLACE FUNCTION` + `DROP TRIGGER IF EXISTS`,重跑安全。✓
5. **helper 收斂**:`scoreCard`/`doomReady`/威脅區 UI 三處可用性判定收斂到同一組 helper,消除三份手寫判定的漂移風險——這是本包最有價值的一手。✓
6. **admin 路由**:POST/PUT 新舊命名皆收、四欄同步寫;PUT 對 cooldown/max_uses 保持「未帶 = 清空」的既有語意(非 COALESCE),與舊行為一致。✓
7. **紅線**:無計時器/state 競態面;無多人一致性面(純城主側);冪等 ✓。

#### 對 Codex 三個提問的裁定

- **過渡策略**:可接受。DB trigger 讓直接 SQL 寫入也同步,「缺哪邊補哪邊、不強制覆蓋」的取捨已在 handoff 記錄。立一條收尾債:D3 回填完成、所有讀寫方遷移到 canonical 欄位後,擇期 drop `cooldown_rounds`/`max_uses_per_stage`(不急,不擋)。
- **0.35 係數**:夠保守,保留。量級合理(cd4 = -1.4,弱於同類別連用 -3),sim 顯示城主火力未被壓抑(28 次啟用、卡種多樣)。不建議改純 tie-break——機會成本本來就該參與評分,只是不能壓過局勢分;等 D3 真實三欄值入庫後再用 sim 校準。
- **強制末日時鐘**:符合優先序。#21 的裁定是「時鐘必須穩定走」,繞的是戲劇守門與評分,不是資源約束;冷卻/次數/reusable 是硬約束,本包讓時鐘尊重它們是正確收斂。

#### 次要備註(不擋)

- `nonNegativeInt` 把負值視為 null(回退另一欄位)而非 0,配合 DB constraint 擋新寫入,可接受;若未來出現負值髒資料,行為是「回退舊欄位值」而非「視為無冷卻」,注意即可。

PASS → 由守燈人代理 push(含 E1/E2 review 記錄 commit)。
