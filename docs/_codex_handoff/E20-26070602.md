# E20 交件 - 遭遇引擎落地與傳奇動作神話卡

日期碼: 26070602

## 範圍

依 `docs/E20工單_遭遇引擎落地_傳奇動作神話卡_26070602.md` 終裁施工:

- 雨夜的真相 `draw_on_turn_end` 改為「城主階段結束後、回合結束補給前,每位存活且站立調查員依席位各抽 1 張」。
- `encounter_type` 髒值收斂回原 6 種互動枚舉,分類語義交回 `threat_type_array`。
- 新增 draft encounter 神話卡機械種子,作為城主傳奇派發遭遇的第一張彈藥。
- 新增城主調查員階段傳奇遭遇選卡/扣能量/冷卻/目標選擇邏輯,神話階段不重複發動。
- sim 鏡射兩個入口 log:`[回合結束遭遇]` 與 `[城主傳奇派發]`。

## 主要變更

- `packages/shared/src/game/encounters.ts`
  - 保留 `draw_on_turn_end` 單一設定鍵,語意改為逐席結算。
  - 新增 `drawAndAutoResolveEncounter()` 供 AI/headless 共用抽牌 + 自動選項 + 結算。
- `packages/shared/src/game/keeperAI.ts`
  - 新增 `isLegendaryEncounterCard()`、`chooseLegendaryEncounterTarget()`、`selectKeeperLegendaryEncounter()`。
  - `investigator_phase_reaction` 卡不再進神話階段 `selectKeeperActivations()`。
- `packages/server/src/db/migrate.ts`
  - 新增 `MIGRATION_041`:
    - `g_slit_mouth_legend_st1.encounter_trigger_config.draw_on_turn_end = true`。
    - 收斂 `encounter_type` 髒值並重掛 6 值 CHECK。
    - upsert `e20_legendary_encounter_dispatch`,綁進雨夜 mythos pool。
- `packages/client/src/screens/TestScenarioScreen.tsx`
  - 回合結束按鈕先跑 E20 遭遇收尾;玩家遭遇開 modal 時暫停,AI 遭遇即時自動選項入 log。
  - 玩家動作完成後,若沒有既有遭遇 modal,城主可插入傳奇遭遇派發。
- `scripts/g1-sandbox/sim-slit-3ai.ts`
  - 接上 encounter deck/config。
  - 調查員階段插入傳奇派發,城主階段後/補給前逐席跑回合結束遭遇。
- `packages/shared/src/game/encounters.test.ts`
  - 新增逐席 turn_end 抽牌與空池不炸測試。
- `packages/shared/src/game/keeperAI.test.ts`
  - 新增傳奇遭遇神話階段排除、扣能量/冷卻/目標選擇測試。

## encounter_type 收斂對照

| 舊值 | 新值 | 理由 |
|---|---|---|
| `passive` | `thriller` | 被動壓力/氛圍收回驚悚互動型 |
| `conditional` | `puzzle` | 條件判斷最接近機關/判讀 |
| `choice_entry` | `choice` | 入口選擇歸回選項型 |
| `choice_fail` | `choice` | 失敗分歧歸回選項型 |
| `choice_responsibility` | `choice` | 責任選擇歸回選項型 |
| `test` | `puzzle` | 檢定挑戰歸回機關/判讀 |
| `chaos_bag` | `thriller` | 混沌袋壓力歸回驚悚/異常 |

`MIGRATION_041` 先 UPDATE 已知髒值,再檢查全表是否仍有非 6 值;若有未知值會中止,避免帶髒資料 VALIDATE CHECK。

## 驗證

```text
> packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\encounters.test.ts
14 passed, 0 failed

> packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\keeperAI.test.ts
23 passed, 0 failed

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\shared\tsconfig.json
exit 0

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\server\tsconfig.json
exit 0

> packages\client\node_modules\.bin\tsc.CMD --noEmit -p packages\client\tsconfig.json
exit 0

> git diff --check
exit 0
```

Live sim smoke(打 prod,本包 migration 尚未部署):

```text
> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --seed 26070602 --json
...
城主啟用:末日推進×14 黑暗滲出×10 深淵呼喚×1 深潛者增援×3(共 28 次)
遭遇入口:回合結束遭遇 0 次 | 城主傳奇派發 0 次 | 剩餘遭遇池 8 張
SIM_RESULT_JSON ... "investigatorTurnEndEncounters":0,"legendaryDispatches":0,"encounterDeckRemaining":8 ...
```

這次 live sim 只證明新 sim 程式在既有 prod 資料上不崩。`MIGRATION_041` 尚未部署,所以 prod 尚未開 `draw_on_turn_end`、也沒有新傳奇 encounter 神話卡綁池;部署後需補跑一次同 seed,預期 log 應出現 `[回合結束遭遇]` 與 `[城主傳奇派發]`。

## 自知風險

- `e20_legendary_encounter_dispatch` 目前是 mechanics-only draft seed。migration 在 insert 時用機械佔位名稱/說明,但 conflict 時保留既有 Gemini 內容;正式名稱與敘事仍應由 keeper-cards 線補齊後再進 review/approved。
- 前端傳奇派發目前接在玩家動作完成後的間隙;AI 調查員階段傳奇派發已由 sim 驗證,前端 AI 計時器尚未逐席插入傳奇窗,避免把 setTimeout 狀態競態重新打開。
- 玩家回合結束遭遇若開 modal,目前需要玩家關閉 modal 後再次按「結束敵人階段 → 下回合」才會繼續 AI 遭遇與補給;這是為了不疊 modal。後續可做成 modal 完成後自動續跑。
- D2 的正向事件 log 需等本 commit 部署後補跑;本機無 DB 可套 migration 做完整 live evidence。

## Review

Codex 送審狀態:

- 方式二本尊 session 呼叫失敗:`No conversation found with session ID: f075f2c2-afb2-4889-8e4a-15622c595c26`。
- 方式三代理 review 因本環境安全策略阻擋外部資料揭露,未執行。
- 請 Uria 在守燈人本尊視窗手動下:`review E20`。

依 E20 工單,請守燈人本尊 review:

- `draw_on_turn_end` 終裁語意是否正確收斂到「城主階段後、補給前逐席抽」。
- `encounter_type` 收斂表是否需 Uria 再調某些舊值對應。
- mechanics-only draft seed 是否接受;若不接受,需先跑 keeper-cards Gemini 單卡文字再部署。
