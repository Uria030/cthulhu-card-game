你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。

# E20c handoff — 遭遇牌堆抽乾重洗

## 變更摘要

- 依 `docs/E20工單_遭遇引擎落地_傳奇動作神話卡_26070602.md` 追加項 E20c 施工。
- Shared 遭遇引擎新增「目前牌堆抽乾時,用原始遭遇池重建後再抽」路徑;原始遭遇池為 0 張時仍維持 no-op。
- `encounterDeckRemaining` 維持「目前牌堆剩餘張數」語意;sim 另增 `encounterDeckReshuffles` 統計重洗次數。
- Client 戰鬥板與 headless sim 都改為傳入原始遭遇池,並在重洗時輸出敘事:「夜還長,厄運重新洗牌。」
- 補 shared 引擎測試與 sim 自測入口 `--self-test-encounter-reshuffle`,明確驗證 8 張池抽第 9 張會重洗且可抽出卡。

## 動過檔案

- `packages/shared/src/game/encounters.ts`
- `packages/shared/src/game/encounters.test.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `scripts/g1-sandbox/sim-slit-3ai.ts`

## 測試結果

### Shared encounter test

```text
> packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\encounters.test.ts

✓ 抽卡:從牌堆抽 1 張,不洗回(抽完即無)
✓ 抽卡:遭遇牌堆抽乾後用原始池重洗,第 9 張仍抽得出且可重現
✓ 抽卡:原始遭遇池為 0 張時維持 no-op
✓ 觸發:四條路徑都能抽遭遇
✓ 逐人回合結束:每位調查員各抽 1 張,池空不炸
✓ 觸發:未命中條件時不消耗牌堆
✓ 結算:檢定成功 → success_effects(得線索)
✓ 結算:檢定失敗 → failure_effects(受恐懼)
✓ 結算:無檢定選項 → no_check_effects(推毀滅)
✓ 結算:spawn_enemy 生成怪物
✓ AI 選項:高意志選檢定(賭得過),低意志選繞道避傷
✓ 法器即時型:過路費 f(S,N)=ceil(S/2)+N,破除遭遇但不吃通用解懲罰
✓ 法器檢定型:固定付 1 費用,成功時完全破除
✓ 法器檢定型:失敗時費用照扣,遭遇卡照常觸發
✓ 法器檢定型:缺 break_test_attribute 時不可出現在可用候選,且不扣費
✓ 法器儲蓄型:f(S,N)=S,用累積計量直接破除

16 passed, 0 failed
```

### Sim reshuffle self-test

```text
> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --self-test-encounter-reshuffle

SIM_SELF_TEST encounter-reshuffle PASS draws=sim-encounter-8,sim-encounter-1,sim-encounter-5,sim-encounter-3,sim-encounter-7,sim-encounter-2,sim-encounter-4,sim-encounter-6,sim-encounter-3 remaining=7 reshuffles=1
```

### TypeScript

```text
> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\shared\tsconfig.json
exit 0

> packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\server\tsconfig.json
exit 0

> packages\client\node_modules\.bin\tsc.CMD --noEmit -p packages\client\tsconfig.json
exit 0
```

### Headless sim

第一次在 sandbox 內跑 prod bootstrap 被網路權限擋下:

```text
> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --seed 26070603 --json

TypeError: fetch failed
cause: Error: connect EACCES 69.46.46.77:443
```

依權限流程升權後重跑通過:

```text
> packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --seed 26070603 --json

[遭遇牌堆] 夜還長,厄運重新洗牌。
...
遭遇入口:回合結束遭遇 37 次 | 城主傳奇派發 5 次 | 剩餘遭遇池 6 張 | 重洗 5 次
SIM_RESULT_JSON {"schema":"ug-cross-test-result-v1","stageId":"9ad171b3-c439-4049-b673-b929f91366ce","seed":26070603,"victory":true,"outcomeCode":"A","turnNumber":13,"investigatorTurnEndEncounters":37,"legendaryDispatches":5,"encounterDeckRemaining":6,"encounterDeckReshuffles":5}
```

### Diff check

```text
> git -c safe.directory=C:/Ug/cthulhu-card-game diff --check
exit 0
```

## 自知風險與範圍外

- Client UI 沒有新增專門的牌堆/棄牌堆視覺元件;本包只照 E20c 要求補引擎、log 與 sim 統計。
- 重洗來源目前是 bootstrap 的原始遭遇池快照;如果未來關卡中途動態增減遭遇池,需要另立「遭遇池變更事件」或 sourceDeck 更新契約。
- 未改玩家牌庫空抽規則;規則書的「玩家牌庫空不自動重洗」仍由 `upkeep.ts` 保持,本包只處理遭遇牌堆。
- 既有 untracked `.tmp_*`、`docs/_hub_*` 與 sandbox 腳本雜項未碰。

## Review

待守燈人 review。

Codex 送審狀態:已嘗試 README 方式二呼叫
`C:\Users\user\AppData\Roaming\npm\claude.cmd --resume f075f2c2-afb2-4889-8e4a-15622c595c26`,
但本環境安全策略拒絕對外部 Claude 服務揭露本機 repo/handoff 內容;Codex 不得改走 hub/代理方式繞過。
請 Uria 在守燈人本尊視窗手動下: `review E20c`。
