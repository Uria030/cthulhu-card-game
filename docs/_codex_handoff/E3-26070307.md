你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。重要:Uria 260703 指示 E3-E10 全部完成後一次 push;本次 review 請寫回本檔並可本機 commit review record,但不要 push。

# E3 handoff — 法器三時機 + 過路費(M2)

日期碼: 26070307  
Codex commit: 本檔所在本機 commit（review 前以 `git log -1` 查證）  
狀態: Codex 實作完成,待守燈人 review  
Push: 不 push。依 Uria 260703 指示,E3-E10 全部完成並通過 review 後一次推送。

## 依據

- `AGENTS.md` 工程協作 Gate
- `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md` E3
- `docs/v07_當前版本_26042606/s09_axis_dual_combat_part2.md` §5.1-5.5,§6.2

## 變更摘要

- 在 `encounters.ts` 補法器破除遭遇卡的 shared engine:
  - `encounterThreatTypes` / `encounterThreatStrength` / `encounterSubroutineCount`
  - `talismanTollCost`
  - `availableTalismansForEncounter`
  - `resolveEncounterWithTalisman`
- 三時機語意:
  - 即時型: `ceil(S / 2) + N`,充能足夠即直接破除。
  - 檢定型:固定費用 `1` + 指定屬性檢定 `DC = S`;成功破除,失敗費用照扣並套用 fallback 通用解後果。
  - 儲蓄型:`S`,用累積計量直接破除。
- `cardMaxUses` 接上法器 `break_charge_max`,讓打出法器資產時可以初始化 `assetState.usesLeft`。
- 玩家開局 bootstrap 會把 `encounter_subroutines` 一起帶到遭遇卡資料;舊資料沒有 subroutines 時才退回用 `option_count/options.length` 估 `N`。
- 戰鬥板遭遇 modal 的選項拍新增可用法器按鈕;法器結算後沿用 E1 的結算拍、倒地同步、進度檢查與傷害分配流程。

## 動過的檔案

- `packages/shared/src/game/encounters.ts`
- `packages/shared/src/game/encounters.test.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/server/src/routes/play.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`

## 測試結果

```text
packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\encounters.test.ts
12 passed, 0 failed
```

```text
packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\ruleEngine.test.ts
79 passed, 0 failed
```

```text
packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\shared\tsconfig.json
exit 0
```

```text
packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\server\tsconfig.json
exit 0
```

```text
packages\client\node_modules\.bin\tsc.CMD --noEmit -p packages\client\tsconfig.json
exit 0
```

## 自知風險 / 範圍外

- 這包只結算「遭遇發生時使用場上法器」;沒有做手牌即時打出、AI 自動選法器、或 stockpile 自動累積來源。累積來源仍要靠既有/後續卡面效果如 `gain_use`。
- 資料沒有 `target_threat_types` 的卡不視為萬用法器;通用法器應明確標三類 threat。
- 檢定型失敗時用 `fallbackOption` 的 failure/no-check effects 表示「遭遇照常觸發」。目前 UI 傳第一個通用選項作為 fallback;若後台日後有正式 default consequence 欄位,可改接該欄。
- 自然 1 額外代價/反噬沒有在 E3 實作;目前只依標準函式 `1 + DC=S`。
- 未做 browser smoke。此包已用 shared 測試覆蓋三時機,並用 client tsc 驗證 UI 接線。

## Review 指令

請依 `docs/_codex_handoff/README.md` 方式四送守燈人 review。Review 通過後請把結論寫回本檔並本機 commit review record,但不要 push;等 E3-E10 全部完成後由 Codex/守燈人依 push 前清單一次推送。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070308)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待 E3-E10 齊後批次推送。

#### 內容審查

1. **範圍對賬**:5 code 檔 + handoff,全在 E3 工單內(shared engine 三時機/過路費、`cardMaxUses` 接 `break_charge_max`、bootstrap 帶 subroutines、遭遇 Modal 法器按鈕);`docs/v07*` 零觸碰(只讀不改);`encounter_subroutines` 表與 `types/talisman.ts`(含 `TOLL_FUNCTIONS`)皆為既有落地,本包正確複用不自創。✓
2. **規則書條文對賬**(s09 Part2 §5.2-5.4):即時 `⌈S/2⌉+N`、檢定固定 1 費 + DC=S、儲蓄 `S`,三式與 `TOLL_FUNCTIONS` 及測試斷言一致;檢定失敗「費用照扣、遭遇照常觸發」符合 §5.3。✓
3. **複跑測試**(26070308 本機實測):`encounters.test.ts` 12 passed(含三時機四案例)、`ruleEngine.test.ts` 79 passed;shared/server/client 三包 `tsc --noEmit` exit 0。
4. **sim 不崩**:另取新種子 `2026070305` 全場跑完,城主啟用 28 次,無 crash。
5. **紅線**:法器結算只動玩家本人(無全隊面);沿用 E1 結算拍/倒地同步/damage_allocatable 管線;無計時器競態面;play.ts 只加一條平行查詢,冪等。✓
6. **失敗路徑守護**:`resolveEncounterWithTalisman` 在 test 時機缺屬性時回傳**原始** investigator(過路費不遺失),資源守恆正確。✓

#### 次要備註(不擋,建議摺進後續 E 包順手修)

- **邊角**:`evaluateTalismanForEncounter` 的充能分支先於 test 分支,test 時機法器若有充能池但**缺 `break_test_attribute`**,eval 會回 canUse=true(候選按鈕出現),resolve 才擋 unavailable → beat 3 顯示「法器不可用」後關 Modal,**遭遇無後果溜走**。僅在資料配置錯誤(test 法器沒填屬性)時發生;建議一行修正:eval 充能分支對 `timing==='test'` 也要求 testAttribute,或 unavailable 時退回 beat 2 讓玩家改選通用解。
- fallback 用第一個通用選項的取捨已在 handoff 記錄,待後台正式 default consequence 欄位時改接,同意。
- 自然 1 反噬未實作、手牌即時打出/AI 自動選法器不在本包——與工單一致,不算欠。

**PASS(不 push,batch push later)。**
