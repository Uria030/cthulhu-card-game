你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」與 `C:\Ug\cthulhu-card-game\AGENTS.md` 工程協作 Gate 執行;結論以 PASS/WARN/BLOCK 開頭回覆,但本輪 Uria 指示 E3-E10 全部做完後才一次 push,所以即使 PASS 也請不要 push。

# E4 handoff:XP 結算 + 長休息/整備模式

## 變更摘要

- 接通 `chapter_outcomes.rewards` 到 `ScenarioReward`:支援 `xp / talent_point / talentPoints / cohesion`,並把 outcome `flag_sets` 寫入 CampaignProgress。
- 隱藏調查加成只讀 `hidden_info.reward_params` 中明確的 XP 欄位,不因「有查到隱藏點」自創平衡數字。
- 新增整備期購卡純函式:計算 `starting_xp × Exceptional` 成本、擋書籍/遺跡升級版、特殊卡、獨特已擁有與 XP 不足,購買成功會扣 XP 並把 card definition id 加進跨章 deck。
- `buildGameFromBootstrap` 支援 `CampaignProgress`:有 carryover deck 時,下一場用跨章一般牌組重建,同時保留簽名卡/弱點。
- `/api/play/stages/:id/bootstrap` 增加唯讀 `upgrade_cards` 卡池,供整備畫面購買。
- TestScenarioScreen 結局畫面顯示戰役 XP/天賦點/凝聚力/下一章;進入整備畫面可購買升級卡,進度寫入 sessionStorage,下一次 bootstrap 會帶入 CampaignProgress。天賦樹入口保留 disabled,實際生效留 E5。

## 動過的檔案

- `packages/shared/src/game/campaignProgress.ts`
- `packages/shared/src/game/campaignProgress.test.ts`
- `packages/shared/src/game/bootstrap.ts`
- `packages/shared/src/game/bootstrap.test.ts`
- `packages/shared/src/game/gameProgress.ts`
- `packages/server/src/routes/play.ts`
- `packages/client/src/game/gameSetup.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.css`

## 測試結果

### campaignProgress.test

```text
✓ registerInvestigator:起始牌組組成是定義 id、滿血滿智
✓ registerInvestigator:已註冊者不覆寫(冪等,不洗掉累積進度)
✓ extractCarryover:創傷深拷貝,後續改 inv 不污染存檔
✓ extractCarryover:變動欄位取自 inv,牌組/xp/天賦點沿用 prev(不讀場景暫態 deck)
✓ 牌組組成跨「打一場 → 結算」不縮水(回歸 BLOCK:只保留抽牌堆會跨章失效)
✓ settleScenarioEnd:更新存活者狀態 + 發 XP/天賦點 + 寫旗標
✓ settleScenarioEnd:永久死亡 → 從存檔移除、不領獎、發 lost 效果
✓ settleScenarioEnd:沒上場的既有調查員原樣保留
✓ settleScenarioEnd:凝聚力獎勵套用且不為負
✓ applyLongRest:+1 凝聚力(ch4 §6.1)+ 進下一章 + 開整備訊號
✓ scenarioRewardFromOutcome:讀 chapter_outcomes.rewards + outcome flag_sets
✓ scenarioRewardFromOutcome:隱藏調查只吃明確 XP 欄位,可依調查員過濾
✓ preparationCardXpCost:starting_xp × Exceptional 倍率
✓ purchasePreparationCard:扣 XP 並加入跨章牌組組成
✓ canPurchasePreparationCard:獨特已擁有與書籍/遺跡升級版不可購買

15 passed, 0 failed
```

### bootstrap.test

```text
✓ 地點拓撲:連線雙向展開
✓ obstacle 連線標記目的地
✓ 夜間環境映射 night
✓ 混沌袋 token 展開
✓ 牌組展開 4 張(2+1+1),手牌上限分割
✓ cardIndex 含 card_definition/signature/weakness
✓ 線索 token 數量(單人)
✓ 調查員出生點 + 八屬性
✓ initial_enemies 形狀支援
✓ 無場景時丟錯
✓ CampaignProgress carryover deck 重建一般牌組,並保留簽名/弱點

11 passed, 0 failed
```

### tsc

```text
.\packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\shared\tsconfig.json
exit 0

.\packages\server\node_modules\.bin\tsc.CMD --noEmit -p packages\server\tsconfig.json
exit 0

.\packages\client\node_modules\.bin\tsc.CMD --noEmit -p packages\client\tsconfig.json
exit 0
```

## 自知風險與範圍外

- 整備卡池目前是 server 端保守唯讀篩選的 `starting_xp > 0` standard 卡;D5 最低升級卡池尚未回填前,實際可買內容取決於 DB 是否已有符合卡。
- 天賦樹入口只保留 disabled 位置;天賦點花費、天賦卡入牌組/被動掛角色、分支鎖規則留 E5。
- 玩家存檔仍是 E4 本機 sessionStorage,不是 server 權威存檔;E6 明確標示需等歸屬裁定後才接。
- 未做瀏覽器互動 smoke;本包已用 shared 純函式測試與 shared/server/client tsc 覆蓋核心風險。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070309)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待 E3-E10 齊後批次推送。

#### 內容審查

1. **範圍對賬**:`0b26c57` 9 code 檔 + handoff,全在 E4 工單內(XP 結算/整備模式/長休息/carryover 重建/唯讀升級卡池);`docs/v07*` 零觸碰;E5(天賦生效)/E6(server 權威存檔)正確留界,天賦樹入口 disabled、存檔走 sessionStorage 並在 handoff 明示。✓
2. **複跑測試**(26070309 本機實測):`campaignProgress.test.ts` 15 passed、`bootstrap.test.ts` 11 passed;shared/server/client 三包 `tsc --noEmit` exit 0。
3. **sim 不崩**:新種子 `2026070310` 全場跑完,城主啟用 28 次,無 crash。
4. **歷史紅線**:
   - 跨章牌組不縮水回歸測試在(extractCarryover 沿用 prev deck);registerInvestigator 冪等不覆寫;結算 useEffect 有 `campaignSettlement` 單次守衛,不重複發獎。✓
   - 隱藏調查加成只讀 `reward_params` 明確 XP 欄位、不自創平衡數字——符合資料驅動紀律。✓
   - 無計時器競態面;server 端只加唯讀查詢,冪等。✓
5. **腳本/流程**:settle → longRest → 整備購卡 → sessionStorage → 下次 bootstrap 帶 CampaignProgress,閉環成立;購卡守門(來源/特殊卡/獨特/XP 不足)有測試。✓

#### 次要備註(不擋,立債待 D5/E6 收)

- **carryover 卡面解析缺口**:`buildGameFromBootstrap` 重建 carryover deck 時,卡面靠 starting_deck + discoverable_cards + upgrade_cards 三池查 def id;`upgrade_cards` 查詢有 `LIMIT 80`(UI 再 slice(0,24))——若未來升級卡池超過 LIMIT 窗口或跨 stage 卡池不同,已購卡在該場會**靜默掉出可玩牌組**(存檔 deck 不受損,僅該場不可用)。D5 最低池(每陣營 3 張)內無實害;建議 E6/後續補「bootstrap 依 carry.deck ids 明確補撈卡面」。
- `chapter_outcomes.rewards`:OutcomeData 型別新增 `rewards?`,現有種子若無該欄回 0 獎勵,行為安全;D4/資料軌回填時注意 key 命名已支援 snake/camel 兩式。
- 整備 Modal `slice(0, 24)` 為靜默上限,池變大時記得加分頁或搜尋(UI 債)。

**PASS(不 push,batch push later)。**
