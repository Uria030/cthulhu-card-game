你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆;本批依 Uria 指示 E3-E10 全部完成後再一次 push,所以本次即使 PASS 也不要 push。

# E5 天賦樹接入(M3)

## 變更摘要

- 在 `CampaignProgress` 加入 `talents` carryover:已解鎖節點、陣營投資等級、已選分支、屬性加成、被動效果快照、天賦卡 id/code。
- 新增整備期天賦 helper:
  - `canUnlockTalentNode` / `unlockTalentNode`:扣天賦點、檢查前置/等級順序/分支鎖、套屬性 boost、記錄被動快照、`talent_card` 節點可把卡定義加入跨章牌組。
  - `canAcquireCardByTalent`:依 s11 天賦等級表限制玩家卡獲取,並疊加 `talent_branch_lock`。
  - `canPurchasePreparationCard` / `purchasePreparationCard` 預設啟用天賦 gate。
- `buildGameFromBootstrap` 會讀 carryover 天賦:
  - 屬性 bonus 套進下一場八屬性。
  - HP/SAN 上限用 boosted constitution / willpower 計算。
  - `talentNodeIds` / `talentBranches` / `talentEffects` 掛進 `InvestigatorState` 供 UI/後續引擎觀察。
  - `talent_cards` 補入卡面查找,讓天賦卡能被 carryover deck 實例化。
- public `/api/play/stages/:id/bootstrap` 加回傳:
  - `talent_tree`:所選調查員陣營的一棵樹,含 branches/nodes/effects。
  - `talent_cards`:該陣營 `talent_card_code` 或 `talent_branch_lock` 相關卡面,含 effects。
- 前端整備模式新增天賦樹面板:
  - 顯示天賦點/已解鎖數。
  - 可點節點,直接走 shared helper 寫回 `CampaignProgress`。
  - 購買升級卡時顯示天賦等級/分支鎖原因。

## 動過的檔案

- `packages/shared/src/game/state.ts`
- `packages/shared/src/game/campaignProgress.ts`
- `packages/shared/src/game/campaignProgress.test.ts`
- `packages/shared/src/game/bootstrap.ts`
- `packages/shared/src/game/bootstrap.test.ts`
- `packages/server/src/routes/play.ts`
- `packages/client/src/game/gameSetup.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.css`

## 測試結果

```powershell
packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\campaignProgress.test.ts
```

結果:

```text
19 passed, 0 failed
```

```powershell
packages\server\node_modules\.bin\tsx.CMD packages\shared\src\game\bootstrap.test.ts
```

結果:

```text
12 passed, 0 failed
```

```powershell
& .\packages\server\node_modules\.bin\tsc.CMD --noEmit -p .\packages\shared\tsconfig.json
```

結果:

```text
exit 0, no output
```

```powershell
& .\node_modules\.bin\tsc.CMD --noEmit
# cwd: packages/server
```

結果:

```text
exit 0, no output
```

```powershell
& .\node_modules\.bin\tsc.CMD --noEmit
# cwd: packages/client
```

結果:

```text
exit 0, no output
```

```powershell
packages\server\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070305 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke
```

結果摘要:

```text
結局:✗ 敗北/未分(B)
回合數:14
城主啟用:末日推進×14 黑暗滲出×8 深淵呼喚×1 深潛者增援×3 瘋狂攫住×1 恐懼侵襲×1(共 28 次)
SIM_RESULT_JSON ... "seed":2026070305 ... "victory":false,"outcomeCode":"B","turnNumber":14 ...
```

## 自知風險與範圍外

- 本包不解讀未成熟的天賦 `effect_code`;被動/里程碑/終極等先以 `talentEffects` 快照掛角色,供 UI/後續批次接入。
- `talent_card_code` 若 DB 找不到對應 `card_definitions.code`,本包只記錄 code,不虛構卡面、不加入 deck。
- `talent_branch_lock` 目前接入整備購卡;探索拾取、鍛造/製作路徑的同一鎖定仍屬後續取得管線範圍。
- public bootstrap 的 `talent_cards` 暫以本陣營 3 個分支鎖 + 節點 code 抓最多 80 張;D5 升級卡池/搜尋分頁可再收斂。
- sim 使用既有本機 bootstrap cache,驗證本次 shared/client 引擎路徑不崩;未打 prod,避免因 E5 server 尚未部署而混入部署順序誤判。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070310)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待 E3-E10 齊後批次推送。

#### 內容審查

1. **範圍對賬**:`ec19477` 9 code 檔 + handoff,全在 E5 工單內(talents carryover/解鎖 helper/天賦 gate/bootstrap 注入/唯讀 talent_tree API/整備天賦面板);`docs/v07*` 零觸碰;`talent_trees`/`talent_branches`/`talent_nodes`/`talent_node_effects`/`talent_branch_lock`(MIGRATION_031)皆既有落地,正確複用。✓
2. **規則書條文對賬**(s11 Part1 §6.2/§6.3):`startingXpLimitForTalentLevel` 與對照表逐級一致(0→不可獲取、1→0、2-3→1、4-5→2、6-7→3、8-9→4、10+→5);中立卡取最高已投資陣營等級(§6.3);分支鎖 `{FACTION}_{1-3}` 格式與 MIGRATION_031 一致;XP 公式 起始投入 × Exceptional 倍率沿 E4(§4.3)。✓
3. **複跑測試**(26070310 本機實測):`campaignProgress.test.ts` 19 passed(含解鎖扣點/分支鎖/talent_card 入牌組/等級+分支鎖疊加)、`bootstrap.test.ts` 12 passed(talents 注入下一場);shared/server/client 三包 `tsc --noEmit` exit 0。
4. **sim 不崩**:新種子 `2026070311` 全場跑完,城主啟用 28 次,無 crash。
5. **紅線與紀律**:
   - 不解讀未成熟 effect_code,只掛 `talentEffects` 快照——資料驅動、不自創語意。✓
   - `talent_card_code` 對不到卡面時只記 code 不虛構卡面,`talentCardCodes` 保留待補——不縮水原則延續。✓
   - 解鎖流程冪等守護(already_unlocked/效果快照 key 去重/talentCardIds 去重);HP/SAN 用 boosted 屬性走 ch6 §3.1 公式。✓
   - server 端只加唯讀查詢;無計時器競態面。✓

#### 次要備註(不擋)

- `talent_cards` 查詢同樣 `LIMIT 80`——與 E4 備註同源的靜默上限債,D5 池擴大時一併收(bootstrap 依 carry 明確 ids 補撈)。
- 天賦解鎖非 branch_choice 節點要求 `selectedBranch === branchIndex`,主幹(is_trunk)不受限——與 8 棵樹「主幹+三分支」資料形狀一致;若未來出現無分支樹,nodeBranchIndex 回 null 自然放行,行為安全。

**PASS(不 push,batch push later)。**

