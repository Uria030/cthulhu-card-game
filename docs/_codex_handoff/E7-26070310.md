你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆;本批依 Uria 指示 E3-E10 全部完成後再一次 push,所以本次即使 PASS 也不要 push。

# E7 章節→戰役串連(M3)

## 變更摘要

- `CampaignProgress` 新增 `chapterResults`,記錄每章 outcome、`next_chapter_version`、完成 stage 與結算時間。
- `scenarioRewardFromOutcome` 現在會從 `chapter_outcomes` 帶出 `outcomeCode` / `nextChapterVersion`;`settleScenarioEnd` 會把它們寫入戰役進度並發出 `chapter_result_recorded` effect。
- 新增 `resolveCampaignStageAccess`:
  - 無進度時只開各 campaign 第一章。
  - 已完成章標 `completed`,未來章標 `locked`。
  - 若上一章 `next_chapter_version` 能匹配當章 stage code/name/id,只開匹配分歧 stage,其他同章 stage 標 `branch_not_selected`。
  - 若 DB 尚未有可匹配分歧 stage,回落到當章第一個 stage,不阻斷連打流程。
- 抽出 client 本地 `CampaignProgress` sessionStorage helper,讓戰鬥板、世界地圖、劇情提要頁共用同一份進度。
- 世界地圖會讀目前選取調查員的進度,標示「完成 / 下一章 / 分歧 / 未解鎖」並禁止點擊 locked stage。
- 劇情提要頁會顯示上一章結局與分歧版本,並擋住直接 URL 跳章。
- 結算/整備流程新增分歧版本顯示與「前往世界地圖」按鈕,支援第一章結算後接第二章。
- `/api/play/stages` 額外回傳 `chapter_code`,供前端 stage list 保留章節代碼。

## 動過的檔案

- `packages/shared/src/game/campaignProgress.ts`
- `packages/shared/src/game/campaignProgress.test.ts`
- `packages/shared/src/game/gameProgress.ts`
- `packages/client/src/game/campaignProgressStorage.ts`
- `packages/client/src/api.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/DepartureBoardScreen.tsx`
- `packages/client/src/screens/DepartureBoardScreen.css`
- `packages/client/src/screens/ScenarioBriefingScreen.tsx`
- `packages/client/src/screens/ScenarioBriefingScreen.css`
- `packages/server/src/routes/play.ts`

## 測試結果

```powershell
packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\campaignProgress.test.ts
```

結果:

```text
21 passed, 0 failed
```

```powershell
packages\client\node_modules\.bin\tsc.CMD -p packages\shared\tsconfig.json --noEmit
```

結果:

```text
exit 0, no output
```

```powershell
packages\client\node_modules\.bin\tsc.CMD -p packages\client\tsconfig.json --noEmit
```

結果:

```text
exit 0, no output
```

```powershell
packages\server\node_modules\.bin\tsc.CMD -p packages\server\tsconfig.json --noEmit
```

結果:

```text
exit 0, no output
```

```powershell
packages\client\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070307 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke
```

結果摘要:

```text
結局:✓ 勝利(A)
回合數:13 | 線索:0 | 毀滅:4 | 場上存活怪:0
城主啟用:末日推進×12 黑暗滲出×5 深潛者增援×3 瘋狂攫住×2 恐懼侵襲×2(共 24 次)
SIM_RESULT_JSON ... "seed":2026070307 ... "victory":true,"outcomeCode":"A","turnNumber":13 ...
```

```powershell
git -c safe.directory=C:/Ug/cthulhu-card-game diff --check
```

結果:

```text
exit 0;僅 Git 回報 LF/CRLF 換行警告,無 whitespace error。
```

## 自知風險與範圍外

- E6「玩家存檔層」仍因存檔歸屬裁定未下而不施工;本包只沿用既有 sessionStorage `CampaignProgress`,不新增後端存檔權威。
- `chapter_outcomes.next_chapter_version` 目前是版本字串,不是 stage 外鍵;本包用 stage code/name/id 做匹配。若 DB 還沒有分歧 stage,會 fallback 到當章第一個 stage 以保連打流程。
- 劇情提要頁只擋跳章;同章但非分歧 direct URL 的完整封鎖需要完整 stage list,目前由世界地圖負責。
- `/api/play/stages` 只新增唯讀欄位 `chapter_code`;未更動 stage/chapter DB schema。
- 未啟動瀏覽器視覺檢查;本包 UI 改動已由 client strict tsc 覆蓋,流程 smoke 由 headless sim 覆蓋不崩。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070311)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待 E3-E10 齊後批次推送。

#### 內容審查

1. **範圍對賬**:`ccc7ecf` 11 code 檔 + handoff,全在 E7 工單內(chapterResults 記錄/分歧解析/世界地圖標示/提要頁擋跳章/共用 sessionStorage helper);`docs/v07*` 零觸碰;E6 存檔歸屬正確持續不施工,只沿用 sessionStorage。✓
2. **複跑測試**(26070311 本機實測):`campaignProgress.test.ts` 21 passed(含 chapterResults 寫入/分歧解析案例);shared/server/client 三包 `tsc --noEmit` exit 0。
3. **sim 不崩**:新種子 `2026070312` 全場跑完,城主啟用 28 次,無 crash。
4. **紅線與紀律**:
   - `/api/play/stages` 只加唯讀欄位 `chapter_code`,無 schema 變動。✓
   - storage helper 抽出後三畫面共用同一份進度,消除 TestScenarioScreen 原地重複(E4 的內嵌版本已移除)。✓
   - 分歧無可匹配 stage 時 fallback 當章第一個 stage——fail-open 保連打流程,符合「守門留逃生路線」。✓
   - `settleScenarioEnd` 只在有 outcomeCode 時寫 chapterResults,冪等安全;`resolvedAt` 可注入供測試。✓

#### 次要備註(不擋,D6 第二戰役落地前收)

- **分歧匹配過鬆**:`stageMatchesBranch` 用子字串 includes 對 code/name/**id(UUID)** 匹配——若 `next_chapter_version` 是短字串(如「A」「B」),UUID hex 幾乎必含該字母,會把全章 stage 都判為 branchMatched,分歧鎖實質失效(fail-open,不破壞流程但守門形同虛設)。D6 第二戰役 + 真分歧資料落地前,建議收斂為:只對 `stage.code` 做完全相等或 `_{version}` 字尾匹配,並要求 version 長度 ≥ 2;或等 DB 補 next_stage 外鍵後改 id 精確匹配。
- 提要頁只擋跳章、同章 direct URL 由世界地圖負責——已在 handoff 明示,接受。

**PASS(不 push,batch push later)。**

