你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」與 `AGENTS.md` 工程協作模式執行,結論以 PASS/WARN/BLOCK 開頭回覆;本批依 Uria 指示 E3-E10 全部完成後再一次 push,所以本次即使 PASS 也不要 push。

# E8 團隊精神接入(M3) handoff

## 工作包

- 依據: `repo(c5c2691):docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md`
- 項目: `E8 | 團隊精神接入(M3)`
- 要求: MOD-04 已有在庫團隊精神,先讀團隊精神設計文件後,接入組隊/整備期選擇與生效管線。
- 本次不 push: Uria 已指示 E3-E10 全部完成後一次推送。

## 讀過的必要文件

- `AGENTS.md`
- `docs/_codex_handoff/README.md`
- `docs/v07_當前版本_26042606/h10_supp09_spirit.md`

## 變更摘要

1. 在 `CampaignProgress` 加入隊伍共用 `teamSpirits` 進度,保存已採用團隊精神、投資深度、里程碑狀態與效果快照。
2. 新增 `adoptTeamSpirit` / `investTeamSpirit` / gate helper:
   - 採用新團隊精神花費 1 凝聚力。
   - 最多採用 7 種。
   - 已採用精神可逐點投入,每點花費 1 凝聚力,深度上限 5。
   - 深度 5 標記里程碑解鎖。
3. Bootstrap 階段把戰役進度內的團隊精神注入 `CampaignState`:
   - `unlockedTeamSpirits`
   - `teamSpiritInvestments`
   - `teamSpiritEffects`
4. Public play bootstrap 唯讀帶出 `spirit_definitions` + `spirit_depth_effects`,供整備 UI 採用/投入。
5. Client 整備 modal 新增「團隊精神」面板,可檢視 33 個候選、花費凝聚力採用、投入已採用精神。
6. 補 shared 測試鎖住採用、投入、上限、里程碑、場景結算保留與下一場 bootstrap 注入。

## 範圍判斷

- 文件指出團隊精神的「投資時點」是長休息/整備期,所以本次接在現有整備 modal,不是 lobby 隊長制。
- E6 玩家存檔歸屬仍待裁定,本次不建立 server-side team record / captain ownership。
- MOD-04 具體每項效果尚未全部裁成引擎 effect_code,本次只做資料驅動 snapshot,不硬寫 `ts_*` 專屬規則。
- Bootstrap 目前每次帶全量團隊精神候選,現有在庫約 33 筆,可接受;未做分頁或快取。

## 動過的檔案

- `packages/shared/src/game/campaignProgress.ts`
- `packages/shared/src/game/state.ts`
- `packages/shared/src/game/bootstrap.ts`
- `packages/shared/src/game/campaignProgress.test.ts`
- `packages/shared/src/game/bootstrap.test.ts`
- `packages/server/src/routes/play.ts`
- `packages/client/src/game/gameSetup.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.css`
- `docs/_codex_handoff/E8-26070311.md`

## 測試結果

### campaignProgress.test

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\campaignProgress.test.ts
```

Result:

```text
26 passed, 0 failed
```

新增覆蓋:

```text
adoptTeamSpirit:採用新團隊精神會扣凝聚力並寫效果快照
investTeamSpirit:投入已採用精神會扣凝聚力、升深度、刷新深度效果
investTeamSpirit:深度 5 解鎖里程碑,且已滿不可再投入
canAdoptTeamSpirit:最多採用 7 種,凝聚力不足會擋
settleScenarioEnd:場景結算會保留團隊精神進度
```

### bootstrap.test

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\bootstrap.test.ts
```

Result:

```text
13 passed, 0 failed
```

新增覆蓋:

```text
CampaignProgress teamSpirits 注入下一場:已採用精神、深度、效果快照
```

### TypeScript

Commands:

```powershell
packages\client\node_modules\.bin\tsc.CMD -p packages\shared\tsconfig.json --noEmit
packages\client\node_modules\.bin\tsc.CMD -p packages\client\tsconfig.json --noEmit
packages\server\node_modules\.bin\tsc.CMD -p packages\server\tsconfig.json --noEmit
```

Result:

```text
all exit 0
```

### G1 smoke

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --team elias_crane,vesper_grey,ada_wexler,marcus_wainwright --seed 2026070313 --json --cache-dir scripts\g1-sandbox\cross-test\.cache\smoke
```

Result:

```text
exit 0
SIM_RESULT_JSON seed=2026070313 victory=false outcomeCode=B turnNumber=14 keeperActivations=28
```

註: `victory=false/outcomeCode=B` 是該 seed 的關卡結果,不是 smoke failure;本次檢查重點是引擎不崩、城主行動正常、E8 不干擾 runtime。

### Diff check

Command:

```powershell
git -c safe.directory=C:/Ug/cthulhu-card-game diff --check
```

Result:

```text
exit 0
```

僅有 Windows checkout 的 LF/CRLF warning,無 whitespace error。

## 自知風險 / 請 reviewer 特別看

1. `spirit_definitions` 由 public `/api/play/stages/:id/bootstrap` 帶出,屬讀取 33 筆草稿設計資料;若守燈人認為 public bootstrap 不該帶草稿欄位,可要求收斂欄位白名單。
2. `teamSpiritEffects` 目前是 snapshot 而非 executable rules;後續若要實際改變檢定/戰鬥,需另開 effect interpreter,本次沒有偷做。
3. UI 面板沿用既有 talent panel CSS,不是完整 MOD-04 專用設計;符合 E8 接線目標,但不是最終 UX。
4. 因 E6 存檔歸屬未裁定,團隊精神進度仍跟 E7 的本地 `CampaignProgress` 一起走 sessionStorage;這是刻意留邊界。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070312)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待 E3-E10 齊後批次推送。

#### 內容審查

1. **範圍對賬**:`d5f179b` 9 code 檔 + handoff,全在 E8 工單內(teamSpirits 進度/採用+投入 helper/bootstrap 注入 CampaignState/唯讀候選池 API/整備面板);`docs/v07*` 零觸碰;`spirit_definitions`/`spirit_depth_effects` 既有表,正確複用;E6 邊界持續尊重(sessionStorage,無 server-side team record)。✓
2. **規則書條文對賬**(h10_supp09 §3 數值表):最多採用 7 種、每種深度 5 點、每點 1 凝聚力、採用 1 凝聚力、點滿 5 解鎖里程碑——五項與程式常數及測試斷言逐一一致;投資時點接在整備期符合 §3「長休息時進行」。✓
3. **複跑測試**(26070312 本機實測):`campaignProgress.test.ts` 26 passed(採用/投入/上限/里程碑/結算保留)、`bootstrap.test.ts` 13 passed(teamSpirits 注入下一場);shared/server/client 三包 `tsc --noEmit` exit 0。
4. **sim 不崩**:新種子 `2026070313` 全場跑完,城主啟用 28 次,無 crash。
5. **紀律**:效果只做 snapshot 不硬解 `ts_*` 規則——資料驅動、不偷做 interpreter;`cloneTeamSpiritProgress` 防禦性 normalise(points clamp 5、milestone 推導);`replaceTeamSpiritSnapshots` 以 code 全量重建快照,冪等。✓

#### 對 handoff 提問的裁定

- **public bootstrap 帶 33 筆草稿 spirit 資料**:可接受——純唯讀遊戲設計資料、無憑證或個資;`SELECT *` 會帶 design_notes 等後台欄位,立一條輕債:後續改欄位白名單(與 upgrade_cards/talent_cards 一致的收斂時機,不擋本包)。
- bootstrap 同時把 `flags`/`cohesion` 從 CampaignProgress 注入 CampaignState(原為空值)——屬生效管線必要部分,範圍內。

#### 次要備註(不擋)

- CampaignState 內的 cohesion 是場內唯讀快照,場內若未來出現「消耗凝聚力」效果,需回寫 CampaignProgress 的管線(E6 存檔裁定後一併設計)。

**PASS(不 push,batch push later)。**

