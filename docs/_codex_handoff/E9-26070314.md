你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」與 `AGENTS.md` 工程協作模式執行,結論以 PASS/WARN/BLOCK 開頭回覆;本批依 Uria 指示 E3-E10 全部完成後再一次 push,所以本次即使 PASS 也不要 push。

# E9 deferred 效果碼掃尾 handoff

## 工作包

- 依據: `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md`
- 項目: `E9 | deferred 效果碼掃尾(M2)`
- 範圍:反應時機窗、混沌袋操縱、tile 殘項、`advance_act`;動工前對照 `effectsExecutor.ts` unsupported 與 cross-test `effect_unsupported`。
- 本次不 push: Uria 已指示 E3-E10 全部完成後一次推送。

## 讀過的必要文件

- `AGENTS.md`
- `docs/_codex_handoff/README.md`
- `docs/工作分軌_資料庫填補×本體工作包_v0_1_26070209.md`
- `docs/調查員交叉測試報告_384場_26070210.md`
- `docs/規劃_卡片效果執行器補完_26061701.md`
- `docs/_hub_brief_p123_card_effects.md`
- `docs/v07_當前版本_26042606/03_rulebook_ch3.md`

## 變更摘要

1. `effectsExecutor` 新增混沌袋動態 token 操作:
   - `add_bless` / `add_curse`
   - `remove_bless` / `remove_curse`
   - `look_chaos_bag`
   - `manipulate_chaos_bag` 的 `remove` / `replace` / `reorder`
2. `retrieve_card` 空棄牌堆不再誤報 `effect_unsupported`;改回 `retrieve_card_empty` 效果。
3. 反應/檢定碼 `reroll` / `auto_success` / `auto_fail` / `make_test` 在 action 路徑改成明確 deferred effect,不洗 unsupported log。
4. commit 卡的檢定結果觸發從只支援 `on_success` 擴成 `on_success` / `on_fail`:
   - 調查失敗、隱藏調查失敗、搜尋失敗、徒手攻擊失敗、武器攻擊失敗都會跑 `on_fail`。
5. tile 殘項映射到現行固定地點模型:
   - `reveal_tile` / `place_tile`:既有地點則解鎖。
   - `remove_tile`:既有地點則從 `unlockedLocations` 拿掉。
   - 無地點定義時產生 `tile_effect_deferred`,不報 unsupported。
6. `advance_act` / `advance_agenda` 補入 executor:
   - `advance_act`:推進 `scenario.actIndex`。
   - `advance_agenda`:有 `doom_tokens` 時走毀滅進度;否則推進 `agendaIndex` 並預設清 doom。

## 動過的檔案

- `packages/shared/src/game/effectsExecutor.ts`
- `packages/shared/src/game/effectsExecutor.test.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/ruleEngine.test.ts`
- `docs/_codex_handoff/E9-26070314.md`

## 測試結果

### effectsExecutor.test

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\effectsExecutor.test.ts
```

Result:

```text
45 passed, 0 failed
```

### ruleEngine.test

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD packages\shared\src\game\ruleEngine.test.ts
```

Result:

```text
84 passed, 0 failed
```

### TypeScript

Commands:

```powershell
packages\client\node_modules\.bin\tsc.CMD -p packages\shared\tsconfig.json --noEmit
packages\client\node_modules\.bin\tsc.CMD -p packages\client\tsconfig.json --noEmit
packages\server\node_modules\.bin\tsc.CMD -p packages\server\tsconfig.json --noEmit
```

Result:

```text
all exit 0
```

### G1 smoke

Command:

```powershell
packages\client\node_modules\.bin\tsx.CMD scripts\g1-sandbox\sim-slit-3ai.ts --seed 2026070316
```

Result:

```text
exit 0
victory=false outcomeCode=B turnNumber=14 keeperActivations=28
no effect_unsupported observed in console output
```

註:第一次在 sandbox 內跑 smoke 被外部 fetch 擋下(`connect EACCES 69.46.46.77:443`),升權後同 seed 跑完。`victory=false/outcomeCode=B` 是該 seed 關卡結果,不是 smoke failure。

### Diff check

Command:

```powershell
git -c safe.directory=C:/Ug/cthulhu-card-game diff --check
```

Result:

```text
exit 0
```

僅有 Windows checkout 的 LF/CRLF warning,無 whitespace error。

## 自知風險 / 請 reviewer 特別看

1. 真正「敵方行動時從手牌即時打 reaction 卡」仍需要容器意圖協議與 UI 時機窗;本包沒有硬造半套介面。這次完成的是 executor 對反應效果碼不再誤報 unsupported,以及 commit 卡 `on_fail` 的可驗證路徑。
2. tile runtime 目前仍是固定地點模型,沒有 G4 隨機地城 tile instance。已用 unlock/lock/deferred effect 取代 unsupported,但不是完整隨機地城生成器。
3. `advance_act` / `advance_agenda` 只改 index/doom state,不直接跑 ACT/Agenda 背面劇情操作;完整敘事翻面仍由 `gameProgress` 管。
4. `manipulate_chaos_bag action=replace` 需要 `replacement_type` / `to` / `with`;缺 replacement 時會產生 noop effect,不報 unsupported。

## Review

### 守燈人代理 review(Hammon @ GAS Hub,26070314)

**結論:PASS。** 依 Uria 260703 指示不 push,review record 本機 commit,待批次推送。

#### 內容審查

1. **範圍對賬**:`6ebf2c3` 4 code 檔 + handoff,全在 E9 工單內(反應碼/混沌袋操縱/tile 殘項/advance_act);`docs/v07*` 零觸碰;動工前有對照 unsupported 清單與 384 場 cross-test 報告,依據正確。✓
2. **複跑測試**(26070314 本機實測):`effectsExecutor.test.ts` 45 passed、`ruleEngine.test.ts` 84 passed(Raviel BLOCK 三回歸仍綠);shared/server/client 三包 tsc exit 0。
3. **sim 不崩**:新種子 `2026070317` 全場跑完,城主啟用 28 次,無 crash。
4. **設計取捨核可**:
   - `applyOnSuccessCommit` → `applyCheckOutcomeCommit` 對稱擴充 on_fail,五條失敗路徑(調查/隱藏調查/搜尋/徒手/武器)全接;失敗路徑同時保留鬧鬼復活順序(after → haunt)。✓
   - unlockLocation 對「unlockedLocations=[] = 全開」語意有防呆(不塞單點造成反向上鎖),lockLocation 先展開全清單再過濾——語意正確。✓
   - tile/reaction 未知情境走 deferred effect 不報 unsupported、不靜默——「unsupported 清單歸零或立債」的驗收方式正確。✓
   - advance_agenda 有 doom_tokens 走毀滅進度、否則翻面預設清 doom(preserve_doom 可保留)——與 AH 慣例一致;敘事翻面仍由 gameProgress 管,已在 handoff 明示。✓
5. **紀律**:manipulate_chaos_bag 缺參數回 noop effect 帶 reason,不虛構行為;commit 卡 on_fail 的 unsupported 會轉為 effect_unsupported 效果,不吞錯。✓

#### 次要備註(不擋)

- `advance_act`/`advance_agenda` 的 index 無上限 clamp(超出 actData 長度由 gameProgress/applyProgress 消化)——資料軌填卡時避免多重推進疊加;若 D2/D3 出現重複推進卡,屆時加 clamp。
- `look_chaos_bag`/`reorder` 以陣列頭部為「頂部」——袋子抽取若是均勻隨機,順序語意目前僅供 UI 展示;真 reaction 時機窗(E9 自知風險 1)留待容器意圖協議,同意不硬造半套。

**PASS(不 push,batch push later)。**
