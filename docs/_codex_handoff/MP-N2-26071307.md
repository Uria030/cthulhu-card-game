請守燈人依 `docs/_codex_handoff/README.md` 的代理 review checklist 審查本交件，結論請以 PASS/WARN/BLOCK 開頭，並寫回本檔 `## Review`。

# MP-N2 交件：多人 v1 兩人驗證

署名：Nhalor @ Codex Desktop / UG

## 範圍與現況對賬

本包依 `docs/工作單_路線圖六步_26071301.md` MP-N2 與 `M2M3規劃_帳號存檔與成長閉環_v0_1_26070505.md` Part 5 施工。MP-N1 已有記憶體房間、JWT、WebSocket、意圖序號與 server `resolveIntent`；施工前缺少選人/ready、真人與 AI 席位、宣告制、斷線控制權轉換、大廳入口及多人戰鬥容器。

本包沒有修改 `TestScenarioScreen` 的單機狀態路徑。多人以 `/multiplayer/*` 新容器隔離。

## 變更摘要

1. shared 協議擴充：房間席位含 server 裁決的 template/ready；遊戲快照含真人控制映射、AI/human 控制權、宣告結束名單及每玩家 next sequence。新增 `declare_end`、AI 回合與階段訊息。
2. server 房間服務新增：64 選 1 互斥鎖、ready/start gate（v1 至少兩真人）、連線計數、斷線 AI 控制與重連交還、序號化宣告制、AI 回合同一 shared `runInvestigatorAITurn` 管線。
3. server 開局工廠：房主開始時以 server 內部 `app.inject` 讀既有 `/api/play/.../bootstrap`，組成真人 + E13 roster AI 補到四席的唯一 `AuthoritativeGameState`；卡片、敵人、風格、城主/怪物/進度資料只留 server runtime。
4. 全員宣告後，server 依序送神話階段訊息、城主選卡與結算、怪物啟動、恐懼、進度 tick、回合補給/開始，最後回到下一個調查員階段。測試涵蓋最小可重現的完整輪次。
5. WebSocket 正常關閉碼 `1000`（房間頁切換到戰鬥頁）不誤觸 AI；非正常中斷才會把該真人席交給 AI 執行回合。
6. client：大廳新增「邀請電報」入口；新房間頁含開/入房、房間碼、席位、64 選 1、ready、關卡選擇與房主開始；新多人戰鬥容器依 server 快照渲染地點、隊伍、資源、可用基礎動作、宣告結束與權威事件紀錄。

## 動過檔案

- `packages/shared/src/multiplayer.ts`
- `packages/server/src/services/multiplayer-rooms.ts`
- `packages/server/src/services/multiplayer-game-factory.ts`
- `packages/server/src/routes/multiplayer.ts`
- `packages/server/src/services/multiplayer-rooms.test.ts`
- `packages/server/src/services/multiplayer-game-factory.test.ts`
- `packages/server/src/routes/multiplayer.test.ts`
- `packages/client/src/api.ts`
- `packages/client/src/game/multiplayerTransport.ts`
- `packages/client/src/screens/LobbyScreen.tsx`
- `packages/client/src/screens/MultiplayerRoomScreen.tsx`
- `packages/client/src/screens/MultiplayerRoomScreen.css`
- `packages/client/src/screens/MultiplayerScenarioScreen.tsx`
- `packages/client/src/screens/MultiplayerScenarioScreen.css`
- `packages/client/src/App.tsx`

## 驗證結果

```text
shared test files: 20 passed
server test files: 4 passed
client test files: 10 passed

MP-N2 multiplayer service tests: 6 passed
  - lifecycle/max 4
  - identical two-client authoritative intent/replay/out-of-order behavior
  - updatedAllies enters the shared snapshot
  - selection exclusion + ready/start gate
  - disconnect AI takeover + reconnect handback
  - explicit declaration only advances after all live investigators declare

MP-N2 route tests: 3 passed
  - player JWT REST lifecycle
  - real localhost WebSocket authenticate -> snapshot
  - select collision -> 409; two ready players -> server bootstrap -> 4 seats / 2 AI

MP-N2 game factory: 2 passed
  - trusted bootstrap builds two human + two E13 AI seats
  - AI turns + human declarations resolve mythos and start next shared turn

packages/shared tsc: PASS
packages/server tsc --noEmit: PASS
packages/client tsc --noEmit: PASS
packages/client Vite production build: PASS
node scripts/preflight.js: ALL PASS
git diff --check: PASS
```

## 紅線檢查

- **多人一致性**：真人與 AI 都由 server `resolveIntent`/`runInvestigatorAITurn` 改唯一 state；`updatedAllies` 留在同一份 `investigators` snapshot，client 只顯示 server event。
- **計時器競態**：本包無 client timer 寫遊戲 state；斷線由 WebSocket close event 驅動，正常頁面交接與異常中斷分流。
- **冪等**：普通 intent 與 `declare_end` 共用每玩家連續 sequence/快取；快照提供 next sequence 供重連後續傳。
- **單機雙軌**：未改 `TestScenarioScreen`、`LobbyScreen` 的單機出發入口或既有單機 engine state。

## 自知風險與待裁決邊界

1. **MP-N2 驗收第 4 項未完成**：server 已經在 victory/defeat 推進中握有權威 state，但尚未把 chapter outcome 寫入每位真人的 `investigator_saves`。現有 E18 `settle-scenario` 是 player JWT route，尚未抽成 server 端多人結算 service；這不是可宣稱完成的項目，請 reviewer 判定 BLOCK 或要求補線。
2. **MP-N2 驗收第 5 項未完成**：多人容器接收並顯示 server effects/log，但未把單機三段 action/encounter Modal 全量搬到多人容器，因此「一人 Modal 未關不擋其他人」尚未有實作與多 client UI 測試。
3. 多人戰鬥容器先提供調查、移動、抽卡、拿資源與宣告結束的權威垂直切片；完整卡牌手牌/投入/傷害分配互動仍留在單機 `TestScenarioScreen`，不可誤稱已全量 parity。
4. 房態仍依 MP-N1 邊界只存在單一 process 記憶體，重啟清房；不支援水平擴展。
5. 本機前端已啟動於 `http://127.0.0.1:5174`，僅確認多人入口先走既有登入 gate；未在 browser 填入或使用任何玩家密碼。

## Review

**WARN**(守燈人代理 Hammon @ GAS Hub,2026-07-13;可 push,但 **MP-N2 工單不得標記完成**——驗收第 4/5 項未完成,需補件再審)

依代理 review checklist:

1. **commit/清單**:`e6c99fa` 單筆,16 檔與 handoff 一致;`TestScenarioScreen` 單機路徑零觸碰(App.tsx 單機 route 原樣),多人以 `/multiplayer/*` 容器隔離——雙軌紀律正確。
2. **複跑**:multiplayer-rooms **6/6**(64 選 1 互斥/ready+兩真人 gate/斷線 AI 代打+重連交還/宣告制)、game-factory 2/2、routes 3/3、ruleEngine 88/88、雙 tsc exit 0、preflight ALL PASS、sim 不崩。
3. **已交付部分的品質——高**:
   - 選人互斥/ready/開局門檻全由 server 裁決;開局工廠以 server 內部 inject 讀 bootstrap,卡片/敵人/城主資料只留 server runtime(client 拿不到底牌)。
   - 斷線代打走 WS 關閉碼區分(1000 正常切頁不觸發),AI 用同一 shared `runInvestigatorAITurn` 管線,重連交還不改調查員狀態(有測試)。
   - 宣告制:AP 未歸零也須明確宣告、全員宣告才進神話階段,序號化防重。
   - 神話階段全序列(城主選卡→怪物→恐懼→進度→補給)server 權威推進,測試涵蓋完整輪次。

**WARN 理由(兩項驗收缺口,handoff 誠實自報,判定如下):**
- **第 4 項(多人結算寫回 `investigator_saves`)——補件必做,不擋本次 push**:現況多人局打完 victory/defeat 不落任何真人存檔=玩了沒有進度。判 WARN 而非 BLOCK 的理由:①多人 v1 是驗證用垂直切片、尚未對玩家開放宣稱;②缺口是「少功能」不是「錯資料」(不會寫壞既有存檔);③E18 settle 抽成 server service 是明確可排的下一包。**條件:MP-N2 工單保持 open,結算補件過審前,多人入口不得對外宣稱「正式可玩」。**
- **第 5 項(一人 Modal 不擋他人)——隨結算包一併補**:現行多人容器只有基礎動作垂直切片,尚無三段 Modal,故此項暫無實害;搬 Modal 進多人容器時必須附多 client UI 測試。
- 手牌/投入/傷害分配等完整 parity 未達,handoff 已明示不可誤稱——認可此誠實邊界。

結論:已交付的房間/選人/宣告/代打/權威推進品質高、測試齊,**WARN——可 push 部署供兩人驗證,但 MP-N2 未完成**;請開 MP-N2b 補件包:①server 端多人結算寫回存檔(必做)②多人 Modal 並行語意+測試③補齊基礎動作以外的 parity 清單。
