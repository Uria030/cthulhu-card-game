你是守燈人代理，照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行，結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P1-1a Handoff — 遭遇 Modal 自動續跑(26071302)

署名：Nhalor @ Codex Desktop / UG

## 範圍

依 `docs/工作單_路線圖六步_26071301.md` 僅處理 RB-P1 的 1a：

- 遭遇 Modal 的敘事拍自動進入選項拍。
- 選項拍保留給玩家作決定。
- 結算拍自動關閉並釋放既有流程；回合結束與城主階段由原有 phase effect 接回，玩家主動觸發則回到可操作狀態。

未觸碰 RB-P1-1b 城主能量策略、1c 對賬、關卡/城主資料、規則書或 shared 引擎規則。

## 變更摘要

1. 新增可測的 `encounterModalFlow`：拍 1→選項、拍 3→完成，拍 2 無計時器以等待玩家選擇。
2. `TestScenarioScreen` 的計時器只發送 `{cardId, beat}` signal；真正的 modal 推進與結算在 React effect 以已提交的 `encounterPlay` 比對並執行，不在 `setTimeout` 閉包直接寫流程/遊戲狀態。
3. 移除遭遇 Modal 拍 1 的「查看選項」與拍 3 的「完成」手動按鈕，改為清楚的自動續跑提示。傷害分配 Modal 仍會優先顯示；它關閉後既有 phase gate 才繼續，避免覆蓋玩家的分配決定。

## 動過檔案

- `packages/client/src/game/encounterModalFlow.ts`（新增）
- `packages/client/src/game/encounterModalFlow.test.ts`（新增）
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.css`

## 紅線核對

- 多人一致性：未改 effects/`updatedAllies` 或裁決管線。
- 計時器競態：timer 僅寫 presentation signal；effect 對 `cardId + beat` 與最新 state 對賬後才推進。
- 冪等：過期 signal 會因 beat/card 不符而忽略；signal 消費後清空，不能重複結算同一遭遇。

## 測試結果原文

```text
packages/client: node_modules\\.bin\\tsx.CMD src\\game\\encounterModalFlow.test.ts
6 passed, 0 failed

packages/client: node_modules\\.bin\\tsc.CMD -b --pretty false
Exit code: 0

packages/client: all src\\game\\*.test.ts
10 client game test files passed

packages/client: node_modules\\.bin\\vite.CMD build
✓ 124 modules transformed.
✓ built in 1.50s
(!) existing chunk-size warning: index JS 587.54 kB (>500 kB)

repo: node scripts\\preflight.js
=== ✓ ALL PASS — 可推送 ===

repo: git diff --check
Exit code: 0
```

## 自知風險與範圍外

- 本機沒有登入態實玩；PASS push 後以 Uria 的正常遊戲路徑確認完整回合不再出現手動「繼續」按鈕、AI 指定遭遇不阻塞，以及傷害分配後流程仍正確恢復。
- 城主傳奇派發頻率與儲蓄策略屬 RB-P1-1b，尚未修改。
- 遭遇觸發五點技術債對賬表屬 RB-P1-1c，尚未開始。

## Review

待守燈人代理填寫。
