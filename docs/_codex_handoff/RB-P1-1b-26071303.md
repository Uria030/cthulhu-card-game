你是守燈人代理，照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行，結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P1-1b Handoff — 城主傳奇派發儲蓄策略(26071303)

署名：Nhalor @ Codex Desktop / UG

## 範圍

依 `docs/工作單_路線圖六步_26071301.md` 僅處理 RB-P1 的 1b：

- 修正城主日常選卡耗盡傳奇遭遇派發能量的問題。
- 保留策略以 `KeeperProfile.legendaryReserve` 與 `game_balance_settings` 資料設定控制。
- 保持強制 `advance_agenda` 時鐘、既有 E20 傳奇卡 draft 種子與關卡/卡片數值不變。

未處理 RB-P1-1c 遭遇觸發對賬，未調整城主/關卡/遭遇卡平衡資料。

## 變更摘要

1. `KeeperProfile` 新增 `legendaryReserve`：啟用旗標、目標回合、提前儲蓄回合、目標戲劇段與最低保留能量。
2. `selectKeeperActivations` 仍先執行強制毀滅時鐘；其後的日常貪婪選卡只可花到保留額之上。可用傳奇卡的最低成本是實際保留額，故不會為已冷卻、已耗盡或不存在的傳奇卡空留能量。
3. 新增冪等 `MIGRATION_047`，將上述策略列入既有 `keeper_action_points` 設定群，bootstrap 已會完整帶出該群，不需另改 API。
4. `keeperAI.test.ts` 新增「T3 高壓拍點前一回合儲蓄 → T3 調查員時機窗派發」案例，並對照關閉設定後恢復日常選卡、傳奇派發不可用。

## 動過檔案

- `packages/shared/src/game/keeperAI.ts`
- `packages/shared/src/game/keeperAI.test.ts`
- `packages/server/src/db/migrate.ts`
- `docs/_codex_handoff/RB-P1-1b-26071303.md`

## 紅線核對

- 多人一致性：未觸碰 `updatedAllies`、效果結算或 client 裁決；只改 shared 的城主選卡預算。
- 計時器競態：無 timer 或 UI state 修改。
- 冪等：`MIGRATION_047` 使用 `ON CONFLICT (setting_key) DO NOTHING`；現有 migration runner 會記錄 `schema_migrations`。
- 資料凍結：未修改關卡、城主卡、遭遇卡或既有平衡數值；只新增策略設定欄位及其預設值。

## 模擬驗收

固定四人小隊，Railway bootstrap，local shared engine：

| seed | 傳奇派發 | 結局 | 回合 |
| --- | ---: | --- | ---: |
| `2026071301` | 5 | B 敗北 | 14 |
| `2026071302` | 6 | B 敗北 | 14 |
| `2026071303` | 6 | B 敗北 | 14 |

- 至少一次派發比例：`3/3 = 100%`，符合工單 `>= 2/3`。
- 三場都維持 B 敗北結構，沒有因城主變強而出現勝負翻轉；依工單只記錄、不調整。
- E20 handoff 的已知根因是舊選卡貪婪耗盡能量、傳奇派發 0 次；本包用相同正式關卡入口驗證已不再為 0。

## 測試結果原文

```text
packages/client: node_modules\\.bin\\tsx.CMD packages\\shared\\src\\game\\keeperAI.test.ts
24 passed, 0 failed

packages/shared: all src\\game\\*.test.ts
21 shared game test files passed

packages/client: node_modules\\.bin\\tsc.CMD --noEmit -p packages\\client\\tsconfig.json
Exit code: 0

packages/server: node_modules\\.bin\\tsc.CMD --noEmit -p packages\\server\\tsconfig.json
Exit code: 0

repo: node scripts\\preflight.js
=== ALL PASS — 可推送 ===

repo: git diff --check
Exit code: 0

Railway smoke (local engine + live bootstrap):
tsx.CMD scripts\\g1-sandbox\\sim-slit-3ai.ts --seed 2026071301 --json
legendaryDispatches=5, outcomeCode=B, turnNumber=14
tsx.CMD scripts\\g1-sandbox\\sim-slit-3ai.ts --seed 2026071302 --json
legendaryDispatches=6, outcomeCode=B, turnNumber=14
tsx.CMD scripts\\g1-sandbox\\sim-slit-3ai.ts --seed 2026071303 --json
legendaryDispatches=6, outcomeCode=B, turnNumber=14
```

## 自知風險與範圍外

- 單人局每回合基礎能量為 2，而強制議程卡成本為 3；此包不改該既有經濟公式，因此單人局無法保證同一回合為傳奇卡留足 2 AP。這是現有數據/平衡邊界，不在本包調整範圍。
- 設定已經由既有 balance API 與 bootstrap 帶出；admin 頁面的欄位呈現沿用既有設定群機制，未另做 UI 調整。
- RB-P1-1c 五點觸發途徑的歷史對賬尚未開始。


## Review

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13;依部署規則 PASS 後 push)

依代理 review checklist:

1. **commit/清單**:`d55cfd7` 單筆,4 檔與 handoff 一致;關卡/城主卡/遭遇卡/既有平衡數值零觸碰(只新增策略設定欄位+預設值)。
2. **複跑**:keeperAI **24/24**、ruleEngine 88/88、turnLoop 6/6、雙 tsc exit 0、preflight ALL PASS。
3. **涉引擎 → sim 已跑且數字復現**:seed 2026071301 → `legendaryDispatches=5, outcomeCode=B, turnNumber=14`,與 handoff 模擬表**完全一致**;E20 舊根因(貪婪耗盡→派發 0 次)已不再為 0。
4. **邏輯審**:
   - 強制毀滅時鐘在儲蓄計算**之前**結算(469 註解自證),不被保留額取消/延後——工單紅線守住。
   - `reservedLegendaryActionPoints`:只對「可執行+未冷卻+未耗盡」的傳奇卡保留,無可用傳奇卡回 0——**不為死卡空留能量**,設計正確;保留額=max(minimumAP, min(可用傳奇成本)),語意清楚。
   - 日常選卡 filter `state.actionPoints - cost >= reserved`——只約束貪婪選卡,不影響評分邏輯。
   - 策略完全資料驅動(`KeeperProfile.legendaryReserve` ← `game_balance_settings`),設定讀取器對 boolean/int/tier 都有 fallback 防呆——「風格即資料」原則正確。
5. **MIGRATION_047**:`ON CONFLICT (setting_key) DO NOTHING` 冪等;併入既有 `keeper_action_points` 設定群,bootstrap 既有路徑帶出、零 API 改動。
6. **紅線核對**(handoff 自列四項,我逐項驗過):多人一致性未觸、無計時器、migration 冪等、資料凍結——全部屬實。
7. **已知邊界誠實記載**:單人局基礎能量 2 < 強制議程成本 3 的既有經濟公式不在本包範圍——正確不越界;若未來單人傳奇派發要保證,屬平衡數據工單。

結論:儲蓄策略資料驅動、強制時鐘不受影響、sim 數字復現、測試全綠,PASS。push 後 Uria 實玩驗:多人局中後期城主確實派得出傳奇遭遇、前期壓力無異常。
