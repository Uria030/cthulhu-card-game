你是守燈人代理，照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行，結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P1-1c Handoff — 遭遇觸發五點對賬(26071304)

署名：Nhalor @ Codex Desktop / UG

## 範圍

依 `docs/工作單_路線圖六步_26071301.md` 僅處理 RB-P1 的 1c：

- 逐條對賬 `docs/技術債_遭遇卡觸發系統_26061202.md` 的 Uria 五點。
- 對現碼發現的玩家地點觸發誤抽做最小修正與測試。
- 將內容與規範殘缺列出明確去向；不手改遭遇內容、不啟動生產線、不改關卡/城主平衡資料。

## 最小修正

新增 shared `playerActionEncounterContext()`：只有 `move` 才附帶 `locationId`，所以地點觸發代表「進入該地點」，而站在該地點拿資源、抽卡等不會反覆抽遭遇；`trigger_actions` 仍照常觸發特定行動。`TestScenarioScreen` 改用此 shared context。

## 五點清單對賬表

| # | 2026-06-12 原問題 | 現碼與部署證據 | 結論 | 殘缺與去向 |
| --- | --- | --- | --- | --- |
| 1 | 回合結束抽缺關卡設定欄位 | `MIGRATION_036` 有 stage/scenario `encounter_trigger_config`、CRUD 與 bootstrap merge；`MIGRATION_041` 對雨夜開啟 `draw_on_turn_end=true`。client `beginTurnEnd` 逐席處理玩家 Modal 與 AI 自動結算。 | 已完成 | 無引擎殘件。 |
| 2 | 混沌袋 `headline` 無法觸發 | `ruleEngine` 產生 `headline_drawn`，client 轉成 `chaos_headline` context；shared `shouldDrawEncounter` 有專屬 gate。Railway bootstrap 的雨夜 config=`chaos_headline:true`、bag 有 `headline:1`。 | 已完成 | 無引擎殘件。 |
| 3 | 城主主動投放沒有 encounter 神話卡彈藥 | `MIGRATION_041` 已 seed 並綁定 `e20_legendary_encounter_dispatch`；E20 接上調查員階段派發；RB-P1-1b 的三種子 sim 各派發 5/6/6 次。 | 機制完成 | 此卡仍 `draft` mechanics-only；正式名稱/敘事由 MOD-10 keeper-cards 生產線處理，非本包手改。 |
| 4 | 玩家進地點／特定行動缺關卡設定與接線 | `trigger_locations`/`trigger_actions` 均已入 config；雨夜配置磚牆入口、`search`、`investigate_hidden`。本包補正「非移動動作誤帶地點」問題，shared 測試覆蓋移動命中、站定不誤抽、特定行動命中。 | 已完成 | 無引擎殘件。 |
| 5 | Pipeline 規範未把觸發方式列為關卡設置 | `pipeline-story-to-stage/regulation.md` 第 10 點仍只規範遭遇池組成、權重與數量，沒有 `encounter_trigger_config` 產出/驗閘；規則書/s14 也未補正式權威條目。 | 殘缺 | 立 `PIPELINE-ENCOUNTER-TRIGGER-SPEC` 工作包：補 pipeline 第 10 點、階段 30 輸出 schema、validator 與規則/s14 的權威引用；不是本包的引擎小修。 |

## Railway 唯讀對賬(2026-07-13)

以正式測試關卡 bootstrap 查到：

- `triggerConfig`: `draw_on_turn_end=true`、`chaos_headline=true`、`keeper_mythos=true`、`trigger_locations=[g_slit_mouth_loc_brick_wall]`、`trigger_actions=[search, investigate_hidden]`。
- 遭遇池有 8 張；`e20_legendary_encounter_dispatch` 已在神話池、`design_status=draft`、`activation_timing=investigator_phase_reaction`。
- 混沌袋有 `mythos_markers.headline.count=1`。
- 卡片內容統計：8 張中只有「書架間的低語」的選項文字與效果完整；其餘 7 張有選項殼但文字/結構化效果均為空。此為現行資料品質事實，不以引擎修補掩蓋。

## 內容與規範移交

1. **MOD-10 / keeper-cards 校準輪**：先補雨夜 7 張空殼遭遇卡，再擴至全庫；每張須有選項文字、敘事、結構化效果與三威脅軸覆蓋檢核。E20 傳奇神話卡的正式名稱/敘事亦由此線處理，維持 draft 到 Uria 終審。
2. **PIPELINE-ENCOUNTER-TRIGGER-SPEC**：將「觸發方式是關卡設置」寫入 pipeline 規範、階段 30 輸出與 validator，補齊第 5 點的權威來源。

## 動過檔案

- `packages/shared/src/game/encounters.ts`
- `packages/shared/src/game/encounters.test.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `docs/_codex_handoff/RB-P1-1c-26071304.md`

## 紅線核對

- 多人一致性：未改遭遇效果結算或 `updatedAllies` 管線；只建立觸發 context。
- 計時器競態：無 timer 變動。
- 冪等：無 migration、無資料寫入；同一 action context 只作判定，不產生重複抽牌。
- 資料凍結：只讀 Railway bootstrap；未修改關卡、城主、遭遇卡或平衡資料。

## 測試結果原文

```text
packages/client: node_modules\\.bin\\tsx.CMD packages\\shared\\src\\game\\encounters.test.ts
17 passed, 0 failed

packages/client: all src\\game\\*.test.ts
10 client game test files passed

packages/shared: all src\\game\\*.test.ts
21 shared game test files passed

packages/client: node_modules\\.bin\\tsc.CMD --noEmit -p packages\\client\\tsconfig.json
Exit code: 0

packages/server: node_modules\\.bin\\tsc.CMD --noEmit -p packages\\server\\tsconfig.json
Exit code: 0

repo: node scripts\\preflight.js
=== ALL PASS — 可推送 ===

repo: git diff --check
Exit code: 0
```

## 自知風險與範圍外

- 目前 player-facing 的 `trigger_locations` 只在玩家移動時觸發，符合原五點的「進地點」語意；AI 席位不會模擬此類地點/行動條件抽卡，仍只跑回合結束與城主派發。是否要讓 AI 主動觸發關卡劇情條件，是多人/AI 行為包的設計題，不在本包臆補。
- 7 張遭遇空殼使引擎雖會自然抽卡，卻未必產生可見後果；這是內容 BLOCK，不能由 client 造假效果掩蓋。
- 第 5 點的 Pipeline 規範缺口仍存在；本包只建立正式去向，沒有未授權修改生產規範。
