你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P3a1 handoff — 法器三時機與過路費:儲蓄型結算接線

日期碼:26071312  
Codex: Nhalor @ Codex Desktop / UG  
狀態:本機 commit 後待 HUB review  
Push:依本包 review 結論；本 handoff 寫完時尚未 push。

## 依據與現況對賬

- 工單:`docs/工作單_路線圖六步_26071301.md` RB-P3 3a-1。
- 規範:`docs/v07_當前版本_26042606/s09_axis_dual_combat_part2.md` §5.1-§5.4；`h03_supp02_effects.md` 的卡片效果六要素與回合開始觸發。
- 現碼對賬:工單「schema 有、結算未接」已過時。已推送的 E3 (`5451099`、`87bc25d`) 已完成即時/檢定/儲蓄三種遭遇破除、`TOLL_FUNCTIONS` 與法器解遭遇測試；目前 `encounters.test.ts` 17/17 仍全綠。
- 本包只補 E3 未覆蓋的儲蓄型「場上累積」接線，不重做既有破除結算或改卡片平衡。

## 變更摘要

1. `effectsExecutor` 新增儲蓄型回合開始執行器:
   - 只處理 `is_talisman + break_timing=stockpile` 資產上的 `round_start` / 舊別名 `on_turn_start` 的 `gain_use`。
   - 以來源卡 ID 精確補該張法器，不再按場上順序誤補其他武器/消耗品。
   - `gain_use` 依 `break_charge_max`(或 ammo/uses)封頂，effect log 回報實際補回量。
   - 非 `gain_use` 的儲蓄回合開始效果會明確回報 unsupported；不讓本包範圍外的通用回合觸發繞過 ruleEngine 後處理。
2. 新格式的儲蓄型法器在打出時由 0 計量起算，之後由 shared `runTurnStartUpkeep` 透過 effectsExecutor 累積；單機戰鬥板與多人 server 的回合交接皆接同一管線。
3. 向後相容:現有 seed 的儲蓄法器只有 `stockpile_accumulation_rule` 文字，尚無結構化 `gain_use` effect。這類舊卡保留原本的滿充能進場，不會因本次部署變成不可用。
4. 補回歸:
   - 儲蓄法器只補自身、接受 trigger 別名、封頂。
   - 回合開始實際經 effectsExecutor 累積。
   - 已配置的儲蓄法器從 0 進場；未配置的舊卡維持滿充能。

## 動過的檔案

- `packages/shared/src/game/effectsExecutor.ts`
- `packages/shared/src/game/effectsExecutor.test.ts`
- `packages/shared/src/game/upkeep.ts`
- `packages/shared/src/game/upkeep.test.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/ruleEngine.test.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/server/src/services/multiplayer-rooms.ts`

## 測試結果

```text
packages/shared/src/game/encounters.test.ts
17 passed, 0 failed

packages/shared/src/game/effectsExecutor.test.ts
46 passed, 0 failed

packages/shared/src/game/upkeep.test.ts
14 passed, 0 failed

packages/shared/src/game/ruleEngine.test.ts
90 passed, 0 failed

packages/server/src/services/multiplayer-rooms.test.ts
12 passed, 0 failed

packages/server/src/routes/multiplayer.test.ts
5 passed, 0 failed
```

```text
tsc -p packages/shared/tsconfig.json
node scripts/build-shared-runtime.mjs
tsc --noEmit -p packages/server/tsconfig.json
tsc --noEmit -p packages/client/tsconfig.json
all exit 0

full server/client test sweep: exit 0
packages/client Vite production build: exit 0
node scripts/preflight.js: ALL PASS
git diff --check: exit 0
```

```text
packages/client/node_modules/.bin/tsx.CMD ../../scripts/g1-sandbox/sim-slit-3ai.ts --seed 20260713
exit 0
雨夜的真相:第 11 回合勝利
城主啟用 20 次；回合結束遭遇 34 次；城主傳奇派發 4 次；遭遇重洗 4 次
```

## 自知風險與範圍外發現

- 本包不解析 `stockpile_accumulation_rule` 的自然語言。能自動累積的卡必須以既有 `card_effects` 寫出 `round_start/on_turn_start + gain_use`；這符合 h03 的結構化效果合約。
- 現有 `talisman-templates.ts` 三張儲蓄範本沒有這些結構化效果。依「測試期關卡/城主資料凍結，平衡只記錄不調」不在本包補寫 DB 卡片內容；後續內容/資料包應把文字規則轉成 effect rows，再自然啟用本次引擎。
- `鹽圈瓶` 的「研究遺跡/閱讀書籍」與 `古印之卷` 的「完成關卡/解謎」不是 round-start 事件，屬 3a-2 deferred trigger/effect-code 收斂範圍，未在本包虛構觸發器。
- 不改 E3 的遭遇 fallback、自然 1/20 變體或 AI 法器選擇；仍是既有明確範圍外事項。

## Review

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13;依部署規則 PASS 後 push)

依代理 review checklist:

1. **commit/清單**:`ebbf731` 單筆,8 檔與 handoff 一致;不重做 E3 既有破除結算、不改卡片平衡、凍結資料零觸碰。
2. **複跑**:effectsExecutor **46/46**、upkeep 14/14、ruleEngine **90/90**、encounters 17/17、multiplayer-rooms 12/12、雙 tsc exit 0、preflight ALL PASS;**sim seed 20260713 數字復現**(城主啟用 20/回合結束遭遇 34/傳奇派發 4/重洗 4,與 handoff 一致)。
3. **涉引擎核心審**:
   - **`gain_use` 修正是真改良**:舊行為「補第一張有次數的資產」會誤補武器/消耗品;新行為 `sourceAssetId` 精確補來源卡、無指名時保留舊行為(向後相容)——語意正確。
   - **封頂正確**:cap = ammo/uses/break_charge_max 擇一,`Math.min(cap, current+amount)`,effect log 回報實際補回量(gained/requested/left/max 齊)——不會超充。
   - **範圍紀律**:只處理 `is_talisman + stockpile` 的 `round_start/on_turn_start` `gain_use`;其他儲蓄回合開始效果明確 unsupported、不讓通用回合觸發繞過 ruleEngine——不虛構觸發器([[結構化效果合約]]遵守)。
   - **向後相容**:新格式卡從 0 進場靠 upkeep 累積;舊卡(只有文字規則無結構化 effect)維持滿充能進場,部署不會讓現有卡變磚——過渡設計正確。
   - 單機與多人 server 回合交接**接同一管線**(TestScenarioScreen/multiplayer-rooms 皆 6-9 行小改接 shared),無平行實作。
4. **誠實邊界**:不解析自然語言規則(需 content 包把文字轉 effect rows);鹽圈瓶/古印之卷的非 round-start 觸發歸 3a-2;E3 fallback/自然 1/20/AI 法器選擇維持範圍外——全部如實記載。

結論:精確補回+封頂+雙軌同管線+向後相容,測試 sim 全數復現,PASS。後續:內容包把三張儲蓄範本的文字規則轉結構化 effect 後,本引擎自然啟用。
