你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P3a2b1 handoff — 傷害/恐懼 reaction 純交易核心

日期碼:26071314  
Codex:Nhalor @ Codex Desktop / UG  
狀態:RB-P3a2 的第一個實作子步驟;本機 commit 後待 HUB review。

## 交件內容

- 新增 shared `reactions.ts`,把受傷/受恐懼前的 reaction 做成無 closure、可序列化的 `PendingReaction` 與純函式交易。
- reaction 候選僅從當前調查員的手牌、額外牌組、場上資產取得;候選僅可回傳給該調查員的私有狀態端點。
- 支援此最小切面的 `before_take_damage` / `before_take_horror` 與 `heal_hp` / `heal_san` 防止量;reaction 不消耗 AP。
- hand/extra event 付費後進棄牌堆;asset 可支付 uses/ammo、橫置而留在場上;額外牌組 event 已有回歸驗證。
- 開窗時即排除資源不足、uses 不足與已橫置的資產;resolve 時再驗候選、區域、資源與 `triggeredReactions` 防重送。
- 擴充 `CardData` 及 client bootstrap adapter,不再遺失 DB `card_effects` 的 `condition`、`cost`、`target` 與 `is_extra` 資訊。

## 動過檔案

- `packages/shared/src/game/reactions.ts`
- `packages/shared/src/game/reactions.test.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/index.ts`
- `packages/client/src/game/cardDataAdapter.ts`

## 驗證

```text
Full server/shared/client test sweep: PASS
reactions.test.ts: 6 passed, 0 failed
server tsc --noEmit: PASS
client tsc --noEmit: PASS
Vite production build: PASS
node scripts/preflight.js: === ALL PASS — 可推送 ===
git diff --check: PASS
```

新增回歸涵蓋:

1. 僅列符合時機且能防止目前 operation 的卡。
2. 資源不足、uses 用盡、已橫置資產不開 reaction 窗。
3. 手牌 event 零 AP、付資源、棄牌、減傷後結算原 operation。
4. 場上 asset 扣 uses、橫置且保留在場。
5. 額外牌組 event 可在 reaction 時機零 AP 打出、付費並離開額外牌組。
6. pass 完整結算、同卡已觸發時拒絕重送。

## 請 reviewer 特別確認

1. `PendingReaction` / `ReactionDecision` / `ReactionResolution` 是否足以讓下一步 server 沿用 pendingEncounter 的 gate、sequence 與 disconnect-AI fallback,而不是建立第二套非同步狀態。
2. 卡片在 hand/extra/asset 三個區域的付款、移動與 uses/exhaust 判斷是否符合 ch3 reaction「條件觸發、玩家主動、零 AP」及額外牌組只能 reaction/action 使用的邊界。
3. bootstrap adapter 的欄位保存是否只擴大資料忠實度,未改變既有非 reaction 卡的行為。

## 明確尚未完成的範圍與風險

- **RB-P3a2 尚未完成**:本包沒有新增 `ActionType`、server route、room pending state、私有 payload、UI modal、sequence 寫入與斷線 AI fallback;下一個子步驟必須直接接 N2c2 的 pendingEncounter 模式。
- 本步刻意只做 `before_take_damage` / `before_take_horror`;`on_take_*`、檢定 reaction 與其他 reaction effect_code 均不宣稱已支援,仍保留 deferred/立債。
- 本步將 `heal_hp`/`heal_san` 在 **before** window 解讀為減少即將結算的傷害/恐懼。既有 `on_take_horror` 樣例的事後治療語意不在本交易核心內,需後續以獨立 after-window 設計處理。
- `direct` 原樣保留於 operation/effect,但本步不把它當成禁止 self-reaction 的規則;其現有用途是盟友分配限制。若規則需禁止 direct 防止,請守燈人裁定後另補。

## Review

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13;RB-P3a2 仍 open——本包為純交易核心,server 接線/UI/斷線 fallback 在下一子步驟)

依代理 review checklist:

1. **commit/清單**:`114cda0` 單筆,6 檔與 handoff 一致;未改遊戲資料/規則書;凍結區零觸碰。
2. **複跑**:reactions **6/6**、ruleEngine 90/90、effectsExecutor 46/46、雙 tsc exit 0、preflight ALL PASS、sim 不崩。
3. **特別確認事項逐項回覆**:
   - **確認 1(合約是否足以沿用 pendingEncounter 模式)— 成立**:`PendingReaction` 可序列化無 closure、`targetInvestigatorId` 明確、`ReactionDecision` 是純訊息、`resolvePendingReaction` 純函式且註解言明「room service 持有 sequence 去重與 triggeredReactions 寫入」——與 N2c2 pendingEncounter 的 gate/sequence/斷線 AI fallback 完全同構,**不會產生第二套非同步狀態**。補一個接線契約要求:`outcome='invalid'` 時原 operation 未結算,room service 必須**保持窗口開啟**(或重新開窗),不可把 invalid 當 pass 丟掉傷害結算——下一子步驟的測試要涵蓋這條。
   - **確認 2(三區付款/移動/uses 邊界)— 符合**:hand/extra event 付資源→棄牌;asset 扣 uses/橫置留場;零 AP 全程未觸 actionPoints;extra 牌只在 reaction 時機打出(action 用途沿既有);開窗即排除付不起/uses 不足/已橫置,resolve 再全套重驗(zone/resources/uses/already-triggered)——雙重驗證正確,防止窗口開啟後狀態變化的競態。prevention 以 `Math.min(operation.amount, preventAmount)` 封頂、原 operation **恰好結算一次**(pass 與 play 兩路都單次 applyOperation)——語意乾淨。
   - **確認 3(adapter 忠實度)— 成立**:只補 `condition/cost/target/is_extra` 欄位保存,未改任何既有非 reaction 卡行為(全量測試綠背書)。
4. **`direct` 裁定(handoff 請守燈人裁)**:規則書現行 `direct` 語意=盟友分配限制,**未見「禁止 self-reaction」的權威條文**;依「不自行添加規則裁定」原則,**維持本包現狀(direct 不禁反應)**。若 Uria 認定 direct 傷害應不可反應,屬規則裁定,請在規則書/s09 補權威條目後再實作——不在引擎端先斬後奏。
5. **誠實邊界**:only before-window;`on_take_*` 事後治療語意留 after-window 獨立設計;檢定 reaction 與其餘 effect_code 維持立債——與 3a2a 盤點一致。

結論:交易核心純函式設計正確、雙重驗證防競態、合約可直接沿用 N2c2 掛起模式,PASS。下一子步驟:server route+room pending+私有 payload+UI+斷線 fallback,並補「invalid 不丟失 operation」測試。
