你是守燈人代理，照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行，結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P3b1a handoff — 光源物件與視線規則核心

日期碼:26071317  
Codex:Nhalor @ Codex Desktop / UG  
狀態:RB-P3b1 的第一個可審查子步驟；本機 commit 後待 HUB review。

## 交件內容

- `ScenarioState.lightSources` 新增可序列化的場上光源物件：來源卡片實例、所在地點與照明半徑。欄位為 optional，既有存檔仍可載入為無光源。
- 新增 `lighting.ts`：光源以物件所在地點為中心，沿 `connectedTo` 以 BFS 計算半徑覆蓋；不因調查員移動而跟著移動。
- 夜間/黑暗不再因 `create_light` 被改寫成 `day`。`create_light` 建立或更新光源物件，`extinguish_light` 移除資產自己的光源；無來源的場景效果則移除指定地點的光源。
- 攻擊、閃避與調查員 AI 攻防估值統一改用 `visibilityModifierAtLocation`，因此同一份光源覆蓋會同時影響真人規則結算與 AI 選擇。
- 資產的 `execute_card_action` 會帶入 `sourceAssetId`，讓重複使用同一盞燈更新既有物件而非堆疊無主光源。

## 動過檔案

- `packages/shared/src/game/state.ts`
- `packages/shared/src/game/lighting.ts`
- `packages/shared/src/game/lighting.test.ts`
- `packages/shared/src/game/checks.ts`
- `packages/shared/src/game/effectsExecutor.ts`
- `packages/shared/src/game/effectsExecutor.test.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/ruleEngine.test.ts`
- `packages/shared/src/game/investigatorAI.ts`
- `packages/shared/src/game/index.ts`

## 驗證

```text
lighting.test.ts: 3 passed, 0 failed
effectsExecutor.test.ts: 47 passed, 0 failed
ruleEngine.test.ts: 91 passed, 0 failed
investigatorAI.test.ts: 49 passed, 0 failed
Full server/shared/client test sweep: PASS
shared tsc: PASS
server tsc --noEmit: PASS
client tsc --noEmit: PASS
node scripts/build-shared-runtime.mjs: PASS
Vite production build: PASS
node scripts/preflight.js: === ALL PASS — 可推送 ===
git diff --check: PASS
G1 fixed-seed sim --seed 260713: PASS (T12 victory)
```

## 請 reviewer 特別確認

1. `create_light` 與 `extinguish_light` 是否確實只處理光源物件，沒有再覆寫夜間/黑暗的基底環境。
2. BFS 半徑是否只經由目前 `connectedTo` 拓撲展開，且光源中心是物件位置而非持有者/行動者位置。
3. 攻擊、閃避與 AI 的攻防成功率是否都已收斂至同一視線 helper，沒有殘留直接讀 `visibility` 的分支。
4. `sourceAssetId` 在資產 action 路徑的傳遞是否不影響既有隨機效果的預設 RNG 行為。

## 明確尚未完成的範圍與風險

- 本子步只完成光源物件與視線判定核心；地圖層的光源呈現、玩家可見的互動入口另列為 RB-P3b1 後續子步。
- 本子步讀取現有 `effect_params.radius`，未建立新的關卡/卡片資料格式；未提供 radius 時預設只照亮自身地點 (`0`)。
- 光源持續時間、消耗/uses 與離場清理屬路線圖後續的 consume 三重稽核，不在本包假裝已完成。
- 現有 `LocationInstance.visibility` 仍是單一基底環境欄位；火焰與黑暗的完整同格疊層表示法不是本步範圍。光源覆蓋已可在夜間與黑暗中提供視線。
- 不新增卡片、關卡、遭遇、城主資料，也不變更凍結的關卡配置。

## Review

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13;RB-P3b1 續 open——地圖光源呈現/互動入口為後續子步)

依代理 review checklist:

1. **commit/清單**:`65f9ac8` 單筆,10 檔與 handoff 一致;不新增卡片/關卡/遭遇/城主資料,凍結配置零觸碰。
2. **複跑**:lighting **3/3**、effectsExecutor **47/47**、ruleEngine **91/91**、keeperAI 25/25、雙 tsc exit 0、preflight ALL PASS、sim 不崩。
3. **特別確認事項逐項回覆**:
   - **確認 1(不改寫基底環境)— 成立**:`create_light` 建立/更新光源物件而非把 visibility 改成 `day`——這是本包最重要的語意修正(舊行為會永久破壞夜間場景);`extinguish_light` 移除自己的光源、場景效果移除指定地點光源;`lightSources` optional,舊存檔載入為無光源零破壞。
   - **確認 2(BFS 拓撲+物件中心)— 成立**:`sourceReaches` 只沿 `connectedTo` 逐層展開(seen 防環、frontier 空即停),中心=物件所在地點、**不隨持有者移動**——「光源是場上物件」的規則語意正確;radius 0 只照自身格;day/fire 恆有視線。
   - **確認 3(視線判定收斂)— 成立**:performAttack/resolveEvade/AI enumerateCandidates(攻+閃兩處)全部改走 `visibilityModifierAtLocation`;grep 殘留檢查僅剩基底 helper `visibilityModifier` 內部的 night/darkness 判斷(它就是規則本體,正確保留)——真人結算與 AI 估值同一份光源覆蓋,無雙軌。
   - **確認 4(sourceAssetId 傳遞)— 成立**:資產 action 帶 `sourceAssetId` 使同一盞燈更新既有物件不堆疊;RNG 參數位置未動(47 項 executor 測試+91 項引擎測試綠背書,含既有隨機效果零回歸)。
4. **範圍誠實**:光源持續時間/uses 消耗/離場清理歸 consume 三重稽核;同格火焰+黑暗疊層表示法不在本步;radius 缺省 0 不造格式——立債清楚不偷擴。

結論:夜間光源從「改寫地形」修正為「場上物件+拓撲覆蓋」,規則語意正確、視線判定單一來源、測試全綠,PASS。後續子步:地圖光源呈現與互動入口。
