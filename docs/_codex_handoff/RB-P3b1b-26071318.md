你是守燈人代理，照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行，結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P3b1b handoff — 測試關卡光源地圖呈現

日期碼:26071318  
Codex:Nhalor @ Codex Desktop / UG  
狀態:RB-P3b1 的第二個可審查子步驟；依賴已推送且 PASS 的 RB-P3b1a (`65f9ac8`)。

## 交件內容

- `TestScenarioScreen` 地圖直接讀既有 `ScenarioState.lightSources`：光源所在格顯示靜態提燈標記與半徑；被覆蓋的夜間/黑暗地點只呈現柔和照明，絕不把「受到照亮」誤畫成「光源在此」。
- 新增 client 純表示層 helper `lightingForLocation`，規則真相仍呼叫 shared 的 `hasLineOfSight`；client 不自算 BFS 或規則修正。
- 戰役紀錄新增 `light_source_created` / `light_source_extinguished` 中文敘事，讓既有卡片 action 或 stage map operation 點燈後有可讀回饋。
- 沒有新增自由放燈按鈕、卡片、關卡或資料格式。光源仍只由既有 `create_light` / `extinguish_light` 效果碼建立或移除。

## 動過檔案

- `packages/client/src/game/mapLighting.ts`
- `packages/client/src/game/mapLighting.test.ts`
- `packages/client/src/screens/TestScenarioScreen.tsx`
- `packages/client/src/screens/TestScenarioScreen.css`

## 驗證

```text
mapLighting.test.ts: 3 passed, 0 failed
Full server/shared/client test sweep: PASS
shared tsc: PASS
server tsc --noEmit: PASS
client tsc --noEmit: PASS
node scripts/build-shared-runtime.mjs: PASS
Vite production build: PASS
node scripts/preflight.js: === ALL PASS — 可推送 ===
git diff --check: PASS
```

本機 Vite 已啟動並能載入登入畫面；未登入的本機環境沒有後端關卡資料，因此 `/scenario/test` 與 `/scenario/card-lab` 依既有守門顯示載入失敗訊息，不能誤稱為真機視覺 smoke 已完成。

## 請 reviewer 特別確認

1. `lightingForLocation` 是否只做 shared 規則結果的展示，沒有在 client 重建 BFS/視線規則或改動 state。
2. 光源標記是否僅在 `source.locationId` 的地圖格出現；相鄰覆蓋格只得到 `loc-illuminated` 狀態。
3. `light_source_created` / `light_source_extinguished` 的 log 參數是否與 RB-P3b1a engine emit 的 `{ location, radius }` 形狀一致。
4. CSS 標記是否不攔截地點的移動/調查按鈕或棋子互動層。

## 部署後真機 smoke

1. 以 Creator 帳號進入已有 `create_light` 效果的測試關卡或實驗場。
2. 觸發點燈：來源格出現一個提燈標記，戰役紀錄顯示地點與半徑。
3. 若 radius=1，相鄰夜間/黑暗格顯示受照明但沒有第二盞燈；半徑外地點不變。
4. 讓調查員離開來源格：提燈仍留在原地，攻擊/閃避依 RB-P3b1a 的規則判定。
5. 觸發熄燈：提燈標記與照明提示消失，紀錄正確寫入。

## 明確尚未完成的範圍與風險

- 本包不製造新的 `create_light` 卡片或關卡內容；若目前資料庫沒有可用的效果來源，真機 smoke 須由既有 stage map operation 或後續內容派工提供，不能由 UI 假造效果。
- 多人 `MultiplayerScenarioScreen` 仍是獨立的簡化地圖，不屬本次單人測試關卡的視覺重製範圍；權威快照本身已包含 `lightSources`。
- 光源燃料/持續時間/離場清理仍由 RB-P3b2 consume triple 語意對賬處理。

## Review

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13;RB-P3b1 續 open——光源燃料/持續時間歸 RB-P3b2)

依代理 review checklist:

1. **commit/清單**:`9c61f41` 單筆,5 檔與 handoff 一致;純 client 表示層,不造卡片/關卡/資料格式,光源仍只由既有效果碼建立。
2. **複跑**:mapLighting 3/3、shared lighting 3/3、ruleEngine 91/91、client tsc exit 0、preflight ALL PASS、vite build 成功。
3. **特別確認事項逐項回覆**:
   - **確認 1(不重建規則)— 成立**:`lightingForLocation` 僅 18 行,illuminated 直接呼叫 shared `hasLineOfSight`、sources 只做 locationId filter——零 BFS 重建、零 state 改動,純投影。
   - **確認 2(標記 vs 照明區分)— 成立**:提燈標記只 render `source.locationId === 該格` 的來源;覆蓋格只得 `loc-illuminated` class(且僅 night/darkness 格才亮,day 格不畫多餘效果)——「受照亮 ≠ 光源在此」語意正確。
   - **確認 3(log 參數形狀)— 成立**:describeEffect 讀 `p.location`/`p.radius`,與 b1a engine emit 的 `{location, radius}` 一致;熄燈敘事只用 location。
   - **確認 4(不攔截互動)— 成立(實查)**:調查/移動按鈕位於 `.loc-footer`(插畫區之外),提燈標記定位在 `.loc-illustration` 右下角、不重疊;標記 `pointer-events:auto` 僅供 tooltip,點擊仍冒泡至地點格 onClick——互動層無遮擋。
4. **誠實記載**:本機無登入態、真機視覺 smoke 未完成——正確不偽稱;且若 DB 目前無 `create_light` 效果來源,smoke 需由既有 stage map operation 或內容派工提供、**不得由 UI 假造效果**——紀律正確。
5. 多人簡化地圖不在本範圍,但權威快照已含 lightSources——後續多人地圖要呈現時直接吃同一資料。

結論:純表示層、規則單一來源、標記/照明語意分明、互動層無遮擋,PASS。部署後照 handoff 五步 smoke(點燈→標記+紀錄→radius 覆蓋→人走燈留→熄燈)。
