你是守燈人代理，照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行，結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P4a handoff — R4 檢定刻度正式收斂

日期碼:26071321
Codex:Nhalor @ Codex Desktop / UG
狀態:RB-P4a 完成候選；本機 commit 後待 HUB review。

## 交件內容

依 `docs/工作單_路線圖六步_26071301.md` 的 RB-P4-1 與 `docs/設計債_檢定刻度分歧_26061201.md`，把檢定難度收斂為唯一的 d20 絕對刻度 `10-28`。

1. shared `checks.ts` 新增唯一的刻度邊界：`isAbsoluteCheckDc` 供新資料入口拒絕非絕對難度；`normaliseCheckDc` 僅供部署交接期間讀取舊快照，把舊相對 `N` 解讀為 `10 + N`。
2. 調查、隱藏調查、搜尋、遭遇結算、遭遇 AI 期望值與神話附著解除都改走同一個 `normaliseCheckDc`。怪物 DC、`threat_strength`、法器強度等非檢定數字完全未接此轉換。
3. `MIGRATION_049` 冪等地把已存的 `locations.shroud`、`encounter_card_options.check_dc`、神話附著 `release_dc` 的 `0-9` 舊值加十；不碰既有 `10+` 絕對值。它也把四段已確認的神話卡面短寫改成「檢定難度 12/13」，並加入資料庫 CHECK 與地點預設值 `10`。
4. 後台入口拒絕新的小數字資料：地點 `shroud`、遭遇選項 `check_dc`，以及神話效果 `release_dc` / `check_dc` / `dc` 必須是 `10-28` 整數；新增地點不再預設 `2`，改為 `10`。
5. 已更新版本化的玩家卡 Gemini prompt 與專案規範索引；也更新根層 s14 part2 的舊範例與 MOD-10 本機規約，讓新內容不再生成 `檢定(N)`。

## 正式資料唯讀盤點

2026-07-13 對 Railway 唯讀取得目前唯一可玩關卡的 bootstrap：

| 資料面 | 盤點結果 | 本包處置 |
|---|---|---|
| 關卡地點 | 6 張皆已是 `11-14`（13/12/12/13/11/14） | migration 不會重算這些正確資料 |
| 遭遇檢定 | `ec_library_whispers` 兩個選項仍為 `4`、`6` | migration 轉為 `14`、`16` |
| 附著解除 | `mc_madness_grip.release_dc=3` | migration 轉為 `13` |
| 神話卡面短寫 | `mc_madness_grip`、`mc_false_lead`、`mc_npc_betrayal`、`mc_clue_tamper` 共 4 段 | migration 改為絕對難度文字 |

正式 player-card catalogue 的 `/api/cards` 需要登入；本機沒有 `DATABASE_URL`、管理 token 或非範例 `.env`，所以沒有繞過帳號憑證掃描 392 張玩家卡。此限制不影響上述三個實際會被引擎解讀的資料面；新版 prompt 與 server gate 已阻止新污染。部署後由有管理權限的帳號在 MOD-14 再掃一次既有玩家卡文字，列為 smoke。

## 動過檔案

### Git 版本化

- `docs/regulation_index_v0_2_26050302.md`
- `packages/client/public/admin/admin-card-prompt.js`
- `packages/server/src/db/migrate.ts`
- `packages/server/src/routes/keeper.ts`
- `packages/server/src/routes/locations.ts`
- `packages/shared/src/game/checks.ts`
- `packages/shared/src/game/checks.test.ts`
- `packages/shared/src/game/ruleEngine.ts`
- `packages/shared/src/game/ruleEngine.test.ts`
- `packages/shared/src/game/encounters.ts`
- `packages/shared/src/game/encounters.test.ts`
- `packages/shared/src/game/keeperAI.ts`
- `packages/shared/src/game/keeperAI.test.ts`

### 同步的權威 / 本機規約（不在子專案 Git）

- `C:\Ug\docs\s14_encounter_card_spec_part2.md`：舊 s14 範例換算並加入 R4 裁定。
- `scripts/mod-agent-local/keeper-cards/regulation_mythos.md`：本機 only / `.git/info/exclude` 的 MOD-10 規約，加入同一條 d20 絕對刻度禁令。

## 新增與保留的測試

- `checks.test.ts` 保留原有 d20、混沌袋、法術副作用與優劣勢案例，額外驗證 `10-28` 唯一合法區間、`3 -> 13` 舊資料換算。
- `ruleEngine.test.ts`：舊 `shroud=3` 讀取為 `DC 13`。
- `encounters.test.ts`：舊 `check_dc=4` 結算為 `DC 14`。
- `keeperAI.test.ts`：正式 `release_dc=13` 與舊 `release_dc=3 -> 13` 都回歸。

## 驗證

```text
Full server/shared/client *.test.ts sweep: PASS
checks.test.ts: 19 passed, 0 failed
ruleEngine.test.ts: 96 passed, 0 failed
encounters.test.ts: 18 passed, 0 failed
keeperAI.test.ts: 26 passed, 0 failed
shared tsc: PASS
node scripts/build-shared-runtime.mjs: PASS
server tsc --noEmit: PASS
client tsc --noEmit: PASS
Vite production build: PASS (現有 bundle >500 kB warning only)
node scripts/preflight.js: === ALL PASS — 可推送 ===
git diff --check: PASS
sim-slit-3ai.ts --seed 260713: PASS，雨夜的真相 T12 勝利
```

## 請 reviewer 特別確認

1. `normaliseCheckDc` 僅用在語意上是檢定難度的三條引擎路徑，沒有誤套到怪物 DC、`threat_strength` 或其他小數字機制。
2. `MIGRATION_049` 的 `WHERE 0-9` 是否確保現有 `11-14` 地點與其他已正確的絕對 DC 不會二次換算；CHECK 約束是否允許未配置檢定的遭遇選項為 NULL。
3. server 的三個 ingress（地點、遭遇選項、神話效果）是否都在資料庫寫入前拒絕 `1-9` / 非整數 / 超界值。
4. 原有 `checks.test.ts` 已完整保留，僅加入兩個本包案例，沒有測試覆蓋倒退。
5. 根層 `C:\Ug\docs\s14_encounter_card_spec_part2.md` 與本機 only 的 MOD-10 規約雖不在 commit，是否與 Git 內 prompt/index 同一裁定、且未碰凍結的 `docs/v07_*`。

## 部署後 smoke

1. Railway server 部署完成後，唯讀取得 `雨夜的真相` bootstrap：六個地點仍維持 `11-14`，`ec_library_whispers` 兩個 `check_dc` 為 `14/16`，`mc_madness_grip.release_dc` 為 `13`。
2. 實玩抽到「書架間的低語」，確認結算紀錄顯示絕對難度 `14` 或 `16`，不再是 `4/6`。
3. 以管理帳號在 MOD-14 / 卡片檢查器掃描既有 player-card catalogue，搜尋 `檢定(N)` / `DC N`（N 為一位數）；若有命中，另開內容資料清洗包，不直接在本包手改內容。
4. 後台新建一個 draft 地點或遭遇選項填 `3`，確認 400 且不寫入；填 `13` 則可進入既有草稿流程。測完不保留測試資料。

## 自知風險與範圍外發現

- **未替 Uria 重調難度曲線**：R4 把表示法統一為絕對 d20；地點日後是否要落在候選 `10-16` 的哪個分布，仍是試玩平衡裁定。本包不動關卡/城主平衡資料。
- **玩家卡舊文本遠端掃描待有管理帳號的 MOD-14 執行**：原因與去向已列於 smoke；本包不繞過登入或讀取憑證。
- **s06 玩家可見敘事 validator** 屬 RB-P4b；本包只處理刻度文字，沒有順手重寫其他敘事。
- **384 場 baseline** 屬 RB-P4d；固定種子 smoke 僅確認引擎未崩且可通關，不代替統計 baseline。
- 不修改 `docs/v07_當前版本_26042606/`、關卡資料、城主配置、玩家卡庫或量產 driver 程式；migration 是部署時唯一資料校正入口。

## Review

待 HUB Claude review。
