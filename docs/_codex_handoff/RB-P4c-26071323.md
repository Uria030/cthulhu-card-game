你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P4c 規則書逐條對照 v2 - Handoff

## 變更摘要

- 依 `docs/工作單_路線圖六步_26071301.md` 的 RB-P4c,以規則書 v1.0、h01-h10、s01-s13 對照現行 shared/server runtime、可重跑測試與近期 RB/MP handoff。
- 新增 `docs/核心玩法檢查清單_引擎對照_v2_26071323.md`,以 PASS / PARTIAL / DEBT / CONTENT 區分「引擎已驗證」與「內容或未接 runtime」,不再沿用 26061301 的舊完成度。
- 將 26061301 舊清單標記為歷史快照,指向 v2,避免後續派工以過期缺口判斷現況。
- 對所有非 PASS 項目落入 D-01 至 D-18 的具名債項。新增且保守保留 D-17 個人秘密任務與 D-18 混沌標記場景後果,不因 state/schema 或抽袋本身存在就宣稱全兌現。

## 範圍與禁碰區

- 本步是文件稽核,沒有修改遊戲規則、shared/server/client runtime、DB schema、測試、卡片或 encounter 資料。
- 未修改 `docs/v07_當前版本_26042606/`、規則書原文或凍結資料。
- 本步不宣稱卡庫、遭遇卡庫、怪物家族或敘事內容已驗收;這些均保留為 D-04、D-05、D-12、D-13、D-16。

## 動過檔案

1. `docs/核心玩法檢查清單_引擎對照_v2_26071323.md` (新增): 現行規則書與引擎對照、D-01 至 D-18 正式債。
2. `docs/核心玩法檢查清單_引擎對照_26061301.md`: 頂端加註已由 v2 取代,保留歷史用途。

## 驗證結果

本步沒有受影響的 runtime package 或測試檔。依 handoff Gate,仍複跑 client TypeScript 與全域 preflight。

```text
Coverage: h01-h10, s01-s13, D-01-D-18 all present.
```

```text
> packages/client: npx.cmd tsc --noEmit

(exit 0; no output)
```

```text
> node scripts/preflight.js

=== ✓ ALL PASS — 可推送 ===
```

preflight 另有既有非阻擋警告: `cast_spell`, `declare_stratagem`, `flip_card`, `forge_item`, `craft_item`, `read_book`, `research_relic` 沒有顯式 VALUE_TABLE 值。本文件已列為 D-16,沒有忽略或以 PASS 掩蓋。

```text
> git diff --check

(exit 0; only Git LF/CRLF informational warning for the historical markdown file)
```

## 自知風險與範圍外發現

- 本表的 PASS 是「已有執行管線與測試證據」,不是資料庫內容品質、整體平衡或 UI 實玩驗收的結論。
- h05 曾在初稿誤作 PASS,校讀後因 `ruleEngine` 仍有未特化混沌標記回報待接通敘事,已改為 PARTIAL 並納入 D-18。
- 判定基於工作區現行原始碼與近期 handoff。RB-P4d 的 384 場統計基線尚未進行,已保留為 D-15。
- v2 取代舊清單是文件導覽更新,不刪除歷史記錄;若未來規則或 runtime 變動,需再更新 v2 或新增後繼版,不可反向修改規則書證據。

## 請 Reviewer 特別確認

1. h01-h10、s01-s13 與規則書六章是否全有明確狀態,且每個 PARTIAL/DEBT/CONTENT 都可回溯至 D-01 至 D-18。
2. D-17、D-18 是否維持保守且正確,沒有將 schema/state 或部分效果誤判為完整 runtime。
3. 文件是否把「引擎 PASS」與「資料庫內容完成」清楚分開,尤其是 MOD-10 遭遇、全卡庫 s06 與雙軸內容稽核。
4. diff 是否僅限 RB-P4c 文件工作,並且沒有觸碰 v07 規則原文或凍結資料。

## Review

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13;純文件稽核包)

依代理 review checklist:

1. **commit/清單**:`9955591` 單筆,僅 2 份文件(v2 新增+舊清單加取代註);零 runtime/schema/測試/內容變更;`docs/v07_*` 與凍結資料零觸碰(diff 驗證)。
2. **複跑**:client tsc exit 0、preflight ALL PASS(既有 7 個 V 值警告已如實列為 D-16,未掩蓋)。
3. **特別確認事項逐項回覆**:
   - **確認 1(覆蓋完整性)— 成立(抽驗)**:規則書六章分四節對照、h01-h10 與 s01-s13 逐份有狀態;**D-01 至 D-18 十八項全數出現**(grep 驗證);每個 PARTIAL/DEBT/CONTENT 都standing帶具名債項去向,PASS 項附程式檔+測試數證據。
   - **確認 2(D-17/D-18 保守性)— 成立**:D-17 明言「state 欄位存在但無 runner」、D-18 明言「抽袋已通但未特化標記只回報待接通」——都是「schema/部分效果 ≠ 完整 runtime」的正確判定;h05 從初稿 PASS 自我校正為 PARTIAL 的過程也如實記載,可信度加分。
   - **確認 3(引擎 PASS vs 內容完成分離)— 成立且是本表最大價值**:判讀方法表開宗明義「PASS 不代表每張內容卡完成」;CONTENT 獨立狀態(s01/s02/s07 等);D-04/05/12/13/16 把 MOD-10 遭遇、全卡庫 s06、雙軸稽核明確劃給內容線——與我歷輪 review 的一貫判定(引擎就緒/內容空殼分開)完全一致。
   - **確認 4(diff 範圍)— 驗證成立**:僅兩份 markdown,舊清單頂端加註不刪歷史。
4. **與歷輪 review 的交叉一致性抽驗**:D-01(23 項效果碼)、D-02(reaction 來源)、D-03(consume 三債)、D-05(7 張空殼+pipeline spec)、D-15(384 場 baseline)與我審過的 RB-P3a2a/b1/b2、RB-P3b2、RB-P1-1c 結論逐項吻合——這張表忠實彙總了整條路線圖的實況,無誇大。
5. **結論第 4 點正確**:最終驗收=Uria 實玩,自動測試不替代產品驗收。

結論:對照表證據紮實、債項保守具名、引擎/內容邊界分明、與歷輪審查全數吻合,PASS。此表可作為後續派工的現況權威;下一步 RB-P4d(D-15 統計基線)。
