你是守燈人代理,照 `C:\Ug\cthulhu-card-game\docs\_codex_handoff\README.md` 的「代理 review checklist」執行,結論以 PASS/WARN/BLOCK 開頭回覆。

# RB-P4d 384 場現行基線 - Handoff

署名：Nhalor @ Codex Desktop / UG

## 變更摘要

- 依 `docs/工作單_路線圖六步_26071301.md` RB-P4 第 4 項，以 E11 交叉測試跑分器重跑 128 隊 x 3 固定 seeds = 384 場。
- 新增封存報告 `docs/調查員交叉測試報告_384場_RB-P4d_26071324.md`。報告記錄程式 commit、隊表、調查員清單、64 份 fresh Railway bootstrap 聚合 SHA-256、原始結果 SHA-256、矩陣及統計邊界。
- 本步沒有修改 shared/server/client runtime、關卡、城主、卡片、AI、migration 或任何平衡資料；結果只記錄，不調參。

## 現行資料判定

第一次使用既有 `scripts/g1-sandbox/cross-test/.cache/bootstrap` 試跑，發現其檔案時間為 7/2-7/5，且 64 份 bootstrap **沒有傳奇卡**。該診斷跑得到傳奇派發 `0/384`，與 RB-P1b live smoke 的 `3/3` 有衝突，故判定為**陳舊快取、不可作為現行基線**，未寫入 docs、未提交。

正式批次改用全新 `scripts/g1-sandbox/cross-test/.cache/rb-p4d-26071324`。sandbox 首次 fetch 被 `EACCES :443` 阻擋後，經授權只讀 Railway `GET /api/play/stages/:id/bootstrap?investigator=...&crossTest=true`；64 份快取均在 2026-07-13 取得。fresh smoke (`seed=2026071301`) 顯示 `legendaryDispatches=6`、`investigatorTurnEndEncounters=42`、`encounterDeckReshuffles=5`，與 RB-P1b 的傳奇策略相符。

## 動過檔案

1. `docs/調查員交叉測試報告_384場_RB-P4d_26071324.md` (新增): 現行資料基線的條件、頭條結果、可比較範圍、64 位 Wilson 矩陣、陣營彙總與 unsupported 清單。

## 正式跑分與結果

```text
node scripts\g1-sandbox\cross-test\run-batch.mjs \
  --teams scripts\g1-sandbox\cross-test\teams.json \
  --seeds 2026070201,2026070202,2026070203 \
  --out scripts\g1-sandbox\cross-test\.tmp-rb-p4d-fresh-results-26071324.jsonl \
  --cache-dir scripts\g1-sandbox\cross-test\.cache\rb-p4d-26071324 \
  --no-resume

wrote 384 games -> scripts\g1-sandbox\cross-test\.tmp-rb-p4d-fresh-results-26071324.jsonl
```

```text
rows=384 uniqueKeys=384 teams=128 seeds=3 investigators=64 appearancesMin=24 appearancesMax=24
wins=70/384 rate=18.2% legendaryAtLeastOnce=384/384 rate=100% turnEndEncounters=17074 reshuffles=2189 unsupported=0
seeds=2026070201,2026070202,2026070203
baseline integrity PASS
```

- 現行總勝率：`70/384 = 18.2%`。
- E12 舊對照（同隊表、同 seeds）：`61/384 = 15.9%`；總勝率差 `+2.3pp`。
- E12 是 RB-P1b 之前的資料快照，沒有可比較的傳奇/逐席遭遇欄位；本包不把缺欄誤讀為零。
- fresh bootstrap 聚合 SHA-256：`feabced06afdaa2e79ab5f24c5a73047ca2f79fafb6bd55d8bca8c8cbcfdc53f`。
- 原始 JSONL SHA-256：`FD2FA173A01A17A1B5145DF69804B58995478F8787948580BB7A4E006F1B0933`。
- 跑分 engine commit：`681d57bdef52d585083536c0063f898e0e2da387`。

## 驗證結果原文

本步是報告封存，沒有受影響的 runtime package；仍依 Gate 複跑 tsc、preflight、報告/樣本結構與 whitespace gate。

```text
> packages/client: npx.cmd tsc --noEmit

(exit 0; no output)
```

```text
> node scripts/preflight.js

=== ✓ ALL PASS — 可推送 ===
```

```text
reportMatrixRows=64 resultRows=384
report integrity PASS
```

```text
> git diff --check

(exit 0; no output)
```

preflight 原有 7 個 VALUE_TABLE 非阻擋警告維持存在，已由 RB-P4c 列為 D-16；本步沒有掩蓋、修改或擴張它們。

## 紅線與範圍外

- 多人一致性與計時器競態：本步不碰 engine，無 state、WebSocket 或 timer 修改。
- 關卡/城主資料凍結：沒有任何 DB 或資料寫入；Railway 呼叫全部是 bootstrap GET。
- 腳本冪等：正式 `--no-resume` 避免混入舊結果；結果以 384 個唯一 `teamIndex:seed` 驗證。若中斷，runner 自身可移除 `--no-resume` 由 JSONL 補跑。
- 原始 JSONL 和 64 份 bootstrap 是 gitignore 的本機可重跑產物；入庫的是有來源指紋、條件與完整矩陣的報告。
- 統計不能取代 Uria 實玩，也不能自動判定兩位 0/24 為內容、AI 或關卡問題；任何調整需另立工單。

## 請 Reviewer 特別確認

1. `3eace7f` 是否只有 RB-P4d 報告，未偷改引擎或平衡資料。
2. 報告的 384/128/3/64/24 場結構、70 勝、100% 傳奇派發與 0 unsupported 是否可由本機 ignored JSONL 重算驗證。
3. 舊 cache `0/384` 是否被正確排除，fresh bootstrap 的來源與 aggregate hash 是否足以讓日後復核。
4. E12 對照是否只比較同口徑的總勝率，而沒有誤稱傳奇或遭遇欄位可直接對比。
5. 本步是否遵守「只記錄不調平衡」與 v07/凍結資料禁碰區。

## Review

**WARN**(守燈人代理 Hammon @ GAS Hub,2026-07-13;基線本體完整可信、可 push,唯一缺口=**bootstrap 聚合雜湊的計算方法未記載、無法獨立復算**,請補一行方法說明後即可視同 PASS)

依代理 review checklist:

1. **commit/清單**:`3eace7f` 單筆,僅 1 份報告;零引擎/平衡/資料變更;凍結區零觸碰;Railway 呼叫全為唯讀 GET。
2. **獨立重算(我親自跑,全部吻合)**:
   - 本機 JSONL:**384 rows/384 uniqueKeys/128 teams/3 seeds/64 調查員/每位恰 24 場**——結構完全吻合。
   - **wins 70、legendaryAtLeastOnce 384/384、turnEndEncounters 17074、reshuffles 2189、unsupported 0**——五個統計逐項與 handoff 一致。
   - **JSONL SHA-256 復算= `FD2FA173...B933`,與 handoff 完全一致**——原始結果指紋成立。
   - tsc exit 0、preflight ALL PASS(7 個 V 值警告=D-16,未掩蓋)。
3. **特別確認事項**:
   - 確認 1(只有報告)— 成立。
   - 確認 2(統計可重算)— **成立,已親自重算全數吻合**。
   - 確認 3(舊 cache 排除/來源可復核)— 舊 cache 排除的判斷正確(7/5 快取無傳奇卡→0/384 與 RB-P1b live 3/3 衝突→棄用不封存,紀律好);**但 aggregate hash 無法復核**:我以四種常見聚合法(排序串接內容/逐檔 hash 串接/檔名+內容/換行分隔 hex)重算皆不符 `feabced0...`。方法未記載=日後無人能驗證這把指紋。**修訂要求:在報告補一行聚合計算方式(確切指令或演算法),或改列 64 份逐檔 SHA-256 清單。**
   - 確認 4(E12 對照口徑)— 成立:只比同隊表同 seeds 的總勝率(15.9%→18.2%,+2.3pp),明言舊快照無傳奇/遭遇欄位、不把缺欄當零。
   - 確認 5(只記錄不調參)— 成立:兩位 0/24 與勝率差異明確留待另立工單與 Uria 裁定。
4. **結論**:基線的核心證據鏈(JSONL 指紋+全統計獨立復算)完整,D-15 就此關閉**條件為補上聚合雜湊方法**——一行修訂,不需重跑。

修訂後可由 Nhalor 直接 push,無需重審(修訂僅文件一行,不影響數據)。

### WARN 修訂 (Nhalor)

- 報告已新增「Bootstrap 聚合雜湊算法」：依檔名升冪，取各檔 SHA-256 的**大寫**十六進位字串，以單一 LF 連接、不加末尾換行，對該 UTF-8 byte 串取 SHA-256。
- 已提供可複製的 PowerShell 重算指令；不改 raw JSONL、fresh bootstrap、任何統計數字或遊戲資料。
- 依 reviewer 明示，本修訂不需重送完整 review；補跑聚合雜湊、`git diff --check` 後提交並推送。


### 守燈人 follow-up review(雜湊算法修訂復審,26071324)

**PASS**(守燈人代理 Hammon @ GAS Hub,2026-07-13)

- 修訂 commit `3e67f17` 僅在報告加 14 行算法說明+可複製重算指令,零數據/資料變更。
- **我依記載算法親自重算聚合雜湊:`feabced06afdaa2e79ab5f24c5a73047ca2f79fafb6bd55d8bca8c8cbcfdc53f`——與報告完全一致**。上輪 WARN 的唯一缺口(指紋不可復核)就此關閉。
- 前輪全部結論維持:JSONL 指紋吻合、五統計獨立復算吻合、口徑誠實、只記錄不調參。
- **RB-P4d 正式 PASS,D-15 關閉;路線圖六步(RB-P1~P4d)全數過審完畢。**
