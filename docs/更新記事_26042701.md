# 更新記事 26042701 — G1 沙盒關卡資料層完成

- **Session 期間**:2026-04-26 21:00 ~ 2026-04-27 04:00(7 小時 single session)
- **版本範圍**:v0.20.13(本 session 起點) → v0.20.16(本 session 終點)
- **Commit chain**:
  - `b97e3dd`(g1-prep)— 加複製 token 按鈕 + G1 工具骨架
  - `826e921`(g1-prep)— 修 combat-styles.ts 18 處 SQL bug
  - `f7962fb`(merged)— Uria 合併 g1-prep
  - `d27163f`(g1-prep-2)— 修 monsters.ts variant POST bug
  - `14ef92d`(merged)— Uria 合併 g1-prep-2
  - **`g1-prep-3`(尚未合併,等 Uria 點網址)**— G1 完整資料層 + 沙盒模式骨架
- **交付報告**:`c:\Ug\docs\GamePlan\G1沙盒關卡交付報告_26042701.md`(完整 §0-§10)
- **下次 session 第一件事**:
  1. 確認 `g1-prep-3` 是否已合併(若沒合併,給 Uria 網址 `github.com/Uria030/cthulhu-card-game/pull/new/g1-prep-3`)
  2. 看 Uria 校準回饋 + 指示優先級
  3. 優先做前端 admin-scenario-editor.html 沙盒 toggle UI(後端已就緒)

---

## §1 本 session 解決什麼

### 1.1 動工前盤點 + 7 校準點審議
- Uria 拍板:鐵證調查員選 ISTP 四字碼;G1_ 前綴獨立新建沙盒關卡(不掛入既有印斯茅斯陰影戰役 seed);v0.18.x 落地的 MIGRATION 已到 026,本次 027
- claude.ai 撰寫者透過 /remote-control 確認所有校準點

### 1.2 取 admin token 流程踩坑(三振警告)
- 我連續嘗試 4 種失敗方案(! 互動 bash / node prompt / env var inline / .bat 雙擊)→ Uria 警告「三振出局」
- Uria 自己提出**建 cred.txt 檔讓 Claude 自動讀**最簡解(白話路徑)
- 結果:owner token 24h,寫入 `.g1-token`(已 .gitignore)

### 1.3 G1 資料層 60+ 件零件全部建立(對齊 Part 1+2+3)

| 類別 | 數量 | code 前綴 |
|---|---|---|
| 地點 | 3 | `G1_alley_entrance` / `G1_slimy_brick_wall` / `G1_deep_one_haunt` |
| 隱藏調查點 | 1 | 印斯茅斯印記(掛濕滑磚牆) |
| 怪物 species 新建 | 1 | `g1_street_thug` 街頭暴徒(family=fallen) |
| 怪物 variant | 3 | `G1_deep_one_slit_mouth` / `G1_deep_one_revenant` / `G1_street_thug_basic` |
| 怪物招式卡 | 6 | `G1_atk_*`(3 裂嘴女 + 2 亡靈 + 1 流氓) |
| 調查員 | 1 | `G1_iron_witness_detective`(無名鐵證偵探,ISTP)is_completed=true |
| 簽名卡 | 2 | 鐵證之眼 / .45 銀色子彈 |
| 弱點 | 1 | 無法忘記的事 |
| 起始牌組 | 18 張 | 2 簽名 + 1 弱點 + 15 普通 |
| 玩家卡 | 30 | series=G,faction=S,涵蓋 §4.1-§4.8 八類 |
| 戰鬥風格卡 | 10 | shooting × 5 + brawl × 5 |
| 神話卡 | 10 | `G1_myth_*` |
| 遭遇卡 | 5 | `G1_enc_*` |
| Stage | 1 | `g1_sandbox_innsmouth`(side type)綁定 神話 10 / 遭遇 5 / 怪物家族 2 |

**重要 ID(下次 session 直接拿)**:
- 鐵證調查員 id:`70de5729-d553-4667-b6d6-25265d56b9c8`
- Stage id:`f06c85a4-34cd-435b-8fd0-4ebd1cf03533`
- 裂嘴女 variant:`4ae67d50-d6ac-4f76-b41c-ca4a8cd69fcb`
- 亡靈 variant:`a0ed00e3-7a49-4f26-8d96-ae44f583f79f`
- 流氓 variant:`73c860d1-19e2-4d2f-adb0-3559ece4fdaa`
- 街頭暴徒 species:`20840aa1-8c40-4149-b678-351639b7d969`
- 暗巷入口:`efc30667-31d6-46f4-9751-2271d0c51503`
- 濕滑磚牆:`aebb48d1-3b10-4747-ba47-e544103f848b`
- 深潛者出沒處:`e1df9371-4295-4314-a742-17a50220efa7`

### 1.4 MIGRATION_027:沙盒模式 schema 落地
- `stages` 加 `is_sandbox BOOLEAN DEFAULT FALSE` + `sandbox_config_id UUID`(FK 到 sandbox_configs)
- 新建 `sandbox_configs` 表(JSONB 5 個 pool 欄位 + notes + UNIQUE(stage_id, config_name))

### 1.5 後端 routes/sandbox-configs.ts(7 endpoint)
```
GET    /api/sandbox-configs              # 列表(支援 stage_id 篩選)
GET    /api/sandbox-configs/:id          # 單筆
POST   /api/sandbox-configs              # 新建
PUT    /api/sandbox-configs/:id          # 更新
DELETE /api/sandbox-configs/:id          # 刪除(admin/owner)
POST   /api/sandbox-configs/:id/load     # 設為 stage.sandbox_config_id
PUT    /api/stages/:id/sandbox-mode      # 切換 is_sandbox
```

### 1.6 兩個 production bug 順手修(從 MIGRATION 落地都壞著沒人發現)

**combat-styles.ts**(commit 826e921):18 處 SQL 用 `combat_style_id` 但 DB 真實欄位 `style_id`,且表名 `specializations` 真實是 `combat_specializations`。整個 MOD-05 GET / POST / PUT 從 MIGRATION_007 落地以來都 500。

**monsters.ts**(commit d27163f):L626 auto-create 5 default `monster_status_descriptions` 時插 `description_zh=null`,但 schema TEXT NOT NULL。POST variant 從 MIGRATION_010 落地以來永遠 ROLLBACK。修法改 `'（待設計）'` 占位字串。

### 1.7 4 條新天條寫進記憶

1. **`feedback_no_user_commands.md`**:禁止叫使用者跑 ! 互動指令(三振紀錄)
2. **`feedback_simplest_path_first.md`**:遇 blocker 先列 2-4 條路徑討論,不要自選方案瞎忙
3. **`feedback_branch_merge_workflow.md`**:推 main 被擋走分支合併,直接給 GitHub PR 網址不要叫使用者找黃色橫條
4. **`feedback_idempotent_scripts.md`**:動工腳本必須冪等,409 conflict 視為「已存在續做」不是失敗

### 1.8 動工腳本 9 支(scripts/g1-sandbox/,全部冪等)

| 檔 | 功能 |
|---|---|
| `api.mjs` | adminFetch / callGemini / 讀 .g1-token 封裝 |
| `read-cred-and-login.mjs` | 從 cred.txt 取帳密 → 登入 → 寫 token → 自動刪 cred |
| `01-inventory.mjs` | 動工前盤點所有 admin entity |
| `02-create-locations.mjs` | 3 地點 + 1 隱藏調查點 |
| `03-create-monsters.mjs` | 1 species + 3 variants + 6 attack cards |
| `04-create-investigator.mjs` | 鐵證骨架(POST + 屬性 PATCH) |
| `05-create-cards.mjs` | 30 玩家卡 |
| `06-investigator-deck.mjs` | 簽名 2 + 弱點 1 + 牌組 15 普通 |
| `07-create-style-cards.mjs` | 10 風格卡 |
| `08-mythos-encounter.mjs` | 10 神話 + 5 遭遇 |
| `09-bind-stage-pools.mjs` | stage 綁定神話池/遭遇池/怪物池 |

**廢用工具(留著當教訓)**:
- `00-login.mjs` env-var 路徑(被 cred.txt 取代)
- `get-token.bat` / `.ps1` 雙擊路徑(Windows SmartScreen 直接關掉)
- `push.bat` 雙擊路徑(改用分支合併網址)

---

## §2 細節要去哪看

| 想知道什麼 | 去哪 |
|---|---|
| 完整交付規格與校準清單 | `c:\Ug\docs\GamePlan\G1沙盒關卡交付報告_26042701.md` §1-§10 |
| 9 條不確定處等校準 | 交付報告 §4 |
| 規則書衝突 / 模糊處 | 交付報告 §5 |
| 下次 session 優先順序建議 | 交付報告 §7 |
| Uria 校準入口(進 admin 哪裡看) | 交付報告 §0 + §8 |
| G1 開工原始文件(Part 1/2/3) | `c:\Ug\docs\G1沙盒關卡執行交付書_Part1/2/3_v0.1_26042607.md` |
| 動工腳本(可重跑) | `cthulhu-card-game/scripts/g1-sandbox/*.mjs` |
| 工作日誌(每支腳本一份) | `cthulhu-card-game/logs/claude-code/g1-*.log`(已 .gitignore) |

---

## §3 待辦(下次 session 接續)

### P0(立刻做)

1. **確認 g1-prep-3 已合併**:
   - GET `https://server-production-fc4f.up.railway.app/health`,看 `tables` 是否含 `sandbox_configs`(若有 = MIGRATION_027 跑過了)
   - 沒有 → 重給 Uria 網址 `github.com/Uria030/cthulhu-card-game/pull/new/g1-prep-3`

2. **看 Uria 校準回饋**:
   - 哪些零件大改寫(我重做)
   - 哪些零件局部調整(我提供 PATCH 腳本)
   - 哪些 Uria 自己在 admin UI 改(我等他完成)

3. **優先做前端 admin-scenario-editor.html 沙盒 toggle UI**:
   - 後端 7 endpoint 已就緒,缺 UI 入口
   - 在概覽分頁加 toggle 切換 is_sandbox + 顯示當前 sandbox_config
   - 設計參考:Part 1 §3.2 UI 概念圖

### P1(短期)

4. **Stage 內配置補齊**:
   - scenarios 表(三地點連接 + 初始光照 + 起始位置)
   - stage_act_cards(目標牌堆「擊敗裂嘴女 + 揭露印記」,7 回合議程上限)
   - stage_agenda_cards(失敗條件)

5. **校準項處理**(交付報告 §4):
   - flag schema 擴充(reward_type 加 'flag' / reveal_condition 加 'attribute_check')
   - 城主獨立 entity(若 Uria 認為有需要)
   - 怪物招式 species → variant 遷移(若精確比共用重要)
   - 風格卡 DC / 傷害修正欄位(若 schema 該存)

6. **`main_attr_is` 函式對齊 s05 v0.2**(若 Uria 不滿意 ISTP 預設智力為主):需 MIGRATION 改 DB 函式

### P2(等 G1 引擎建立)

7. 對話泡泡引擎(裂嘴女三段式)
8. 沙盒模式引擎讀取分流邏輯
9. 視野光照判定(shroud → 黑暗影響檢定)

---

## §4 重要記憶 / 紀律(本 session 強化)

### 4.1 Uria 工作環境鐵律(避免再三振)

- **iPad 遠端控電腦** + 非資工背景 = 操作粒度上限只到「滑鼠雙擊 / 點按鈕 / GUI 表單填字 / 複製貼上」
- 禁止叫使用者跑 ! / bash / curl / node 任何 CLI
- Push 流程改走分支合併,直接給 PR 網址(不叫他找黃色橫條,黃條會過期)
- 雙擊 .bat 不可靠(Windows SmartScreen 會擋),改 cred.txt 讓 Claude 自動讀

### 4.2 工作節奏紀律

- 遇 blocker 先列 2-4 條路徑(含「最簡情境」如建記事本檔)讓 Uria 選
- 失敗連續換方案 = 瞎忙 = 三振
- 動工腳本必須冪等(GET 既有 → 409 視為「已存在續做」非失敗)

### 4.3 Production bug 修復流程(本 session 兩次踩到)

- combat-styles + monsters 都是 routes 用 `|| null` 但 schema NOT NULL,從 MIGRATION 落地以來壞著沒人發現
- 修復路徑:routes 改占位值 + bump 版本 + commit + 推 feature branch + 給 Uria 合併網址
- **教訓**:後續若再遇「endpoint 從沒成功過」的 production bug,優先檢查 schema NOT NULL vs routes default 是否一致

---

## §5 小結

G1 沙盒關卡資料層完整落地,Uria 早上(2026-04-27)起來可進 admin 後台抽查校準。沙盒模式工程後端 100% / 前端 0% / 引擎 0%,後端 API 已就緒等前端 UI 串。動工腳本全部冪等,Uria 校準後若需要批次重做某類零件,我可以快速重跑。

兩個 production bug 順手修了(combat-styles + monsters,從 MIGRATION 落地以來都壞著),整個 MOD-05 + MOD-03 variant POST 從本 session 起恢復可用。

下次 session 開機:讀本記事 §3 接續。
