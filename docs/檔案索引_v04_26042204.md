# UG 專案檔案索引 v04

- **凍結日期**：2026-04-22（日期碼 26042204）
- **版次**：v04
- **舊版**：`docs/old/檔案索引_v01_26041801.md`、`docs/檔案索引_v02_26041903.md`、`docs/檔案索引_v03_26042202.md`（已 rename 至本檔）
- **範圍**：排除 MOD-12（使用者指示）。gemma-bridge 屬 MOD-12 基礎設施，亦排除
- **格式**：每項僅列「名稱 + 行號 + 一句用途」
- **目的**：下次 session 開機必讀，取代從 Glob 摸起的「第一天上班」狀態
- **維護**：發現檔案新增/刪除/大幅重構時更新版次（v02, v03, v04…）

### v04 變更摘要（相對 v03，支柱一 v0.3 落地）
- **新增 rulebook s07**：`packages/client/public/rulebook/s07_faction_narrative.md`（951 行，支柱一 v0.3 八陣營詳細敘述 + 違和即資產哲學 + §12.4 先攻勘誤）；`rulebook/index.json` 升 v04
- **E/S 戰鬥風格互換**（MIGRATION_020 落地）：
  - `migrate.ts` L580-594 `talent_trees` seed 修訂 E 號令 primary military→sidearm、I 深淵 primary arcane→assassin、P 流影 primary assassin→archery
  - 新增 `MIGRATION_020_SQL`（migrate.ts `MOD-06 示範戰役種子` 段之前）條件式 UPDATE `talent_trees` 既有資料，僅在 primary/secondary 均為舊預設值時才更新，保留設計者手動修改
- **先攻勘誤全面清除**（支柱一 v0.3 §12.4）：
  - 規則書第一章 §7.6 反應屬性影響範圍刪「先攻、」；底下關於「先攻判定由反應決定」的整段註記刪除
  - 規則書第六章 §2.1 反應屬性影響範圍刪「先攻、」
  - rulebook s06 升 v0.3：刪 §3.3「先攻」術語行、§2.2 註記改寫、§6.3 驗證清單刪先攻項、§8.2 範例不以先攻舉例
  - 支柱五 v0.2（c:\Ug\docs\）§4 E/S 戰鬥熟練互換
  - 工單檔加 DEPRECATED 聲明（c:\Ug\docs\工單_先攻效果改由反應觸發_26042201.md）
- **索引升 v04**：規則書索引 / 檔案索引各由 v03 rename 為 v04（git mv 保留歷史連續性）

### v03 變更摘要（相對 v02，保留歷史紀錄）
- **支柱一 v0.2 八屬性化落地**（ADMIN_VERSION 0.14.0+b26、root package.json 0.11.0）
  - `packages/shared/src/constants/attributes.ts` 新增 `reflex`，並為全部屬性補 `category: 'physical'|'mental'`
  - `admin-shared.js`:`CREATION_FREE_POINTS: 4`、`ATTRIBUTE_COUNT: 8`、`FACTION_ATTRIBUTE_MAP` 全部 `isShared:false`，T→agility、P→reflex
  - `MIGRATION_019_SQL`（migrate.ts L2487 後）：investigator_templates 加 `attr_reflex`、faction_attribute_map UPDATE T/P、骨架重算、`chk_inv_total_points` 改八屬性版（NOT VALID）
  - `investigators.ts`:`ATTR_KEYS` 含 `attr_reflex`；create/clear/clone 路徑加 reflex 參數
  - MOD-01 卡片設計器 commit icons stepper、attribute_modifiers multi 模式新增反應欄；`validKeys` 含 reflex
  - MOD-11 調查員設計器 `ATTR_META` 增反應列；freePoints fallback 5→4、基礎總計 13→14
  - SIM-03 創角模擬器 `LETTER_TO_ATTR/ZH` T/P 重寫；`TOTAL_FREE` 5→4；4 組 PRESETS 補 reflex
  - 規則書 v1.0 第一章 §7.6、第三章 §3.1、第六章 §2.1-2.2 改八屬性（含反應）；創角總點數 21→18
- **新增 rulebook s06 專項規範**：`packages/client/public/rulebook/s06_card_text_style.md`（1,464 行，卡片敘述文法規範 v0.2，三 Part 合併）；`index.json` 版本升 v03
- **本 session 無新增後端 routes、migration 僅 +1（019）**

### v02 變更摘要（相對 v01，保留歷史紀錄）
- MOD-06 / MOD-07 從「尚未建置」改為 READY；後端 routes 12→14（+campaigns +stages）；新增 helper/services/seeds/migrations 017/018、types/scenario.ts、三份 admin 資料檔

---

## §0 專案總覽

### 技術堆疊
- Monorepo：pnpm workspaces
- `packages/shared` — 共用 TypeScript 型別與常數
- `packages/server` — Fastify + TypeScript 後端（port **3001**）
- `packages/client` — React + Vite 前端（port **5173**）；admin 後台為單檔 HTML+inline JS
- `gemma-bridge` — 外部 AI 橋接（port **8787**，屬 MOD-12，本索引排除）
- Ollama — 本地 LLM（port **11434**）
- DB：PostgreSQL（`migrate.ts` 內嵌 DDL 管理）

### 啟動指令
- `pnpm dev` — 同時啟動前後端
- `pnpm dev:client` / `pnpm dev:server` — 分別啟動

### 後端啟動鏈
- `packages/server/src/index.ts` L7 `start()` — `runMigrations()` → `createDefaultAdmin()` → `buildApp()` → listen
- `packages/server/src/app.ts` L17 `buildApp()` — 註冊 cors + 14 個 route plugin

### MOD → 檔案對應總表

| MOD | 名稱 | 前端 HTML | 後端 routes | 狀態 |
|---|---|---|---|---|
| MOD-01 | 卡片設計器 | `admin-card-designer.html` | `cards.ts` | READY |
| MOD-02 | 天賦樹設計器 | `admin-talent-skill.html` | `talent-trees.ts` | READY |
| MOD-03 | 敵人設計器 | `admin-enemy-designer.html` | `monsters.ts` | READY |
| MOD-04 | 團隊精神 | `admin-team-spirit.html` | `team-spirits.ts` | READY |
| MOD-05 | 戰鬥風格 | `admin-proficiency.html`（舊檔名） | `combat-styles.ts` | READY |
| MOD-06 | 戰役敘事 | `admin-campaign.html`（2288 行） | `campaigns.ts`（1200 行） | READY |
| MOD-07 | 關卡編輯器 | `admin-scenario-editor.html`（3997 行） | `stages.ts`（1691 行） | READY |
| MOD-08 | 地點設計器 | `admin-location-designer.html` | `locations.ts` | READY |
| MOD-09 | 鍛造製作 | `admin-forge-craft.html` | `forge-craft.ts` | READY |
| MOD-10 | 城主設計器 | `admin-keeper-designer.html` | `keeper.ts` | READY |
| MOD-11 | 調查員設計器 | `admin-investigator-designer.html` | `investigators.ts` | READY |
| MOD-12 | AI 主控台 | （排除） | （排除） | 排除 |
| 輔助 | 混沌袋 | `admin-chaos-bag.html`（stub 34 行） | 無 | 未使用 |

---

## §1 MOD-01 卡片設計器

### 前端 `packages/client/public/admin/admin-card-designer.html`（2809 行）
單檔 HTML + inline JS。AI 管道完全內嵌於此檔。

**CRUD 與清單**
- `loadCards()` L1084 — `GET /api/cards` 拉全列表
- `mapCardFromApi(c)` L1097 — API 回傳→前端資料物件
- `renderList()` L1151 — 渲染左側卡片列表
- `loadCard(id)` L1188 — 載入單張卡到表單
- `collectForm()` L1255 — 表單→資料物件
- `validate(data)` L1320 — 必填欄位檢查
- `saveCard()` L1328 — POST/PUT 存卡
- `saveAsNew()` L1371 — 另存為新卡
- `deleteCard()` L1377 — DELETE 卡片
- `newCard()` L1396 — 清空表單準備新增
- `clearForm()` L1470 — 表單重置
- `generateCode(series, faction, style)` L1118 — 本地預估卡片代碼
- `updateCodePreview()` L1131 — 呼叫 `/api/cards/next-code`
- `exportJSON()` / `importJSON()` / `handleImport(e)` L2275 / L2284 / L2285 — 匯出匯入

**效果系統**
- `renderEffects(effects)` L1533 — 效果列表 DOM
- `effectHTML(e, i)` L1548 — 單個效果 HTML
- `addEffect()` L1735 / `removeEffect(i)` L1741 — 增刪效果
- `collectEffects()` L1743 / `collectSingleEffect(item)` L1747 — 蒐集效果資料
- `collectSingleEffectConditions/CondParams/Cost/Params` L1768/1772/1780/1790 — 蒐集子欄位
- `buildDynamicParamsHTML(effectCode, params)` L1643 — 根據 effect_code 渲染動態欄
- `resolveOptionsRef(ref)` L1676 / `getNestedParam(params, key)` L1687 — 選項資料讀取
- `renderDynamicParams(idx)` L1698 / `onDurationChange(idx)` L1709 / `toggleCondition(btn, idx)` L1720 — 動態效果欄操作
- `loadEffectLanguageOptions()` L1507 — 載入 `data/effect-language-options.json`
- `renderGroupedSelect/renderFlatSelect` L1514/L1525 — 下拉渲染
- `autoGenerateDesc(idx)` L1822 / `generateDescZh(eff)` L1832 / `generateDescEn(eff)` L1935 — 自動生成效果描述
- `getEffectTextZh/En(code, params, targetText)` L1878/L1965 — 效果語言組合

**價值計算（核心）**
- `calcEffectValue(eff)` L841 — 單效果 V 值
- `calcAllEffectsValue()` L847 — 全效果累加 V
- `calcWeaponOutputValue()` L852 — 武器輸出 V
- `calcAllyBaseValue()` L859 — 盟友基礎 V
- `calcBalanceInfo()` L865 — 平衡資訊總計算
- `rarityFromEffectValue(effectValue)` L952 — V 值→稀有度等級
- `updateBalancePanel()` L962 — 更新平衡面板

**三重用途欄位**
- Commit：`getCommitIcons()` L635 / `calculateCommitValue(icons)` L648 / `stepCommitAttr/updateCommitAttrValue/updateCommitTotal/toggleCommitWild/loadCommitIcons` L656–L707
- Consume：`getConsumeEffect()` L747 / `toggleConsumeEffect()` L763 / `onConsumeTypeChange()` L770 / `updateConsumeValue()` L787 / `loadConsumeEffect(enabled, eff)` L825

**升級/變形**
- `switchUpgradeTab(lv)` L1403 / `saveUpgradeLevelSilent/saveUpgradeLevel/clearUpgradeLevel` L1420/1439/1444 / `updateUpgradeTabIndicators()` L1454 / `loadUpgrades(upgrades)` L1462

**輔助欄位**
- `onWeaponTierChange()` L2087 — 武器階層變更
- `switchAttrModMode()` L2097 / `loadAttrModifiers(mods)` L2104 / `collectAttrModifiers()` L2133 — 屬性修正
- `loadSubtypes(arr)` / `collectSubtypes()` L2154/2157 — 子類型
- `toggleConditionalFields()` L2017 — 條件欄位顯示/隱藏

**預覽與 UI**
- `bindLivePreview()` L2164 / `updatePreview()` L2187 — 右側即時預覽
- `toggleLeftPanel/toggleRightPanel/toggleSection(id)` L1991/2001/2008

**AI 生成（前端直接呼叫 Gemini）**
- `showApiKeyDialog()` L2313 / `updateApiKeyStatus()` L2318 — Gemini API Key 管理（存 `localStorage.gemini_api_key`）
- `aiTemplate(type)` L2324 — 6 個快速模板注入 textarea
- `buildGeminiPrompt(userDescription)` L2329 — **內嵌全規則 prompt（300+ 行）**
- `validateAndFixCardData(d)` L2599 — AI 回傳 JSON 驗證與修正
- `aiGenerate()` L2645 — **主流程：直接 fetch gemini-2.5-flash API → fillFormFromData**
- `aiTranslate(zhText)` L2674 — 中譯英通用
- `aiTranslateName()` L2693 — 卡片名中譯英按鈕
- `aiTranslateEffectDesc(idx)` L2702 — 效果描述中譯英
- `fillFormFromData(d)` L2714 — AI JSON→表單自動填入

**utilities**
- `el(id)` L2789 / `intVal(id)` L2790 / `toggle(id, show)` L2791

**初始化**
- `init()` L988 — 啟動時初始化
- `populateSelects()` L998 — 下拉選單初始化

### 後端 `packages/server/src/routes/cards.ts`（337 行）
純 CRUD。**無 AI 端點**。preHandler hook 掛 `requireAuth`（L7）。
- `GET /api/cards` L10 — 列表；過濾 faction/style/type/search/series/combat_style
- `GET /api/cards/export` L43 — JSON 匯出（帶 Content-Disposition header）
- `GET /api/cards/next-code` L61 — 預覽下一個自動編碼
- `GET /api/cards/:id` L88 — 單張卡片（含 effects）
- `POST /api/cards` L102 — 建立（自動產 code + effects transaction）
- `PUT /api/cards/:id` L172 — 更新（刪重建 effects，version+1）
- `DELETE /api/cards/:id` L234 — 刪除
- `POST /api/cards/import` L247 — 批次匯入（old format 自動遷移）
- `insertEffects(client, cardId, effects)` L311 — helper（transaction 內插入 card_effects）

### 資料庫
- `card_definitions` migrate.ts L28（MIGRATION_001）+ ALTER 擴充於 MIGRATION_002~006、014
- `card_effects` migrate.ts L76

### AI 整合模式
前端直接打 `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${localStorage.gemini_api_key}`。後端無 AI 角色。

---

## §2 MOD-02 天賦樹設計器

### 前端 `packages/client/public/admin/admin-talent-skill.html`
- `populateSelects()` L322 — 下拉初始化
- `loadAllTrees()` L340 / `loadTree(factionCode)` L348 / `loadCompareData()` L356 — 資料載入
- `renderFactionList()` L365 / `selectFaction(fc)` L394 — 陣營列表
- `switchTab(tab)` L402 / `toggleSection(id)` L410 / `toggleLeftPanel()` L418 — UI
- `renderTreeEditor()` L424 / `saveTreeInfo()` L444 — 樹總覽
- `renderBranches()` L462 / `toggleBranchEdit(idx)` L497 / `saveBranch(idx)` L502 — 分支
- `calcPositions()` L522 / `renderSvgTree()` L540 / `svgEl(tag, attrs)` L602 / `drawLine(svg, from, to, color)` L608 — SVG 樹狀繪製
- `showTooltip(node, e)` L614 / `hideTooltip()` L628 — 節點 tooltip
- `openNodeEditor(nodeId)` L633 / `closeNodeEditor()` L670 / `markNodeDirty()` L677 — 節點編輯器
- `onNodeTypeChange()` L679 — 節點類型切換
- `renderEffects(effects)` L689 / `addEffect()` L708 / `removeEffect(i)` L714 / `updateEffectTotal()` L720 — 節點效果
- `saveNode()` L726 / `determineNodeStatus()` L764 / `markNodeComplete()` L772 — 節點儲存
- `searchTalentCards()` L786 — 卡片搜尋
- `renderCompareTab(data)` L800 — 陣營對照
- `getGeminiKey()` L859 / `promptApiKey()` L860 — API Key
- `openAiNodeDialog()` L865 / `openAiBranchDialog(branchIdx)` L879 / `closeAiDialog()` L892 — AI dialog
- `runAiGenerate()` L894 / `buildNodePrompt()` L934 / `buildBranchPrompt()` L967 — AI 生成
- `applyAiNodeResult(data)` L997 / `applyAiBranchResult(data)` L1007 — 套用 AI 結果
- `exportJSON()` L1043 / `importJSON(event)` L1055 — 匯入匯出
- `aiTranslate(srcId, dstId)` L1071 — 中譯英
- `navigateNode(key)` L1112 — 鍵盤導覽

### 後端 `packages/server/src/routes/talent-trees.ts`
- `GET /api/talent-trees/export` L13 — 全部匯出
- `POST /api/talent-trees/import` L42 — 批次匯入
- `GET /api/talent-trees/compare` L144 — 陣營對照資料
- `GET /api/talent-trees` L222 — 全部列表
- `GET /api/talent-trees/:factionCode` L269 — 單陣營樹
- `PUT /api/talent-trees/:factionCode` L308 — 更新樹資訊
- `GET /api/talent-trees/:factionCode/stats` L342 — 統計
- `GET /api/talent-trees/:factionCode/branches` L381 — 分支列表
- `PUT /api/talent-trees/:factionCode/branches/:index` L398 — 更新分支
- `GET /api/talent-trees/:factionCode/nodes` L434 — 節點列表
- `GET /api/talent-trees/:factionCode/nodes/:nodeId` L451 — 單節點
- `PUT /api/talent-trees/:factionCode/nodes/:nodeId` L476 — 更新節點
- `POST /api/talent-trees/:factionCode/nodes` L512 — 新增節點
- `DELETE /api/talent-trees/:factionCode/nodes/:nodeId` L542 — 刪節點
- `PUT /api/talent-trees/:factionCode/nodes/:nodeId/effects` L559 — 節點效果

### 資料庫
- `talent_trees` L494 / `talent_branches` L513 / `talent_nodes` L529 / `talent_node_effects` L561（MIGRATION_009）

---

## §3 MOD-03 敵人設計器

### 前端 `packages/client/public/admin/admin-enemy-designer.html`
**資料載入**
- `populateSelects()` L342 / `loadFamilies()` L359 / `loadSpeciesForFamily(familyId)` L362 / `loadSpeciesDetail(id)` L365 / `loadVariantDetail(id)` L368 / `loadVariantsForSpecies(speciesId)` L371

**清單渲染**
- `renderFamilyList()` L376 / `renderSpeciesList()` L386 / `renderVariantList()` L394

**選擇切換**
- `selectFamily(id)` L405 / `selectSpecies(id)` L413 / `selectVariant(id)` L420 / `hideAllPanels()` L427

**編輯器**
- `showFamilyInfo()` L429 / `showSpeciesEditor()` L446 / `showVariantEditor()` L456
- `renderStatGrid(v)` L495 / `onTierChange()` L508

**關鍵字/元素**
- `renderKeywords(v)` L523 / `toggleKw(elem, code)` L539 / `collectKeywords()` L540 / `collectKwCodes()` L542
- `renderElementConfig(v)` L550 / `toggleElem(elem)` L561 / `collectElementField(field)` L562

**狀態**
- `renderInflictedStatuses/renderSelfBuffs/renderStatusList` L566/570/574
- `addInflictedStatus()` L582 / `addSelfBuff()` L583 / `collectStatusList(containerId)` L584

**其他區塊**
- `renderResolvedAI(v)` L592 / `renderAttackCards(v)` L600 / `renderNarrative(v)` L632 / `collectNarrative()` L640 / `renderDesignChecklist(v)` L645

**髒狀態與儲存**
- `markDirty()` L659 / `updateFloatSave()` L660
- `saveCurrentEditor()` L662 / `saveSpecies()` L667 / `saveVariant()` L676
- `createSpecies()` L706 / `createVariant()` L714 / `duplicateVariant()` L723

**攻擊牌**
- `createAttackCard()` L731 / `editAttackCard(id)` L733 / `closeAcDialog()` L742 / `saveAttackCard()` L744 / `deleteAttackCard(id)` L758

**UI**
- `switchTab(tab)` L764 / `toggleSection(id)` L770 / `toggleLeftPanel()` L771

**總覽**
- `loadOverview()` L774 / `renderOverview(data)` L777

**AI**
- `getGeminiKey()` L802 / `promptApiKey()` L803 / `closeAiDialog()` L804
- `aiGeneratePool()` L806 / `aiGenerateNarrative()` L815 / `runAiGenerate()` L823
- `buildPoolPrompt()` L851 / `buildNarrativePrompt()` L866

### 後端 `packages/server/src/routes/monsters.ts`
- `validateNoArcane(body)` L15 — helper
- `GET /api/admin/monsters/stats/overview` L41
- `GET /api/admin/monsters/attack-cards`（帶 filter）L82 / `POST` L117 / `PUT /:id` L151 / `DELETE /:id` L186
- `GET /api/admin/monsters/families` L203 / `GET /:id` L222 / `PUT /:id` L235
- `GET /api/admin/monsters/species`（帶 filter）L295 / `GET /:id` L326 / `POST` L363 / `PUT /:id` L401 / `DELETE /:id` L441
- `GET /api/admin/monsters/variants`（帶 filter）L458 / `GET /:id` L490 / `POST` L547 / `PUT /:id` L651 / `DELETE /:id` L726 / `POST /:id/duplicate` L759
- `PUT /api/admin/monsters/variants/:id/status-descriptions` L872

### 資料庫
- `monster_families` L720 / `monster_species` L754 / `monster_variants` L778 / `monster_attack_cards` L813 / `monster_status_descriptions` L836（MIGRATION_010）

---

## §4 MOD-04 團隊精神

### 前端 `packages/client/public/admin/admin-team-spirit.html`
- `toggleLeftPanel()` L325 / `switchTab(tab)` L336 / `toast(msg, ok)` L346
- `loadSpirits()` L358 / `renderSpiritList()` L368
- `toggleCatGroup(header)` L409 / `computeDisplayStatus(s)` L416 / `updateGlobalStats()` L420
- `selectSpirit(id)` L440 — 選精靈編輯
- `renderDepthTable()` L498 / `toggleDepthDetail(idx)` L556 / `toggleValueRef(idx)` L561 / `updateDepth(idx, field, value)` L565
- `aiTranslateDepth(idx, srcField, dstField)` L573 — 深度描述中譯英
- `recalcStatus()` L597 / `saveSpirit()` L618 / `markComplete()` L672
- `exportAll()` L688 / `importJSON(input)` L701
- `loadCompare()` L718 / `renderCompareGrid()` L739 / `toggleCompareSelect(id)` L767 / `compareSelected()` L774
- `drawCompareChart(items)` L799 / `buildCompareTable(items)` L881 / `buildRankLists()` L908
- `getGeminiKey()` L929 / `callGemini(promptText)` L938 / `aiTranslate(srcId, dstId)` L954
- `buildSpiritContext()` L970 — AI 上下文
- `aiGenDepths()` L977 / `aiGenMilestone()` L1020 / `aiGenFull()` L1056 — 三種 AI 生成模式
- `escHtml(str)` L1134 / `escAttr(str)` L1135

### 後端 `packages/server/src/routes/team-spirits.ts`
- `GET /api/team-spirits/export` L14 / `stats` L41 / `compare` L67
- `POST /api/team-spirits/import` L113
- `GET /api/team-spirits` L194（帶 filter） / `GET /:id` L245
- `POST /api/team-spirits` L274 / `PUT /:id` L314 / `DELETE /:id` L358
- `GET /api/team-spirits/:spiritId/depths` L375 / `PUT /depths` L394 / `PUT /depths/:depth` L448

### 資料庫
- `spirit_definitions` L387 / `spirit_depth_effects` L416（MIGRATION_008）

---

## §5 MOD-05 戰鬥風格

**檔名歷史警告**：前端檔名為 `admin-proficiency.html`（舊稱「熟練度」），但當前模組名已改為「戰鬥風格」。API 與後端均使用 `combat-styles`。

### 前端 `packages/client/public/admin/admin-proficiency.html`
- `toggleLeftPanel()` L291 / `toggleRightPanel()` L298 / `toast(msg, ok)` L306
- `loadStyles()` L318 / `renderStyleList()` L328 / `updateGlobalStats()` L338 / `selectStyle(id)` L354
- `newStyle()` L385 / `saveStyle()` L403 / `deleteStyle()` L429
- `renderSpecTable()` L445 / `addSpecRow()` L464 / `saveSpec(id, btn)` L479 / `deleteSpec(id)` L493
- `renderCardList()` L505 / `renderDistribution()` L535 — 風格卡列表與分布
- `loadCard(id)` L558 / `newCard()` L577 / `clearCardForm()` L582
- `saveCard()` L599 / `saveCardAsNew()` L635 / `deleteCard()` L641
- `updateCardPreview()` L659
- `exportAll()` L691 / `importJSON(input)` L704
- `getGeminiKey()` L721 / `callGemini(prompt)` L730 / `aiTranslate(srcId, dstId)` L920
- `buildStyleCardPrompt(userInput, narrativeStyle)` L747
- `aiGenSingle(style)` L796 / `openBatchGen()` L820 / `closeBatchModal()` L828 / `runBatchGen()` L833
- `adoptBatch(idx)` L872 / `editBatch(idx)` L897 — 批次生成採用與編輯

### 後端 `packages/server/src/routes/combat-styles.ts`（>421 行）
- `GET /api/combat-styles/export` L14 / `stats` L40
- `POST /api/combat-styles/import` L58
- `GET /api/combat-styles` L134 / `GET /:id` L150 / `POST` L170 / `PUT /:id` L187 / `DELETE /:id` L206
- `GET /api/combat-styles/:styleId/specs` L223 / `POST` L235
- `PUT /api/specs/:id` L267 / `DELETE /api/specs/:id` L286
- `GET /api/combat-styles/:styleId/cards` L324 / `POST` L336
- `PUT /api/style-cards/:id` L393 / `DELETE /api/style-cards/:id` L421
- `updateStyleCounts(client, styleId)` L435 — helper

### 資料庫
- `combat_styles` L279 / `combat_specializations` L292 / `combat_style_cards` L308（MIGRATION_007）

---

## §6 MOD-06 戰役敘事

### 前端 `packages/client/public/admin/admin-campaign.html`（2288 行）
六分頁單檔 HTML + inline JS：戰役總覽 / 章節編輯 / 旗標字典 / 間章事件 / 混沌袋演變 / 完整性檢查。

**戰役列表與總覽**
- `loadCampaignList()` / `renderCampaignList()` / `selectCampaign(id)` — 左欄列表 CRUD
- `openCreateCampaignDialog()` / `confirmCreateCampaign()` — 建立戰役（自動產生十章骨架）
- `saveCampaign()` / `renderOverviewTab()` — 戰役總覽儲存
- `renderChaosBagEditor(bag)` / `collectChaosBagFromForm()` / `applyDifficultyPreset(preset)` — 起始混沌袋
- `exportCampaignJSON()` / `importCampaignJSON(event)` — 匯出匯入

**章節編輯**
- `selectChapter(id)` / `renderChapterEditor()` / `saveChapter()` — 單章節編輯
- `renderOutcomeSlots()` / `toggleOutcome(code, enabled)` / `collectOutcomesFromForm()` — A-E 結果分支槽

**旗標字典**
- `loadFlags()` / `renderFlagsList()` / `saveFlag()` / `deleteFlag(id)` — 旗標 CRUD（含反向引用檢查）
- `renderFlagMultiSelect(selected)` / `openFlagPicker(btn)` — 多選旗標元件

**間章事件**
- `loadAllInterludes()` / `renderInterludesGrouped()` / `selectInterlude(id)` — 章首/章末事件
- `saveInterludeEvent()` / `deleteInterludeEvent()` — CRUD
- `collectInterludeOperations()` / `collectInterludeChoices()` — 六種操作類型

**混沌袋演變**
- `renderEvolutionMatrix()` / `runSimulation()` / `applyAllChanges(bag, path)` — 最佳/最差/平均路徑模擬

**條件表達式編輯器**
- `renderConditionExpressionHTML(expr, depth)` / `collectConditionExpression(el)` — AND/OR/NOT/flag_set/flag_equals/act_progress_gte/agenda_progress_gte

**完整性檢查**
- `runCompletenessCheck()` / `checkCampaignLevel()` / `checkAllChapters()` — 戰役/章節/結構/旗標四類

**AI 整合**（Gemini 2.5 Flash 直連）
- `callGemini(system, user)` / `extractJson(text)` — API wrapper
- `aiGenerateCoverNarrative()` / `aiGenerateNarrativeIntro()` / `aiGenerateOutcomeNarrative(code)` — 敘事生成
- `aiGenerateInterludeNarrative()` / `aiGenerateFlagDescription()` / `aiTranslate(src, tgt)` — 其他生成點

### 前端資料 `packages/client/public/admin/data/campaign-options.json`
難度預設配置（easy/standard/hard/expert）、情境標記效果、旗標類別、場景效果建議

### 後端 `packages/server/src/routes/campaigns.ts`（1200 行）
- 戰役 CRUD：`GET/POST/PUT/DELETE /api/campaigns[/:id]`
- 章節：`GET /api/campaigns/:id/chapters` / `GET/PUT /api/chapters/:id`
- 結果分支：`GET/POST /api/chapters/:chapterId/outcomes` / `PUT/DELETE /api/outcomes/:id`
- 旗標字典：`GET /api/campaigns/:id/flags` / `GET/POST /api/campaigns/:id/flags` / `PUT/DELETE /api/flags/:id`
- 間章事件：`GET/POST /api/chapters/:chapterId/interlude-events` / `PUT/DELETE /api/interlude-events/:id`
- 匯出匯入：`GET /api/campaigns/:id/export` / `POST /api/campaigns/import`

### 後端 helper `packages/server/src/utils/campaign-validators.ts`（189 行）
- `validateFlagCodes()` / `validateMonsterFamilyCodes()` / `validateMythosCardCodes()` / `validateTeamSpiritCodes()` — 跨模組校驗
- `extractFlagCodesFromExpression()` — 從條件表達式遞迴收集旗標代碼
- `extractReferencedCodes()` — 從 operations 收集家族/神話/精神代碼

### 種子資料 `packages/server/src/db/seeds/mod06-campaigns.ts`（249 行）
示範戰役「印斯茅斯陰影」：第 1-2 章完整（敘事/結果/旗標/間章）、第 3-10 章骨架。`seedInnsmouthCampaign(client)` 在 campaigns 表為空時條件式插入。

### 資料庫（MIGRATION_017）
五張表：`campaigns` / `chapters` / `chapter_outcomes` / `campaign_flags` / `interlude_events`

---

## §7 MOD-07 關卡編輯器

### 前端 `packages/client/public/admin/admin-scenario-editor.html`（3997 行）
11 分頁單檔 HTML + inline JS（條件式顯示：重返覆寫僅 side_return、隨機地城規則僅 side_random）。

**關卡列表與選擇**
- `loadStageList()` / `renderStageList()` / `selectStage(id)` — 依類型分組的左欄列表
- `openCreateStageDialog()` / `confirmCreateStage()` / `onNewStageTypeChange()` — 建立關卡（四類型）

**關卡總覽分頁**
- `renderOverviewTab()` / `applySectionVisibility()` / `saveStage()` — 元資料/章節綁定/旗標/縮放
- `loadCampaignsForStageDropdown()` / `onCampaignChange()` — 戰役→章節聯動下拉

**場景序列分頁**
- `renderScenariosTab()` / `addScenario()` / `selectScenario(id)` / `saveScenario()` — 場景 CRUD
- `applyScenarioMode()` — 起始場景 vs 備忘錄切換
- `renderScenarioMapPreview(c)` — SVG 地圖預覽（環形排列）

**目標牌堆分頁**
- `renderActCardsTab()` / `addActCard()` / `selectActCard(id)` / `saveActCard()` — 五大任務類型、推進條件、獎勵、地圖操作

**議案牌堆分頁**
- `renderAgendaCardsTab()` / `addAgendaCard()` / `selectAgendaCard(id)` / `saveAgendaCard()` — 毀滅門檻、懲罰、地圖操作

**地圖操作指令（共用元件）**
- `renderMapOperationsEditor(el, ops, onChange)` / `renderOpsList()` / `renderParamField(p, val)` — 13 動詞動態參數
- `openVerbPickerDialog(onSelect)` — 動詞選擇對話框（7 分類）
- `updateOpParam()` / `moveOp()` / `duplicateOp()` — 指令編輯

**地點挑選對話框（共用）**
- `openLocationPicker(context, options)` / `renderLocationPickerList()` / `toggleLocationPick(code)` — 三模式：指定/權重/替換

**混沌袋配置分頁**
- `renderChaosBagTab()` / `applyChaosBagPreset(preset)` / `calculateChaosBagStats(bag)` — 四類標記 + 難度預設
- `applySuggestionsBasedOnMonsterPool()` — 家族對應場景效果建議

**遭遇/神話卡池分頁**
- `renderEncounterPoolTab()` / `openEncounterCardPicker()` / `confirmAddEncounters()` — 從 MOD-10 引用遭遇卡
- `renderMythosPoolTab()` / 對稱 API — 從 MOD-10 引用神話卡

**怪物家族池分頁**
- `renderMonsterPoolTab()` / `openFamilyPicker(role)` / `addFamilyToPool(code, role)` — 主/副家族（上限 1-2 個主）
- `openBossPicker()` / `addFixedBoss(variantId)` / `removeFixedBoss(poolId, variantId)` — 從 MOD-03 固定頭目

**重返覆寫分頁（side_return）**
- `renderReturnOverridesTab()` / `renderOverridesActCards()` / `renderOriginalConfigSummary(parent)` — 左原始/右覆寫差異介面
- `collectAllOverrides()` / `saveReturnOverrides()` / `previewResolvedStage()` — 合併結果預覽呼叫 /resolved

**隨機地城分頁（side_random）**
- `renderRandomGeneratorTab()` / `saveRandomGenerator()` / `runSeedVerification()` — 種子驗證器
- `renderGenerationPreview(r)` — 生成結果預覽

**AI 整合（Gemini 2.5 Flash 直連）**
- `aiGenerateActFrontNarrative()` / `aiGenerateAgendaFrontNarrative()` / `aiGenerateSceneNarrative()` — 敘事生成
- `aiSuggestMapLayout()` / `aiSuggestReturnOverrides()` / `aiGenerateRandomDungeonTheme()` — 建議類
- `aiTranslate(src, tgt)` / `callGemini()` / `extractJson()` — 通用

**完整性檢查**
- `runCompletenessCheck()` / `checkMetadata()` / `checkScenarios()` / `checkDecks()` / `checkPools()` / `checkReferences()` — 通用六項
- `checkTypeSpecific()` — 依關卡類型分派
- `checkMainStage()` / `checkSideStage()` / `checkSideReturnStage()` / `checkSideRandomStage()` — 四類型各自規則

### 前端資料
- `packages/client/public/admin/data/stage-options.json` — 難度預設、情境效果、家族建議
- `packages/client/public/admin/data/map-operations-options.json` — 13 動詞的動態參數 schema

### 後端 `packages/server/src/routes/stages.ts`（1691 行）
- 關卡 CRUD：`GET/POST/PUT/DELETE /api/stages[/:id]`
- 合併重返版：`GET /api/stages/:id/resolved`（執行期引擎與前端預覽共用）
- 場景：`GET /api/stages/:stageId/scenarios` / `PUT/DELETE /api/scenarios/:id` / `POST /api/scenarios/:id/reorder`
- 目標牌堆：`GET/POST /api/stages/:stageId/act-cards` / `PUT/DELETE /api/act-cards/:id` / `POST /api/act-cards/:id/reorder`
- 議案牌堆：對稱目標牌堆
- 遭遇卡池：`GET/POST /api/stages/:stageId/encounter-pool` / `PUT/DELETE /api/encounter-pool/:id`
- 神話卡池：對稱遭遇卡池
- 混沌袋：`GET/PUT /api/stages/:stageId/chaos-bag`
- 怪物家族池：`GET/POST /api/stages/:stageId/monster-pool` / `PUT/DELETE /api/monster-pool/:id`
- 隨機地城：`GET/PUT /api/stages/:stageId/random-generator` / `POST /api/stages/:stageId/random-generator/generate`
- 引用檢查：`GET /api/stages/:id/references-check`
- 匯出：`GET /api/stages/:id/export`

### 後端 helper `packages/server/src/utils/stage-validators.ts`（480 行）
- `validateLocationCodes()` / `validateMonsterFamilyCodes()` / `validateMonsterVariantIds()` — 跨模組校驗
- `validateMythosCardIds()` / `validateEncounterCardIds()` / `validateStageFlagCodes()` — 跨模組校驗
- `extractReferencedCodes(operations)` — 從地圖操作指令抽取代碼
- `extractFlagCodesFromSets(flagSets)` — 從 back_flag_sets 抽取代碼
- `validateStageReferences(stageData, client)` — 統整校驗（完整性檢查呼叫）
- `resolveReturnStage(returnStage, parentStage)` — JSON Merge Patch 合併（主入口）
- `mergeCardOverrides()` / `applyMonsterPoolOverride()` / `applyChaosBagOverride()` / `applyPoolOverride()` — 分類合併

### 後端服務 `packages/server/src/services/random-dungeon-generator.ts`（363 行）
- `generateRandomDungeon(stageId, seed, client)` — 主入口（決定論、相同 seed 相同結果）
- `seedFromString(seed)` / `mulberry32(a)` / `makeRng(seed)` — 可重現 PRNG（無外部依賴）
- `generateMap(gen, rng, client)` — 依拓撲形狀（linear/tree/mesh/hub）產生地圖
- `generateActCards()` / `generateAgendaCards()` / `generateMonsterPool()` / `generateChaosBag()` — 產出子資料
- `sampleMythosPool()` / `sampleEncounterPool()` — 從模板池取樣
- `weightedSample()` — 加權隨機取樣

### 資料庫（MIGRATION_018）
九張表：`stages` / `scenarios` / `stage_act_cards` / `stage_agenda_cards` / `stage_encounter_pool` / `stage_mythos_pool` / `stage_chaos_bag` / `stage_monster_pool` / `random_dungeon_generators`

---

## §8 MOD-08 地點設計器

### 前端 `packages/client/public/admin/admin-location-designer.html`
**utility**
- `el/intVal` L701/702 / `escapeHtml(s)` L704
- `toggleLeftPanel()` L709 / `toggleSection(id)` L714 / `switchTab(name)` L721
- `openDialog(id)` L731 / `closeDialog(id)` L732 / `markDirty()` L734 / `clearDirty()` L739 / `showStatus(elId, text, color)` L744

**AI**
- `promptApiKey()` L754 / `callGemini(promptText, opts)` L764
- `buildSvgPrompt(name_zh, scale, prompt)` L1125 / `generateSVG()` L1169
- `aiTranslateDesc()` L1487

**地點主流程**
- `loadLocations()` L796 / `renderLocList()` L823 / `onFilterChange()` L853
- `loadTags()` L860 / `openCreateDialog()` L889 / `confirmCreate()` L898
- `selectLocation(id)` L929 / `renderEditor()` L945 / `onScaleSelectChange()` L999
- `renderTagsSection()` L1008 / `addTag(tagId)` L1046 / `removeTag(tagId)` L1062

**SVG 藝術**
- `getArtType()` L1072 / `onArtTypeChange()` L1077
- `updateSvgPreview()` L1097 / `clearSVG()` L1118
- `openSvgEditor()` L1211 / `updateSvgEditorPreview()` L1217 / `saveSvgFromEditor()` L1227

**隱藏資訊**
- `renderHiddenInfo()` L1244 / `formatRevealCondition(h)` L1272 / `formatReward(h)` L1281
- `openHiddenInfoDialog()` L1291 / `editHiddenInfo(id)` L1310
- `onRevealTypeChange()` L1333 / `onRewardTypeChange()` L1339
- `saveHiddenInfo()` L1346 / `deleteHiddenInfo(id, name)` L1395

**可發現卡**
- `renderDiscoverableCards()` L1404 / `removeDiscoverableCard(id)` L1419
- `openCardPickerDialog()` L1425 / `renderCardPicker(list)` L1444 / `filterCardPicker()` L1460 / `toggleCardPick(id, checked)` L1470 / `confirmCardPick()` L1475

**完成度與儲存**
- `renderCompleteness()` L1505 / `saveLocation()` L1531 / `showToast(msg)` L1608
- `reloadCurrentLocation()` L1616 / `duplicateLocation()` L1628
- `deleteLocation()` L1642 / `confirmDelete()` L1661

**標籤管理**
- `renderTagManage()` L1708 / `openTagDialog(tagId, defaultCat)` L1736 / `saveTag()` L1766 / `deleteTag(id, name)` L1799

**總覽**
- `loadOverview()` L1808 / `renderOverview(d)` L1819

### 後端 `packages/server/src/routes/locations.ts`
- `GET /api/admin/locations/stats/overview` L42
- 標籤：`GET /tags` L103 / `POST /tags` L121 / `PUT /tags/:tagId` L143 / `DELETE /tags/:tagId` L167 / `PUT /:id/tags` L196
- 隱藏資訊：`PUT /hidden-info/:infoId` L236 / `DELETE /hidden-info/:infoId` L284 / `POST /:id/hidden-info` L300 / `PUT /:id/hidden-info/reorder` L347
- SVG：`PUT /:id/svg` L378
- 地點 CRUD：`GET /api/admin/locations` L406 / `POST` L464 / `GET /:id` L499 / `PUT /:id` L533 / `DELETE /:id` L587 / `POST /:id/duplicate` L618

### 資料庫
- `locations` L930 / `location_hidden_info` L977 / `location_style_tags` L998 / `location_tag_map` L1015（MIGRATION_011）

---

## §9 MOD-09 鍛造製作

### 前端 `packages/client/public/admin/admin-forge-craft.html`
**分頁與列表**
- `switchTopTab(tab)` L381 / `toast(msg, isErr)` L393 / `setDirty(on)` L402
- `renderCatNav()` L410 / `selectCategory(code)` L428
- `filterAffixes()` L437 / `renderAffixList()` L450 / `escapeHtml(s)` L470

**詞條（Affix）**
- `loadAffixes()` L478 / `selectAffix(id)` L495 / `renderAffixEditor()` L516
- `getTierBadgeCls(label)` L596 / `onSubtypeChange()` L604 / `onTierModeChange()` L611
- `collectAffixForm()` L621 / `saveAffix()` L635 / `createAffix()` L659 / `deleteAffix()` L688 / `validateAffix()` L709

**階層（Tier）**
- `openTierModal()` L729 / `renderTierModal()` L746 / `addTierRow()` L770 / `removeTierRow(idx)` L785 / `closeTierModal()` L794 / `saveAllTiers()` L798

**詞條預覽**
- `renderAffixPreview()` L843 / `renderPreviewTable()` L877 / `renderPreviewMatrix()` L894

**總覽**
- `loadOverview()` L917 / `renderOverviewMatrix()` L969

**配方（Recipe）**
- `setRecipeDirty(on)` L1013 / `loadRecipes()` L1018 / `renderRecipeUnlockOptions()` L1029 / `renderRecipeList()` L1035
- `selectRecipe(id)` L1063 / `renderRecipeEditor()` L1078 / `collectRecipeForm()` L1144
- `saveRecipe()` L1159 / `createRecipe()` L1178 / `deleteRecipe()` L1194
- `renderRecipePreview()` L1210

**配方素材**
- `openRecMatModal(matId)` L1269 / `editRecipeMaterial(matId)` L1284 / `renderRecMatModal()` L1286 / `closeRecMatModal()` L1315
- `saveRecipeMaterial()` L1320 / `deleteRecipeMaterial(matId)` L1347

**素材（Material）**
- `loadMaterials()` L1363 / `renderMatCatNav()` L1375 / `selectMatCategory(code)` L1392 / `renderMaterialList()` L1398
- `openMatModal(id)` L1421 / `renderMatModal()` L1438 / `closeMatModal()` L1457
- `saveMaterial()` L1462 / `createMaterial()` L1489 / `deleteMaterial()` L1491

**卡片引用**
- `loadCards()` L1506

**AI**
- `getGeminiKey()` L1517 / `openGeminiKeyDialog()` L1521 / `callGemini(systemPrompt)` L1530
- `showAIModal(title, html)` L1556 / `closeAIModal()` L1562
- `aiSuggestAffix()` L1567 / `applyAIName(zhEncoded, enEncoded)` L1635 / `applyAIDesc(zhEncoded, enEncoded)` L1644
- `aiSuggestRecipeNarrative()` L1654 / `applyAINarrative(narrEncoded)` L1698
- `aiSuggestMaterialNames()` L1706 / `applyAIMaterialName(categoryCode, level, zhEncoded, enEncoded)` L1754

### 後端 `packages/server/src/routes/forge-craft.ts`
**helpers**
- `getSV(level)` L8 / `validateAffixTiers(affix, tiers)` L16
- 常數：`TIER_MODES` L5 / `DESIGN_STATUSES` L6

**素材**
- `GET /api/materials/categories` L54
- `GET /api/materials`（帶 filter）L83 / `GET /:id` L111 / `POST` L122 / `PATCH /:id` L150 / `DELETE /:id` L172
- `GET /api/materials/by-family/:monster_family_id` L67

**詞條**
- `GET /api/affixes`（帶 filter）L193 / `GET /:id` L228 / `POST` L240 / `PATCH /:id` L274 / `DELETE /:id` L301
- `GET /api/affixes/:id/validate` L312
- `POST /api/affixes/:id/tiers` L331 / `PATCH /:id/tiers/:tier_id` L359 / `DELETE /:id/tiers/:tier_id` L388
- `GET /api/forging/preview` L402 / `POST /api/forging/preview-batch` L423
- `GET /api/forging/stats` L457

**配方**
- `GET /api/recipes`（帶 filter）L487 / `GET /:id` L521 / `POST` L533 / `PATCH /:id` L565 / `DELETE /:id` L588
- `GET /api/recipes/:id/validate` L599
- `POST /api/recipes/:id/materials` L619 / `PATCH /:id/materials/:mat_id` L643 / `DELETE /:id/materials/:mat_id` L670
- `GET /api/crafting/stats` L687

### 資料庫
- `material_categories` L1877 / `material_definitions` L1890 / `forging_affixes` L1932 / `forging_affix_tiers` L1955 / `crafting_recipes` L1972 / `crafting_recipe_materials` L1996（MIGRATION_014）
- MIGRATION_015（詳見 migrate.ts L2064）— MOD-09 Part 2 擴充

---

## §10 MOD-10 城主設計器

### 前端 `packages/client/public/admin/admin-keeper-designer.html`
**utility**
- `el/intVal` L867/868 / `escapeHtml(s)` L869
- `toggleLeftPanel()` L873 / `toggleSection(id)` L874 / `openDialog/closeDialog` L878/879
- `markDirty()` L880 / `clearDirty()` L881 / `showToast(msg, color)` L882

**AI**
- `promptApiKey()` L893 / `callGemini(promptText, opts)` L902 / `aiTranslate(srcId, dstId)` L925

**初始化**
- `populateSelect(selectEl, dict, includeAll, allLabel)` L939 / `populateAllStaticSelects()` L954

**資料載入**
- `loadTags()` L1000 / `loadMonsterFamilies()` L1026
- `switchLibrary(lib)` L1040 / `switchTab(name)` L1058
- `loadMythosCards()` L1071 / `loadEncounterCards()` L1092
- `renderList()` L1110

**建立**
- `openCreateDialog()` L1153 / `confirmCreateMythos()` L1167 / `confirmCreateEncounter()` L1193
- `suggestIntensity(cost)` L1217

**選擇與編輯**
- `selectCard(id)` L1227 / `renderEditor()` L1243 / `renderMythosEditor()` L1260

**神話卡動作**
- `onActionCostChange()` L1288 / `onCategoryChange()` L1294 / `onTimingChange()` L1305
- `renderActionList()` L1311 / `openAddActionDialog()` L1341 / `confirmAddAction()` L1361 / `editAction(effectId)` L1371
- `renderActionParamsForm(actionCode, params)` L1382 / `collectActionParams()` L1486
- `updateAutoDescription()` L1504 / `generateActionDescription(code, params)` L1510
- `saveActionParams()` L1558 / `deleteAction(effectId)` L1582
- `renderMythosCompleteness()` L1591

**遭遇卡**
- `renderEncounterEditor()` L1610 / `renderEncounterTagsSection()` L1626 / `addETag(tagId)` L1657 / `removeETag(tagId)` L1668
- `renderEncounterOptions()` L1675 / `renderOptionCard(o)` L1688
- `renderEffectChips(effects, optionId, branch)` L1742 / `formatEffectText(e)` L1753
- `updateOptionField(optionId, field, value)` L1764 / `debouncedSaveOption(optionId)` L1774 / `savEncounterOption(optionId)` L1779 / `toggleOptionCheck(optionId, checked)` L1805
- `addEncounterOption()` L1816 / `deleteEncounterOption(optionId)` L1831
- `openAddEffectDialog(optionId, branch)` L1846 / `renderEffectParamsForm()` L1855 / `confirmAddEffect()` L1883 / `removeEffect(optionId, branch, idx)` L1903
- `renderEncounterCompleteness()` L1913

**共用**
- `saveCurrent()` L1935 / `reloadCurrent()` L2001 / `duplicateCurrent()` L2016 / `deleteCurrent()` L2033 / `confirmDelete()` L2040

**AI 生成**
- `openAIGenMythos()` L2064 / `buildMythosPrompt(category, timing, intensity, cost, userNote)` L2076 / `generateMythosCard()` L2128
- `openAIGenEncounter()` L2188 / `buildEncounterPrompt(encType, tagNames, userNote)` L2198 / `generateEncounterCard()` L2247

**總覽/平衡**
- `loadOverview()` L2317 / `renderOverview(d)` L2328
- `loadBalance()` L2424 / `renderBalance()` L2436 / `updateBalanceSetting(key, value)` L2471 / `runSimulation()` L2481

### 後端 `packages/server/src/routes/keeper.ts`
**統計/平衡**
- `GET /api/admin/keeper/stats/overview` L37
- `GET /api/admin/keeper/balance` L98 / `POST /balance/simulate` L117
- `GET /balance/:setting_key` L170 / `PUT /balance/:setting_key` L182

**神話卡效果**
- `PUT /api/admin/keeper/mythos-effects/:effect_id` L205 / `DELETE` L231
- `POST /api/admin/keeper/mythos-cards/:id/effects` L247 / `PUT /effects/reorder` L274

**神話卡**
- `GET /api/admin/keeper/mythos-cards` L303 / `POST` L330 / `GET /:id` L359 / `PUT /:id` L376 / `DELETE /:id` L423
- `POST /:id/duplicate` L435

**遭遇卡選項**
- `PUT /api/admin/keeper/encounter-options/:option_id` L488 / `DELETE` L541
- `POST /api/admin/keeper/encounter-cards/:id/options` L567

**遭遇卡標籤**
- `PUT /api/admin/keeper/encounter-cards/:id/tags` L618

**遭遇卡**
- `GET /api/admin/keeper/encounter-cards` L649 / `POST` L697 / `GET /:id` L736 / `PUT /:id` L769 / `DELETE /:id` L810 / `POST /:id/duplicate` L822

### 資料庫
- `mythos_cards` L1114 / `mythos_card_effects` L1150 / `encounter_cards` L1166 / `encounter_card_options` L1190 / `encounter_card_tag_map` L1217 / `game_balance_settings` L1228（MIGRATION_012）

---

## §11 MOD-11 調查員設計器

### 前端 `packages/client/public/admin/admin-investigator-designer.html`
**基礎**
- `api(url, opts)` L575 — 封裝 fetch
- `init()` L589 / `switchView(v)` L619 / `switchTab(t)` L627

**矩陣**
- `refreshMatrix()` L637 / `renderMatrix()` L648 / `refreshMatrixCell(id)` L766

**載入**
- `loadInvestigator(id)` L697 / `renderAll()` L704 / `renderBanner()` L715 / `renderProgress()` L722 / `renderBasic()` L737

**基本欄位**
- `saveField(k)` L753

**屬性**
- `renderAttrs()` L779 / `adjFreeAttr(key, delta)` L815 / `saveAttributes()` L829 / `resetAttributesToBase()` L840

**熟練**
- `renderProf()` L851 / `addProficiency()` L867 / `removeProf(pid)` L880 / `applyRecommendedProf()` L889
- `saveAbilityTextValue()` L899 / `saveAbilityTextSource()` L907

**Slot 與簽名/弱點卡**
- `renderSlots()` L918
- `openRight(id)` L944 / `closeRightEditor()` L950
- `newSig(order)` L952 / `editSig(cid)` L970
- `renderCommitIcons(icons)` L989 / `addCommitIcon()` L999 / `updCommit(i, k, v)` L1004 / `delCommit(i)` L1005
- `renderBreakdown(items)` L1007 / `addBreakdownItem()` L1018 / `updBreakdown(i, v)` L1026 / `delBreakdown(i)` L1027
- `recomputeSigValue()` L1029
- `saveSignatureCard()` L1043 / `deleteSignatureCard()` L1070
- `newWk()` L1079 / `editWk()` L1089 / `updWkFinalV()` L1107 / `saveWeakness()` L1113 / `deleteWeakness()` L1133

**起始牌組**
- `renderDeckTab()` L1143 / `calcAvgCost(deck)` L1153 / `renderDeckList(deck)` L1162 / `renderDeckAnalysis(deck)` L1190
- `validateDeck()` L1212 / `loadAvailableCards()` L1224
- `deckAdd(cardId)` L1242 / `deckQty(slotId, delta)` L1251 / `deckRemove(slotId)` L1262

**樣板**
- `createCustomTemplate()` L1271 / `cloneTemplate()` L1278 / `clearTemplate()` L1284 / `deleteTemplate()` L1291

**AI**
- `openGeminiKeyDialog()` L1304 / `callGemini(systemPrompt)` L1309
- `showAI(title, html)` L1327 / `closeAI()` L1333
- `aiGenerateBackground()` L1335 / `aiGenerateSigFlavor()` L1373 / `aiGenerateWeakness()` L1389
- `aiEstimateAbilityValue()` L1406 / `aiBreakdownSig()` L1439

**總覽/平衡**
- `renderOverview()` L1460 / `renderBalance()` L1519
- `renderScatter(matrix, mean, sd)` L1537 / `renderBoxplot(matrix)` L1564 / `median(arr)` L1590 / `renderStackChart(matrix)` L1592
- `renderWarnings(stats, matrix)` L1623 / `reloadBalance()` L1642
- `openValueConfig()` L1648 / `exportBalanceCSV()` L1664

### 後端 `packages/server/src/routes/investigators.ts`
**helpers**
- `ATTR_KEYS` / `NARRATIVE_KEYS` / `MBTI_STRUCTURAL_KEYS` L5/10/16
- `sumAttrs(t)` L18 / `computeIsCompleted(invId)` L22 / `refreshIsCompleted(invId)` L51

**預設/總覽**
- `GET /api/admin/investigators/preset-matrix` L68
- `GET /api/admin/investigators/stats` L86
- `GET /api/admin/investigators/value-matrix` L122 / `value-stats` L137
- `POST /api/admin/investigators/recalculate-all` L169

**價值配置**
- `GET /api/admin/value-config` L175 / `PATCH` L180
- `GET /api/admin/faction-attribute-map` L193
- `GET /api/admin/proficiencies` L199

**調查員 CRUD**
- `GET /api/admin/investigators` L213 / `GET /:id` L243
- `GET /:id/value-breakdown` L276 / `POST /:id/recalculate-value` L318
- `POST` L328 / `PATCH /:id` L379 / `DELETE /:id` L412
- `POST /:id/clear` L424 / `POST /:id/clone` L467

**簽名卡**
- `GET /:id/signature-cards` L496 / `POST` L505 / `PATCH /signature-cards/:cid` L553 / `DELETE /signature-cards/:cid` L581

**弱點**
- `GET /:id/weakness` L592 / `PUT /:id/weakness` L598 / `DELETE /:id/weakness` L645

**起始牌組**
- `GET /:id/starting-deck` L659 / `POST /cards` L676 / `PATCH /cards/:slot` L716 / `DELETE /cards/:slot` L728
- `GET /:id/starting-deck/validate` L744 / `GET /:id/available-cards` L769

### 資料庫
- `faction_attribute_map` L1403 / `investigator_templates` L1452 / `investigator_signature_cards` L1523 / `investigator_weaknesses` L1552 / `investigator_starting_deck` L1574 / `investigator_value_config` L1593（MIGRATION_013）

---

## §12 共用前端 `packages/client/public/admin/admin-shared.js`（905 行）

**API 基礎**
- `ADMIN_API_BASE` L19 — 自動判斷 API 根 URL（dev port 3001 / prod 同源）
- `window.ADMIN_API_BASE` L23 — 全域公開

**認證**
- `checkAdminAuth()` L28 — 檢查 localStorage 的 admin_token/admin_user
- `adminFetch(url, options)` L56 / `window.adminFetch` L69 — 帶 JWT Bearer token 的 fetch 封裝
- `adminLogout()` L74 / `window.adminLogout` L79 — 登出並清 localStorage

**屬性計算**
- `calculateBaseAttributes(mbti)` L156 — MBTI → 七屬性基礎值

**檢定/擲骰**
- `hitRate(modifier, dc)` L707 — 命中率
- `fmtPercent(rate)` L719 — 百分比字串
- `randInt(min, max)` L726 — 整數隨機
- `rollD20()` L733 — d20 擲骰

**鍛造計算**
- `getMaterialSV(level)` L762 — 素材等級→SV
- `calcForgingQuantity(affixValue, materialLevel)` L773 — 鍛造數量

**模組說明 Modal**
- `_loadModuleHelp()` L816 — 載入 `data/module-help.json`
- `_ensureHelpModalDOM()` L828 — Modal DOM 建立
- `openModuleHelp(code)` L856 / `closeModuleHelp()` L898 — 開關
- `_switchHelpTab(idx)` L882 / `_renderHelpSection(sections, idx)` L890 — 內容渲染
- `window.openModuleHelp` L903 / `window.closeModuleHelp` L904 / `window._switchHelpTab` L905 — 全域公開

---

## §13 Admin 首頁 `packages/client/public/admin/index.html`（257 行）

- L236 讀取 `localStorage.admin_user` 判斷角色
- L241 `document.querySelector('.module-grid')` 為 12 個模組卡片容器
- L243 動態建立 MOD-12 卡片（僅 admin/owner 可見）
- 其餘為靜態 HTML 結構（模組卡片均為 `<a href="admin-*.html">`）

---

## §14 後端基礎設施

### `packages/server/src/index.ts`（24 行）
- `start()` L7 — bootstrap：`runMigrations()` → `createDefaultAdmin()` → `buildApp()` → `app.listen`
- 預設 port：`process.env.PORT || 3001`

### `packages/server/src/app.ts`
- `buildApp()` L17 — 建立 Fastify 實例、註冊 cors、註冊 14 個 route plugin
- 註冊順序：health / auth / cards / combatStyle / teamSpirit / talentTree / monster / location / **campaigns** / **stages** / keeper / investigator / forgeCraft / aiConsole

### `packages/server/src/routes/index.ts`
- Barrel 檔：re-export 所有 routes plugin

### `packages/server/src/routes/health.ts`（27 行）
- `GET /health` L5 — 回 status/db/tables/adminCount/seedCounts（診斷用）

### `packages/server/src/routes/auth.ts`
- `const JWT_SECRET` L6 — 從 `ADMIN_JWT_SECRET` env，fallback 警告用
- `const SESSION_HOURS` L7 — 從 `ADMIN_SESSION_HOURS` env
- `POST /api/auth/login` L12 — 帳密登入，回 JWT token
- `GET /api/auth/me` L56 — 當前使用者資訊（需 token）
- `POST /api/auth/logout` L77 — 登出（client 端清 token）

### `packages/server/src/middleware/auth.ts`
- `requireAuth(request, reply)` L6 — 解析 `Authorization: Bearer ...` JWT token，失敗 401
- cards/combat-styles 等透過 `app.addHook('preHandler', requireAuth)` 全檔掛載

### `packages/server/src/middleware/requireAdmin.ts`
- `requireAdmin(request, reply)` L8 — 檢查 JWT role ∈ {admin, owner}，失敗 403
- MOD-12 使用

### `packages/server/src/db/pool.ts`
- `pool` L9 — pg Pool instance，dbUrl/SSL 自動偵測（Railway production）

### `packages/server/src/db/seed.ts`
- `createDefaultAdmin()` L4 — 首次啟動若無 admin 使用者，用 `ADMIN_PASSWORD` env bcrypt 建立 owner

### `packages/server/src/db/migrate.ts`（2260+ 行）
**Migrations（runMigrations 依序執行）**
- MIGRATION_001 L28 — `card_definitions` / L76 `card_effects` / L95 `admin_users` / L108 `admin_sessions`
- MIGRATION_002 L122 — card_definitions 新增 combat_style / attribute_modifiers，刪除舊欄位
- MIGRATION_003 L157 — card_definitions 新增 spell_type / spell_casting / xp_cost
- MIGRATION_004 L178 — card_definitions 新增 commit_icons / consume_enabled / consume_effect
- MIGRATION_005 L199 — card_definitions 新增 is_book / is_relic / study_* / upgrades
- MIGRATION_006 L251 — card_definitions 新增 is_exceptional / transform_*
- MIGRATION_007 L274 — `combat_styles` / `combat_specializations` / `combat_style_cards`（MOD-05）
- MIGRATION_008 L382 — `spirit_definitions` / `spirit_depth_effects`（MOD-04）
- MIGRATION_009 L489 — `talent_trees` / `talent_branches` / `talent_nodes` / `talent_node_effects`（MOD-02）
- MIGRATION_010 L715 — `monster_families` / `monster_species` / `monster_variants` / `monster_attack_cards` / `monster_status_descriptions`（MOD-03）
- MIGRATION_011 L922 — `locations` / `location_hidden_info` / `location_style_tags` / `location_tag_map`（MOD-08）
- MIGRATION_012 L1108 — `mythos_cards` / `mythos_card_effects` / `encounter_cards` / `encounter_card_options` / `encounter_card_tag_map` / `game_balance_settings`（MOD-10）
- MIGRATION_013 L1396 — `faction_attribute_map` / `investigator_templates` / `investigator_signature_cards` / `investigator_weaknesses` / `investigator_starting_deck` / `investigator_value_config`（MOD-11）
- MIGRATION_014 L1865 — card_definitions.is_temporary / `material_categories` / `material_definitions` / `forging_affixes` / `forging_affix_tiers` / `crafting_recipes` / `crafting_recipe_materials`（MOD-09）
- MIGRATION_015 L2064 — MOD-09 Part 2 擴充
- MIGRATION_016 L2211 — `ai_console_tasks`（MOD-12，索引排除）
- MIGRATION_017 L2245 — `campaigns` / `chapters` / `chapter_outcomes` / `campaign_flags` / `interlude_events`（MOD-06）
- MIGRATION_018 L2342 — `stages` / `scenarios` / `stage_act_cards` / `stage_agenda_cards` / `stage_encounter_pool` / `stage_mythos_pool` / `stage_chaos_bag` / `stage_monster_pool` / `random_dungeon_generators`（MOD-07）
- `seedInnsmouthCampaign(client)` L2347 — 條件式插入示範戰役（僅當 campaigns 表為空）

### `packages/server/src/utils/dice.ts`
- `rollD20()` L2 — 後端擲骰
- `skillCheck(...)` L6 — 檢定判定

### `packages/server/src/utils/campaign-validators.ts`（189 行，MOD-06）
跨模組校驗：旗標/家族/神話卡/精神代碼存在性檢查；從條件表達式遞迴收集旗標。詳見 §6。

### `packages/server/src/utils/stage-validators.ts`（480 行，MOD-07）
跨模組校驗 + 重返版 JSON Merge Patch 合併演算法。詳見 §7。

### `packages/server/src/services/random-dungeon-generator.ts`（363 行，MOD-07）
決定論隨機地城生成服務（mulberry32 PRNG，無外部依賴）。詳見 §7。

### `packages/server/src/db/seeds/mod06-campaigns.ts`（249 行，MOD-06）
示範戰役「印斯茅斯陰影」種子資料常數。由 `migrate.ts` 的 `seedInnsmouthCampaign` 匯入使用。

---

## §15 共用型別 `packages/shared/src/`

### `index.ts`
- `export * from './types'` / `export * from './constants'`

### `types/card.ts`
- `CardType` L1 — 'skill' | 'item' | 'spell' | 'weakness'
- `Card` interface L3

### `types/campaign.ts`
- `Campaign` interface — 戰役主體（code/name_zh/theme/difficulty_tier/initial_chaos_bag 等）
- `Chapter` interface — 章節
- `ChapterOutcome` interface — 結果分支 A-E
- `CampaignFlag` interface — 旗標字典條目
- `InterludeEvent` interface — 間章事件

### `types/combat.ts`
- `SkillCheckResult` interface L1
- `EnemyTier` L10 — 'minion' | 'threat' | 'elite' | 'boss' | 'titan'
- `Enemy` interface L12

### `types/investigator.ts`
- `Investigator` interface L3

### `types/scenario.ts`（MOD-07）
- `StageType` — 'main' | 'side' | 'side_return' | 'side_random'
- `MonsterRole` — 'primary' | 'secondary'
- `DifficultyPreset` — 'easy' | 'standard' | 'hard' | 'expert'
- `ObjectiveType` — 五大任務類型聯合
- `Stage` / `Scenario` / `StageActCard` / `StageAgendaCard` interface — 關卡主要結構
- `MapOperation` interface — 地圖操作指令（verb + params）
- `StageChaosBag` / `StageMonsterPoolEntry` / `StageEncounterPoolEntry` / `StageMythosPoolEntry` interface — 子資源
- `RandomDungeonGenerator` interface — 隨機地城產生器

### `constants/attributes.ts`
- `ATTRIBUTES` L1 — 七屬性常數
- `AttributeId` type L11

### `constants/game-rules.ts`
- `GAME_RULES` L2 — 遊戲規則常數

---

## §16 客戶端 SPA（玩家端，尚未實作）

- `packages/client/src/main.tsx` — React 入口
- `packages/client/src/App.tsx` — 主 App（玩家遊戲 UI 目前未建）

Admin 後台走單檔 HTML 路線，不走 React SPA。兩邊共用同一 Vite dev server。

---

## §17 文件目錄 `docs/`

### 規則書（v1_0，2026 年版）
- `規則書v1_0_第一章_核心設計原則.md`
- `規則書v1_0_第二章_遊戲規則總綱.md`
- `規則書v1_0_第三章_卡片系統總綱.md`
- `規則書v1_0_第四章_成長系統總綱.md`
- `規則書v1_0_第五章_場景系統總綱.md`
- `規則書v1_0_第六章_數值規格.md`

### 工具/安裝指南
- `01_Ollama_Gemma4_安裝指南.md`
- `02_gemma-bridge_Claude_Code_指令書.md`
- `03_使用Prompt範本與測試清單.md`

### SystemPrompt 模板庫（gemma-bridge/prompts/ 來源文件）
- `04_SystemPrompt模板庫_卡片設計篇.md`
- `05_SystemPrompt模板庫_天賦樹敵人場景篇.md`
- `06_SystemPrompt模板庫_Combo陣營構築篇.md`

### MOD-12 相關（本索引排除，但列檔名以便溯源）
- `Claude_Code_MOD12_AI_Console_指令_Part1.md` / `Part2.md` / `Part3.md`
- `MOD12_指令修正補充_v1.md`
- `gemma-bridge_錯誤修正檢查清單.md`

### 狀態/事故紀錄
- `狀態紀錄_26041801.md`
- `事故紀錄_26041802_gemma-bridge架構與術語錯誤.md`

### 舊版封存
- `docs/old/` — 舊版設計文件歸檔

---

## §18 本索引維護規則

- 檔名採 `檔案索引_vNN_YYMMDDXX.md` 格式（與專案既有文件命名一致）
- 版次升級條件：
  - **小更新（修正行號、補函式）**：直接修改當前版，更新頂部日期碼
  - **大重構（新增 MOD、大量檔案移動）**：開新版次 v02、v03…，舊版保留於 `docs/old/`
- 每個 session 開機應先讀 `MEMORY.md` → 找到此索引連結 → 讀取最新版
- 若此索引與實際程式碼不一致，以**程式碼為準**，並立即修正索引
